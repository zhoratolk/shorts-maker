---
phase: 06-tiktok-instagram-auto-publish
plan: 07
type: execute
wave: 4
depends_on: ["06-05", "06-06"]
files_modified:
  - tests/test_tiktok_publish.py
  - tests/test_instagram_publish.py
  - docs/publish-queue.md
autonomous: true
requirements: [PUB-06, PUB-07]
tags: [publish, tiktok, instagram, isolation, docs]

must_haves:
  truths:
    - "Killing or pausing a TikTok queue entry never modifies work/_publish/queue.json (YouTube) or work/_publish/instagram_queue.json, and vice versa for Instagram - structural isolation (Success Criterion 3), proven by a test, not just asserted by convention"
    - "TIKTOK_SCOPES/INSTAGRAM_SCOPES and the authorize URLs built from them request only the documented minimal scopes, never a broader one (V4)"
    - "The operator has a complete, accurate runbook for registering both platforms' apps, running the one-time OAuth consent, flipping tiktok_enabled/instagram_enabled, and extending the periodic Task Scheduler check - including the user's explicit build-both-scenarios instruction for Instagram, stated as an instruction, not buried"
  artifacts:
    - "tests/test_tiktok_publish.py and tests/test_instagram_publish.py each gain an isolation test proving cross-file non-interference"
    - "docs/publish-queue.md gains TikTok and Instagram operator sections (app registration, one-time consent, dry-run-to-live opt-in, Task Scheduler extension, and the Instagram Standard-vs-Advanced-Access attempt-first runbook)"
  key_links:
    - "The isolation test constructs real tmp-dir queue files for all three platforms and asserts byte-for-byte non-interference after a kill/pause operation on only one"
    - "docs/publish-queue.md's Instagram section explicitly instructs 'attempt first, file App Review only if InstagramAccessError says so' - never 'file App Review before you try'"
---

<objective>
Close out Phase 6 with the two things that can only be verified once both platform modules exist: a cross-file isolation test proving Success Criterion 3 (one platform's bug/audit-delay structurally cannot touch another platform's queue or YouTube's), a scope-minimization test, and the operator-facing documentation an actual human needs to register both platforms' apps and go live - including the precise, non-negotiable wording of the user's Instagram build-both-scenarios decision so a future session (or a future you) doesn't accidentally "fix" instagram_publish.py by adding a TikTok-style pre-publish gate.

Purpose: Success Criterion 3 ("one platform's audit/review delay does not block the other platform or YouTube") is a structural claim about file isolation - it needs a test that actually proves it, not just three separate manifest paths that happen to never collide by construction. D-02/D-04 require an explicit runbook for the (human-only) app registration steps; 06-RESEARCH.md's Open Question 1 requires that runbook to correctly reflect the user's resolution (attempt Standard Access first) rather than the pre-resolution "always needs review" assumption.

Output: Isolation + scope tests in both test files; `docs/publish-queue.md` extended with complete TikTok/Instagram operator sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-CONTEXT.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-RESEARCH.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-PATTERNS.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-VALIDATION.md

# The operator-doc precedent this plan extends (structure, tone, "Kill-path
# verification"-style unverified-assumption flagging):
@docs/publish-queue.md

# Both modules this plan tests/documents (built across Plans 06-03/06-05 and 06-04/06-06):
@scripts/publish_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isolation and scope-minimization tests</name>
  <files>tests/test_tiktok_publish.py, tests/test_instagram_publish.py</files>
  <read_first>06-VALIDATION.md rows 06-03-01 (isolation) and 06-03-02 (scope-minimization) for the exact behavior contract</read_first>
  <action>In tests/test_tiktok_publish.py, add a test that: creates three separate tmp-dir files (tiktok_queue.json, instagram_queue.json, queue.json - the latter two representing the other two platforms' manifests, populated with at least one entry each including realistic fields), captures instagram_queue.json's and queue.json's raw bytes before, calls tiktok_publish.py's pause_item and kill_item against ONLY the tiktok_queue.json-backed queue object (loaded via tiktok_publish.load_queue), saves only that queue back to tiktok_queue.json, then re-reads instagram_queue.json and queue.json from disk and asserts their bytes are unchanged - proving tiktok_publish.py's functions never open, read, or write either other file (Success Criterion 3). Add a second test asserting TIKTOK_SCOPES == ["video.publish", "video.upload"] exactly, and that the authorize URL string built by run_tiktok_oauth_consent's URL-construction step (or the module-level template it uses) contains "video.publish,video.upload" as the scope parameter and no other scope substring (V4). Mirror both tests (with tiktok/instagram swapped) into tests/test_instagram_publish.py: instagram's isolation test proves instagram_publish.py's pause_item/kill_item never touch tiktok_queue.json or queue.json; its scope test asserts INSTAGRAM_SCOPES == ["instagram_business_basic", "instagram_business_content_publish"] exactly and the authorize URL contains only those two scope names.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_tiktok_publish.py tests/test_instagram_publish.py -k "isolation or scope" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>Both isolation tests pass, proving byte-for-byte non-interference between all three platforms' manifest files after a kill/pause operation on only one; both scope tests pass, confirming no broader-than-documented OAuth scope is ever requested; full combined suite (`pytest tests/test_tiktok_publish.py tests/test_instagram_publish.py --basetemp=D:/shorts-maker/.pytest-tmp`) green.</done>
</task>

<task type="auto">
  <name>Task 2: Operator documentation for TikTok and Instagram</name>
  <files>docs/publish-queue.md</files>
  <read_first>docs/publish-queue.md in full - sections 1-5 (Task Scheduler setup, dry-run-to-live opt-in, daily grid, notifications, manual/pause/kill) and the "Kill-path verification" section's tone/structure for flagging an empirically-unverified assumption</read_first>
  <action>Append two new numbered sections to docs/publish-queue.md (continuing the existing numbering, i.e. "## 6." and "## 7."), following section 1-5's exact tone/structure (short prose + fenced command blocks, not a wall of text), plus one final unnumbered subsection extending "Kill-path verification"'s pattern for a new unverified assumption:

"## 6. TikTok setup and going live (PUB-06, D-02/D-04)" covering: (a) TikTok Developer Portal app registration steps and the exact video.publish+video.upload scope, redirect URI http://127.0.0.1:8765/callback, saving client_key/client_secret into tiktok_client_key.json; (b) running the one-time interactive consent (python -c "from scripts.tiktok_publish import run_tiktok_oauth_consent; run_tiktok_oauth_consent('tiktok_client_key.json', 'tiktok_token.json')" or an equivalent one-liner matching whatever the actual function signature ended up being in Plan 06-03); (c) the config.yaml publish.tiktok_enabled: true opt-in, worded with the same "⚠️ going live is hard to undo" caution section 2 already uses for YouTube; (d) an explicit statement that pre-audit posts will be SELF_ONLY (private) and the D-05 notification line format to expect, with a pointer to file the separate Content Posting API audit (not just base app approval - Pitfall 2) once ready to go fully public - no estimated timeline promised, since 06-RESEARCH.md's Assumption A1 has no official published SLA, only unverified secondary-source estimates.

"## 7. Instagram setup and going live (PUB-07, D-02/D-04, user's build-both-scenarios decision)" covering: (a) Meta App (Business type) creation, adding the operator's own Instagram Business account as tester/admin, the instagram_business_basic+instagram_business_content_publish scopes, redirect URI http://127.0.0.1:8766/callback, saving app id/secret into instagram_client_secret.json; (b) the one-time interactive consent equivalent; (c) the config.yaml publish.instagram_enabled: true opt-in with the same going-live caution; (d) a clearly-labeled subsection stating in plain language: "Unlike TikTok, this module does NOT pre-check whether your account needs Meta App Review before every publish attempt. Per the user's explicit decision (06-CONTEXT.md, resolving 06-RESEARCH.md Open Question 1), it attempts a real publish directly. If it succeeds, Standard Access was sufficient - no review needed for this self-owned-account use case. If it raises InstagramAccessError, the message will say Advanced Access/App Review is likely needed - only then should you file App Review in the Meta App Dashboard. Do NOT file App Review preemptively." - this must read as an instruction, matching the same imperative tone as the TikTok section's audit-filing guidance, not a footnote.

Then add a final subsection titled "## Instagram permission-error heuristic (unverified assumption)" mirroring "Kill-path verification"'s exact tone: state that `_check_meta_permission_error`'s detection of a permission/access-tier rejection is a best-effort heuristic built without a captured real Meta error response (06-RESEARCH.md never made a live API call this session), and that it should be empirically confirmed once real Instagram credentials exist and a real publish attempt either succeeds or produces a real 403/permission error to compare against - update this section with the actual observed error shape once that happens, the same way "Kill-path verification" was updated from "Status: not yet performed" to a dated PASS/FAIL outcome after Phase 3's live test.

Finally, extend section "1. Setting up the periodic check" (do not renumber it) with two new sibling Task Scheduler entries (same 3-hour cadence, same rationale) for the new modules: `shorts-maker-publish-tiktok` running `python D:\shorts-maker\scripts\tiktok_publish.py --check`, and `shorts-maker-publish-instagram` running `python D:\shorts-maker\scripts\instagram_publish.py --check --ig-user-id &lt;your-ig-user-id&gt;` - as two additional sibling tasks, not folded into the existing `shorts-maker-publish` task (schtasks' `/tr` only accepts one command per task, and keeping them separate preserves this phase's whole isolation design at the OS scheduling layer too, not just the file layer). Update the closing `*Phase: ...*` / `*Plans: ...*` footer to add a line for Phase 6's plans, without removing Phase 3's existing footer line.</action>
  <verify>
    <automated>cd D:/shorts-maker && grep -c "InstagramAccessError\|Advanced Access" docs/publish-queue.md</automated>
  </verify>
  <done>docs/publish-queue.md has complete TikTok and Instagram operator sections (app registration, one-time consent, opt-in, going-live caution) plus the Instagram Standard-vs-Advanced-Access attempt-first instruction stated imperatively, the unverified-permission-heuristic flag section, and both new Task Scheduler entries documented alongside the existing one - a human reading only this doc could complete D-02's runbook end to end for both platforms without needing to read 06-RESEARCH.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator documentation → future execution decisions | A future session (human or Claude) reads docs/publish-queue.md to decide how to operate/modify these modules - inaccurate docs here could reintroduce a design the user explicitly rejected (a static TikTok-style gate on Instagram) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Tampering (of design intent, not code) | docs/publish-queue.md Instagram section | medium | mitigate | The attempt-first instruction is written imperatively and explicitly attributed to the user's decision (not left implicit), reducing the chance a future edit "fixes" instagram_publish.py by adding an unwanted TikTok-style pre-publish gate |
</threat_model>

<verification>
- Both isolation tests and both scope-minimization tests pass.
- `docs/publish-queue.md` contains both new sections with the exact imperative Instagram instruction and the unverified-heuristic flag.
- Full combined test suite (`pytest --basetemp=D:/shorts-maker/.pytest-tmp`) green - the whole phase's tests, not just this plan's.
</verification>

<success_criteria>
Success Criterion 3 (platform isolation) has an automated test proving it, not just a file-naming convention; both platforms have a complete, accurate operator runbook; the Instagram design decision that diverges from TikTok's is documented clearly enough that it survives beyond this session's context.
</success_criteria>

<output>
Create `.planning/phases/06-tiktok-instagram-auto-publish/06-07-SUMMARY.md` when done.
</output>
