---
phase: 06-tiktok-instagram-auto-publish
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - scripts/tiktok_publish.py
  - tests/test_tiktok_publish.py
autonomous: true
requirements: [PUB-06]
user_setup:
  - service: tiktok
    why: "Content Posting API access for scheduled auto-publish (PUB-06)"
    env_vars: []
    dashboard_config:
      - task: "Register an app in the TikTok Developer Portal (developers.tiktok.com -> Manage apps -> Create app), request the video.publish + video.upload scopes, and save the client_key/client_secret into a local tiktok_client_key.json ({\"client_key\": ..., \"client_secret\": ...}) - gitignored, never committed."
        location: "https://developers.tiktok.com/"
      - task: "Register the redirect URI http://127.0.0.1:8765/callback for the app (used by this plan's one-time interactive consent helper) - sandbox/dev credentials are usable pre-audit for SELF_ONLY (private) testing per D-02."
        location: "TikTok Developer Portal -> your app -> Login Kit / Redirect URIs"
      - task: "Once ready to go public, file the Content Posting API audit request (separate from base app approval - see 06-RESEARCH.md Pitfall 2; no published SLA, expect weeks) - flip publish.tiktok_enabled to true in config.yaml only after this clears."
        location: "TikTok Developer Portal -> your app -> Content Posting API"

must_haves:
  truths:
    - "With publish.tiktok_enabled=False (default), running upload_and_publish makes zero HTTP calls and zero credential loads (PUB-03 parity)"
    - "A queued clip's title (rendered metadata caption) is sent as TikTok's post_info.title via the FILE_UPLOAD source type, chunked via Content-Range PUT calls covering the whole file"
    - "creator_info/query is checked BEFORE every video/init call to pick a valid privacy_level and detect the SELF_ONLY trap (D-05) - never hardcoded PUBLIC_TO_EVERYONE"
    - "A crash between write-ahead persist and status/fetch reaching a terminal state is reconcilable on the next check without a duplicate video/init call"
  artifacts:
    - "scripts/tiktok_publish.py: load_queue/save_queue/enqueue/select_next_due/pause_item/resume_item, load_credentials + one-time interactive consent helper, build_direct_post_body/init_direct_post/upload_video_chunks/fetch_post_status/check_tiktok_publish_gate, upload_and_publish, reconcile_uploading/reconcile_all_uploading"
    - "tests/test_tiktok_publish.py covering all of the above via a hand-written FakeSession/FakeResponse HTTP double (no unittest.mock)"
  key_links:
    - "upload_and_publish checks config.tiktok_enabled as the literal first statement, before load_credentials or any requests call (mirrors publish_queue.py::upload_and_schedule's dry-run-gate-first ordering)"
    - "check_tiktok_publish_gate's result feeds both init_direct_post's privacy_level argument AND the D-05 notification branch in upload_and_publish"
    - "entry['publish_id'] is persisted via save_queue immediately after init_direct_post returns, before upload_video_chunks starts (Pitfall 5 write-ahead point)"
---

<objective>
Build the core of `scripts/tiktok_publish.py`: its own queue manifest lifecycle, TikTok OAuth credential handling (cache/refresh + one-time interactive consent), the Content Posting API HTTP layer (chunked `FILE_UPLOAD`, status polling, `creator_info/query` gating), and the orchestration function that ties them together with PUB-03's dry-run-first discipline and D-05's SELF_ONLY detection. This plan does not build pause/kill or the CLI wrapper (Plan 06-05) - it produces a fully-tested, importable module with everything needed to actually publish one queued clip to TikTok.

Purpose: PUB-06 requires the same dry-run-default/idempotency safety mechanism Phase 3 built for YouTube, extended to TikTok's Content Posting API, with its own manifest file (`work/_publish/tiktok_queue.json`) so a TikTok audit delay or bug can never touch the already-live YouTube queue (Success Criterion 3). D-05 requires detecting the SELF_ONLY pre-audit trap via `creator_info/query`, not a post-hoc status check (TikTok's `status/fetch` has no achieved-privacy-level field - 06-RESEARCH.md Pitfall 1).

Output: `scripts/tiktok_publish.py` (importable, queue+OAuth+HTTP+orchestration complete) and `tests/test_tiktok_publish.py` covering every behavior in 06-VALIDATION.md's PUB-06 rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-CONTEXT.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-RESEARCH.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-PATTERNS.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-VALIDATION.md

# The whole-file analog this module mirrors (queue lifecycle shape, dry-run-gate-first
# ordering, write-ahead persistence, notification reuse):
@scripts/publish_queue.py
@tests/test_publish_queue.py

# load_credentials contract this plan's OAuth loader mirrors the shape of:
@scripts/youtube_analytics.py

# metadata.py's per-platform schema - tiktok entries use the already-rendered "caption" field:
@scripts/metadata.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Queue lifecycle + OAuth credential handling</name>
  <files>scripts/tiktok_publish.py, tests/test_tiktok_publish.py</files>
  <read_first>scripts/publish_queue.py lines 1-73 (module docstring/imports/status enum/load_queue/save_queue), lines 305-332 (pause_item/resume_item/select_next_due), lines 76-117 (enqueue); scripts/youtube_analytics.py lines 30-55 (load_credentials contract)</read_first>
  <behavior>
    - load_queue/save_queue: missing file yields {"entries": []}; save writes UTF-8 JSON with ensure_ascii=False, indent=2, creating parent dirs.
    - enqueue(queue, clip_id, video_path, metadata_path, caption): idempotent on clip_id (re-enqueuing returns the existing entry unchanged), sequential seq starting at 1, status=QUEUED, publish_id=None, video_share_url=None. No description/tags/publish_at fields (TikTok has no analog - Pattern shows post_info only takes title+privacy_level, and there is no native scheduling per Pitfall 4).
    - select_next_due: returns lowest-seq QUEUED entry or None; never returns PAUSED/KILLED/UPLOADING/PUBLISHED entries.
    - pause_item/resume_item: flip status QUEUED<->PAUSED, touch updated_at only.
    - load_credentials(client_key_path, token_path, session=requests): returns a cached access_token if unexpired; if expired, POSTs grant_type=refresh_token to TIKTOK_TOKEN_URL, persists the refreshed token+expires_at, returns the new access_token; raises FileNotFoundError with an actionable message if token_path does not exist yet (first-time consent is manual, see Task 1's second half).
    - run_tiktok_oauth_consent(client_key_path, token_path, port=8765, session=requests): builds the authorize URL (client_key, scope=video.publish,video.upload, response_type=code, redirect_uri=http://127.0.0.1:{port}/callback, a random state), opens it via webbrowser.open, blocks on a local HTTP listener bound to 127.0.0.1 (never 0.0.0.0) for exactly one redirect request, extracts the code query param, exchanges it for tokens via TIKTOK_TOKEN_URL (grant_type=authorization_code), writes the token file in the same shape load_credentials expects (access_token, refresh_token, expires_at). This is genuinely new code (06-RESEARCH.md Security Domain: "this is a new construction point... the planner must build it deliberately"), not something google-auth-oauthlib provides for TikTok - use a fixed port (not port=0 like Google's flow) since TikTok apps require an exact pre-registered redirect_uri, unlike Google's loopback-exception handling.
  </behavior>
  <action>Create scripts/tiktok_publish.py with the module docstring/import-safety shape from publish_queue.py's own header, adapted per 06-PATTERNS.md's tiktok_publish.py section (import requests at top level - it is a lightweight always-available dependency, not deferred like google-api-python-client). Define QUEUED/UPLOADING/PUBLISHED/KILLED/PAUSED (no SCHEDULED - TikTok Direct Post has no publishAt-style native schedule per Pitfall 4) and VALID_STATUSES. DEFAULT_QUEUE_PATH = "work/_publish/tiktok_queue.json". DEFAULT_NOTIFICATIONS_PATH = "work/_publish/notifications.log" (SHARED, D-06 - same value as publish_queue.py's constant, not imported, matching 06-PATTERNS.md's redeclaration convention). TIKTOK_SCOPES = ["video.publish", "video.upload"] (V4 scope minimization - never request a broader scope). TIKTOK_TOKEN_URL = "https://open.tiktokapis.com/v2/oauth/token/". Implement load_queue/save_queue/enqueue/select_next_due/pause_item/resume_item/_find_entry copying publish_queue.py's exact shape, adapted per the &lt;behavior&gt; block above. Implement load_credentials and run_tiktok_oauth_consent per 06-RESEARCH.md Pattern 3 (refresh path is given verbatim there) and the &lt;behavior&gt; block above for the new consent-flow half - build a small internal helper (e.g. _capture_oauth_redirect_code(host, port, timeout_seconds)) using stdlib http.server.HTTPServer bound explicitly to ("127.0.0.1", port), calling handle_request() exactly once (never serve_forever - bounded, no lingering listener) to receive the single redirect.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_tiktok_publish.py -k "queue or credential or oauth or consent" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>load_queue/save_queue/enqueue/select_next_due/pause_item/resume_item behave exactly per the &lt;behavior&gt; block, tested; load_credentials returns a cached token, refreshes an expired one via a FakeSession, and raises FileNotFoundError with no cached token; a test asserts the local redirect-capture server binds to ("127.0.0.1", &lt;port&gt;), never "0.0.0.0" (V4/Spoofing mitigation, threat T-06-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TikTok HTTP upload layer</name>
  <files>scripts/tiktok_publish.py, tests/test_tiktok_publish.py</files>
  <read_first>06-RESEARCH.md Pattern 1 (init_direct_post/upload_video_chunks/fetch_post_status) and Pattern 4 (check_tiktok_publish_gate) - copy the request/response shapes verbatim; scripts/publish_queue.py lines 162-208 (build_insert_body) for the pure-body-builder-separate-from-network-call convention this task mirrors</read_first>
  <behavior>
    - validate_title_length(title): raises ValueError if len(title) exceeds MAX_TITLE_LENGTH (2200, TikTok's UTF-16-rune title limit approximated via Python str length per this project's existing len()-based limit-check convention in build_insert_body).
    - build_direct_post_body(title, privacy_level, video_size, chunk_size, total_chunk_count): pure function, calls validate_title_length first, returns the exact {"post_info": {...}, "source_info": {"source": "FILE_UPLOAD", ...}} shape from Pattern 1. No network call.
    - init_direct_post(access_token, body, session=requests): POSTs body to https://open.tiktokapis.com/v2/post/publish/video/init/ with the Authorization header, raise_for_status(), returns response.json()["data"] ({"publish_id", "upload_url"}).
    - upload_video_chunks(upload_url, video_path, chunk_size, session=requests): PUTs the local file in chunk_size pieces with correct Content-Range headers per Pattern 1 (works correctly for both a single-chunk file under chunk_size and a genuinely multi-chunk file).
    - fetch_post_status(access_token, publish_id, session=requests): POSTs {"publish_id": publish_id} to the status/fetch endpoint, returns response.json()["data"].
    - check_tiktok_publish_gate(access_token, session=requests): POSTs to creator_info/query, returns ("PUBLIC_TO_EVERYONE", False) if that option is present in privacy_level_options, else ("SELF_ONLY", True) - exact shape from Pattern 4.
  </behavior>
  <action>Implement the six functions in the &lt;behavior&gt; block, each accepting an injectable session=requests parameter (mirrors this project's runner=subprocess.run injectable pattern - tests pass a FakeSession, never monkeypatch the requests module globally). Copy the request/response field shapes from 06-RESEARCH.md Pattern 1/Pattern 4 verbatim - do not invent different field names or endpoints. For a class TikTokPublishError(ValueError): pass, following the project's `class ConfigError(ValueError)` convention - use it if fetch_post_status ever needs to surface a FAILED status distinctly (decide during implementation whether upload_and_publish in Task 3 raises this directly or a plain RuntimeError; either is fine as long as it's a named, not-bare Exception type).</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_tiktok_publish.py -k "init_direct_post or upload_video_chunks or publish_gate or title_length" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>test_init_direct_post_body asserts the exact FILE_UPLOAD post_info/source_info shape via FakeSession.calls; test_upload_video_chunks asserts correct Content-Range headers covering the whole file (including a multi-chunk case); test_check_tiktok_publish_gate_self_only asserts is_still_gated=True when PUBLIC_TO_EVERYONE is absent from privacy_level_options; an oversized-title test asserts build_direct_post_body raises ValueError before any session call is made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orchestration (dry-run gate, D-05 detection, write-ahead) and reconciliation</name>
  <files>scripts/tiktok_publish.py, tests/test_tiktok_publish.py</files>
  <read_first>scripts/publish_queue.py lines 211-287 (upload_and_schedule's dry-run-gate-first ordering and write-ahead persistence) and lines 559-592 (_success_notification_text/_error_notification_text/_upload_one's error-vs-success branching)</read_first>
  <behavior>
    - upload_and_publish(queue, entry, credentials_factory, config, session=requests, queue_path=DEFAULT_QUEUE_PATH): if not config.tiktok_enabled, returns {"dry_run": True} as the LITERAL FIRST statement - credentials_factory is never called, no session call happens. Otherwise: get access_token = credentials_factory(); set entry["status"]=UPLOADING, save_queue (write-ahead #1, before creator_info/query or init_direct_post); privacy_level, is_still_gated = check_tiktok_publish_gate(access_token, session); build body via build_direct_post_body(entry["caption"], privacy_level, video_size=os.path.getsize(entry["video_path"]), chunk_size=<a constant within TikTok's 5-64MB range>, total_chunk_count=<computed from file size and chunk_size>); data = init_direct_post(access_token, body, session); entry["publish_id"] = data["publish_id"]; save_queue (write-ahead #2, Pitfall 5's specific point - persisted before the chunk PUT loop starts, so a crash mid-upload leaves a durable publish_id trace); upload_video_chunks(data["upload_url"], entry["video_path"], chunk_size, session); poll fetch_post_status(access_token, entry["publish_id"], session) until status is a terminal value (PUBLISH_COMPLETE or FAILED) - simple loop, this project's clips are short so a handful of polls suffices, no need for exponential backoff machinery; on PUBLISH_COMPLETE: entry["status"]=PUBLISHED, save_queue, return {"publish_id": ..., "is_still_gated": is_still_gated}; on FAILED: raise RuntimeError with the fail_reason field, entry status stays UPLOADING for the next reconcile pass to resolve.
    - _upload_one(entry, queue, credentials_factory, config, session): mirrors publish_queue.py's exact error-vs-success branching - wraps upload_and_publish in try/except, appends an error notification and re-raises on any Exception; on a dry-run result, prints/returns without notifying; on success with is_still_gated=False, appends a "залил {seq} в TikTok" style success notification; on success with is_still_gated=True (D-05), appends a DISTINCT notification line making clear the account is still pre-audit/SELF_ONLY (e.g. "TikTok {seq}: залито, но аккаунт всё ещё SELF_ONLY (аудит Content Posting API не пройден) - видео приватное") rather than the normal success line - this is D-05's core requirement, do not silently report success as if it were public.
    - reconcile_uploading(queue, entry, credentials_factory, session): if entry has a publish_id recorded, call fetch_post_status; if PUBLISH_COMPLETE, adopt: status=PUBLISHED, save; if FAILED or the fetch itself 404s/errors (publish_id expired/invalid), reset: status=QUEUED, publish_id=None, save, so a clean retry can happen; if still in-progress (PROCESSING_UPLOAD/PROCESSING_DOWNLOAD), leave untouched - it is legitimately still in flight. If entry has NO publish_id recorded (crash occurred before init_direct_post ever returned), reset to QUEUED directly with no API call - nothing was created on TikTok's side.
    - reconcile_all_uploading(queue, credentials_factory, session): iterates every UPLOADING entry, calling reconcile_uploading on each; wired as the mandatory first step before select_next_due is ever called (mirrors publish_queue.py's reconcile_all_uploading precedent, adapted since TikTok's reconciliation checks status/fetch by publish_id rather than searching the creator's public uploads for a marker - TikTok has no such "list my public videos" endpoint documented, and unlike YouTube's insert() call, video/init returns publish_id synchronously, making a marker-search unnecessary here).
  </behavior>
  <action>Implement upload_and_publish, _upload_one, reconcile_uploading, and reconcile_all_uploading per the &lt;behavior&gt; block, importing append_notification from scripts.publish_queue (the one sanctioned cross-script import, reused verbatim per 06-PATTERNS.md's Shared Patterns section - do not reimplement the notification log). Choose a fixed CHUNK_SIZE_DEFAULT module constant within TikTok's documented 5MB-64MB per-chunk range (e.g. 10 * 1024 * 1024) - this project's clips are short (30-150s vertical shorts), so most uploads will be a single chunk in practice, but the loop in Task 2's upload_video_chunks must still handle a genuinely multi-chunk file correctly (already covered by Task 2's test). Deliberately do NOT read or reference config.daily_slots_utc anywhere in this module (06-RESEARCH.md Open Question 3): TikTok Direct Post has no publishAt-equivalent to schedule against, so the grid has no role to play here - the existing "select_next_due returns at most one entry, --check uploads at most one item per invocation" behavior already provides the only pacing this platform needs or can use, matching Open Question 3's own recommendation not to misapply a scheduling concept that doesn't exist for this platform.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_tiktok_publish.py -k "dry_run or idempotent or reconcile or self_only" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>test_dry_run_default_no_upload asserts zero session calls and zero credentials_factory calls when tiktok_enabled=False; test_idempotent_retry_no_duplicate asserts that an entry with a publish_id already recorded never triggers a second init_direct_post call on reconciliation; a D-05 test asserts the distinct SELF_ONLY notification line is appended (and the normal success line is NOT) when check_tiktok_publish_gate reports is_still_gated=True; full test_tiktok_publish.py suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local queue manifest → TikTok API | Locally-authored caption/video_path data crosses into an external platform's public-facing API |
| Browser redirect → local HTTP listener (one-time consent) | The one-time OAuth consent flow opens a local port to receive an authorization code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | build_direct_post_body (title/caption from queue manifest) | medium | mitigate | validate_title_length raises ValueError before any network call on an oversized/malformed title, mirroring build_insert_body's exact per-field-limit pattern (V5) |
| T-06-03 | Spoofing / Elevation of Privilege | Local OAuth redirect-capture HTTP listener (one-time consent flow) | high | mitigate | Bound explicitly to 127.0.0.1 only (never 0.0.0.0), single handle_request() call (no lingering listener), tested directly (Task 1) |
| T-06-04 | Repudiation / reliability (not classic STRIDE) | Crash between write-ahead persist and a terminal status/fetch result | medium | mitigate | Two-stage write-ahead (UPLOADING before creator_info/query, publish_id persisted immediately after init_direct_post returns) + reconcile_uploading resolves via status/fetch using the recorded publish_id before any retry is considered - no blind re-init |
</threat_model>

<verification>
- `pytest tests/test_tiktok_publish.py --basetemp=D:/shorts-maker/.pytest-tmp` - full file green.
- Dry-run makes zero HTTP calls (asserted via FakeSession.calls == []).
- D-05 SELF_ONLY detection happens via creator_info/query before init_direct_post, never inferred from status/fetch after the fact.
- Write-ahead persistence is visible in the saved queue file at the moment each network call fires (mirrors publish_queue.py's spying-insert test technique).
</verification>

<success_criteria>
`scripts/tiktok_publish.py` is a fully importable, fully-tested module capable of publishing one queued clip to TikTok end-to-end (given real credentials), safely dry-run by default, with D-05's SELF_ONLY detection and crash-safe reconciliation - pause/kill and the CLI wrapper are Plan 06-05's job.
</success_criteria>

<output>
Create `.planning/phases/06-tiktok-instagram-auto-publish/06-03-SUMMARY.md` when done.
</output>
