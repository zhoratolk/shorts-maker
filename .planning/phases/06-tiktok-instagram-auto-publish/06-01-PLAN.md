---
phase: 06-tiktok-instagram-auto-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [PUB-06, PUB-07]
user_setup: []
tags: [publish, tiktok, instagram, dependencies]

must_haves:
  truths:
    - "A human confirmed requests' legitimacy on pypi.org (exact spelling, github.com/psf/requests canonical source) before it is declared as a direct dependency"
    - "requests continues to import successfully from the project's .venv — no new pip install runs in this plan, it is already installed transitively"
  artifacts:
    - "requirements.txt gains a requests>=2.32 line under a new comment explaining direct use by scripts/tiktok_publish.py and scripts/instagram_publish.py"
  key_links:
    - "requirements.txt declares requests as a direct dependency before Plan 06-03/06-04 add 'import requests' at module top level in the two new platform modules"
---

<objective>
Get a human legitimacy sign-off on `requests` (flagged `SUS` in 06-RESEARCH.md's Package Legitimacy Audit, tool-signal-only, not a real risk) before it becomes a directly-declared dependency, then register it in `requirements.txt`. `requests` is already installed transitively in this environment — this plan does not run `pip install`, it only gates and documents the upgrade from "transitive" to "directly declared and imported."

Purpose: `scripts/tiktok_publish.py` and `scripts/instagram_publish.py` (Plans 06-03/06-04) both `import requests` at module top level for every TikTok/Instagram HTTP call — the package legitimacy gate (per this project's planning protocol) requires a blocking-human checkpoint before any package crosses from transitive to directly-declared/imported, mirroring the exact protocol already used for opencv-python-headless/librosa in Phase 4 Plan 01. This dependency is provisioned now, not deferred until either platform's audit clears (D-01/D-03: building/wiring the integration is not gated on audit approval — only *going public* is).

Output: A human-approved, documented `requests>=2.32` line in `requirements.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-CONTEXT.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-RESEARCH.md

# Existing optional/direct-dependency comment convention this plan mirrors 1:1:
@requirements.txt

# Precedent for exactly this checkpoint-then-requirements.txt shape:
@.planning/phases/04-context-driven-transitions/04-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy checkpoint for requests</name>
  <read_first>.planning/phases/06-tiktok-instagram-auto-publish/06-RESEARCH.md (Package Legitimacy Audit section)</read_first>
  <action>Pause for a blocking-human legitimacy review of `requests` BEFORE it is declared as a direct dependency (it is currently only a transitive one). 06-RESEARCH.md's Package Legitimacy Audit flagged it `SUS`, but explicitly attributes that verdict to a missing-download-count tool signal only — repo, age, and postinstall signals were all clean, and the package is already installed and in active transitive use in this exact environment. Per the package-legitimacy protocol, a human must still confirm before the direct-dependency line is added. Threat T-06-SC (supply-chain tampering via a newly-declared direct dependency) is mitigated by this gate. This checkpoint is never auto-approvable regardless of workflow.auto_advance.</action>
  <what-built>Nothing changed yet. requests is already installed (transitive dependency, pip show requests confirms 2.34.2). This checkpoint only gates the requirements.txt edit in Task 2.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/requests/ — confirm the source repo is github.com/psf/requests (the canonical, official PSF-maintained repository), and that the project is actively maintained with a current release.
    2. Confirm it is not a typosquat: exact spelling is `requests` (not `request`, `requests2`, `python-requests`, etc.).
    3. Confirm the locally installed version (`pip show requests`, already run during research: 2.34.2) is a legitimate published release, not a yanked/pre-release build.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the requirements.txt edit, or describe concerns. This checkpoint is NOT auto-approvable regardless of workflow.auto_advance.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Register requests as a direct dependency in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt (the existing "# optional, only needed when ..." / "# optional, only needed by ..." comment blocks above pyannote.audio, the google-api clients, and opencv-python-headless/librosa)</read_first>
  <action>Only after Task 1 is approved. Append a new line to requirements.txt: a comment "# used directly by scripts/tiktok_publish.py and scripts/instagram_publish.py (TikTok/Instagram publish API calls) - previously only a transitive dependency" immediately above `requests>=2.32`. Unlike the optional-feature blocks (pyannote, opencv/librosa), do NOT phrase this as "optional, only needed when X is enabled" — requests is imported unconditionally at module top level in both new modules (it is a lightweight, always-available stdlib-adjacent HTTP client, not a heavy optional extra like pyannote/opencv), matching how google-api-python-client's block is worded ("optional, only needed by scripts/youtube_analytics.py") rather than a feature-flag-gated block. No pip install command needs to run — requests is already present in .venv.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -c "import requests; print(requests.__version__)"</automated>
  </verify>
  <done>requirements.txt has a new `requests>=2.32` line under a descriptive comment; the import still succeeds from the project's venv Python (unchanged, since nothing new was installed); the legitimacy checkpoint was approved by a human before this edit was made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local .venv (declared dependency) | requests moves from transitive to directly-declared/imported; its code executes in-process for every TikTok/Instagram API call made by later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering / Elevation of Privilege | requests becoming a directly-declared dependency in requirements.txt | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies the package on pypi.org against its known GitHub repo (psf/requests) with an exact-spelling typosquat check, before the requirements.txt line is added; never auto-approvable |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -c "import requests"` succeeds with no ImportError (unchanged behavior, confirms nothing was broken).
- `requirements.txt` diff shows a new `requests>=2.32` line under a descriptive comment.
- The legitimacy checkpoint was approved by a human before the requirements.txt edit was made.
</verification>

<success_criteria>
`requests` is documented as a direct dependency in requirements.txt, after human legitimacy sign-off, ready for Plans 06-03/06-04 to import it unconditionally.
</success_criteria>

<output>
Create `.planning/phases/06-tiktok-instagram-auto-publish/06-01-SUMMARY.md` when done.
</output>
