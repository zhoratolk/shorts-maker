---
phase: 06-tiktok-instagram-auto-publish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/config.py
  - .gitignore
  - tests/test_config.py
autonomous: true
requirements: [PUB-06, PUB-07]
user_setup: []
tags: [publish, tiktok, instagram, config]

must_haves:
  truths:
    - "config.publish.tiktok_enabled and config.publish.instagram_enabled both default to False when the publish: section (or the whole config.yaml) is absent (PUB-03 dry-run-default discipline, per-platform)"
    - "Custom tiktok_enabled/instagram_enabled/path values in config.yaml round-trip correctly through load_config"
    - "The four new credential filenames (tiktok_client_key.json, tiktok_token.json, instagram_client_secret.json, instagram_token.json) are gitignored before any code that could write them exists"
  artifacts:
    - "scripts/config.py PublishConfig gains 8 new fields: tiktok_enabled, tiktok_queue_path, tiktok_client_key_path, tiktok_token_path, instagram_enabled, instagram_queue_path, instagram_client_secret_path, instagram_token_path"
    - ".gitignore lists the four new credential filenames alongside the existing client_secret.json/token.json/upload_token.json entries"
    - "tests/test_config.py covers defaults and a custom-values round-trip for both new field groups"
  key_links:
    - "Plans 06-03/06-04's CLI main() functions reference these exact attribute names on config.publish - a mismatch would silently no-op every TikTok/Instagram --check run"
---

<objective>
Extend the existing `scripts/config.py::PublishConfig` dataclass in place with the TikTok (PUB-06) and Instagram (PUB-07) per-platform fields 06-RESEARCH.md's "PublishConfig Extension Proposal" already specified concretely, and gitignore the four new credential filenames before any later plan's code could write them. This is the shared interface both platform modules (Plans 06-03/06-04) read from — get the field names locked and tested first.

Purpose: PUB-03's dry-run-default discipline (`enabled: bool = False`) must apply per-platform, independently, per D-01 — a shared `enabled` flag repurposed to cover all three platforms would violate D-01's literal "per-platform enable flag" wording and could accidentally flip TikTok/Instagram live at the same moment YouTube's `enabled` flag is toggled. Do NOT create sibling dataclasses (`TikTokPublishConfig`/`InstagramPublishConfig`) — extend the existing flat `PublishConfig` in place, matching the `MonetizationConfig`/`DiarizationConfig` opt-in-bool-per-feature convention this file already uses everywhere else.

Output: `PublishConfig` with 8 new fields (all safe defaults), `.gitignore` updated, `tests/test_config.py` covering both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-CONTEXT.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-RESEARCH.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-PATTERNS.md

# Exact dataclass this plan extends, and its existing tests/gitignore precedent:
@scripts/config.py
@.gitignore
@tests/test_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PublishConfig and gitignore the new credential files</name>
  <files>scripts/config.py, .gitignore</files>
  <read_first>scripts/config.py (PublishConfig dataclass, lines ~189-200, and the `_validate` function's existing `publish.daily_slots_utc` checks) — 06-RESEARCH.md's "PublishConfig Extension Proposal" section has the exact field block to add</read_first>
  <action>In scripts/config.py, add 8 new fields to the existing PublishConfig dataclass (do not create new dataclasses, do not touch the existing enabled/daily_slots_utc/queue_path/notifications_path/client_secret_path/upload_token_path fields): tiktok_enabled: bool = False, tiktok_queue_path: str = "work/_publish/tiktok_queue.json", tiktok_client_key_path: str = "tiktok_client_key.json", tiktok_token_path: str = "tiktok_token.json", instagram_enabled: bool = False, instagram_queue_path: str = "work/_publish/instagram_queue.json", instagram_client_secret_path: str = "instagram_client_secret.json", instagram_token_path: str = "instagram_token.json". No new `_validate()` rule is needed for these fields (bool/path fields need no extra validation, same precedent as MonetizationConfig) - do not add one. Do not add a `publish:` section to config.example.yaml either: this repo's existing convention (confirmed by grepping config.example.yaml) never documented a `publish:` section for the original YouTube fields either, since every field is a safe opt-in-False default requiring no user action to be safe - stay consistent with that precedent rather than introducing a new documentation surface Phase 3 didn't use.

Then in .gitignore, add four new lines for the new credential files, placed near the existing `client_secret.json` / `token.json` / `upload_token.json` block: `tiktok_client_key.json`, `tiktok_token.json`, `instagram_client_secret.json`, `instagram_token.json`. This must land before Plans 06-03/06-04 write any code that could create these files (Privacy constraint, this project's prior real leaked-credential incident, 06-RESEARCH.md Security Domain's "Leaked credential" threat entry).</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -c "from scripts.config import PublishConfig; c = PublishConfig(); assert c.tiktok_enabled is False and c.instagram_enabled is False and c.tiktok_queue_path == 'work/_publish/tiktok_queue.json' and c.instagram_queue_path == 'work/_publish/instagram_queue.json'; print('ok')"</automated>
  </verify>
  <done>PublishConfig has all 8 new fields with the exact defaults listed above; .gitignore has all 4 new credential filenames; `git check-ignore tiktok_token.json instagram_token.json` (run from repo root) confirms both are ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Test coverage for the new PublishConfig fields</name>
  <files>tests/test_config.py</files>
  <read_first>tests/test_config.py (test_default_config_publish_enabled_is_false, test_load_config_publish_defaults_when_section_missing, test_load_config_publish_custom_values_round_trip — the exact shape to mirror for the 2 new field groups)</read_first>
  <action>Add tests mirroring the existing publish-field test shapes exactly, for both new field groups: (1) a defaults test asserting Config(input_dir=..., output_dir=...).publish.tiktok_enabled is False and .instagram_enabled is False; (2) a test that load_config on a config.yaml with no `publish:` section at all resolves tiktok_enabled/instagram_enabled/tiktok_queue_path/instagram_queue_path/tiktok_client_key_path/tiktok_token_path/instagram_client_secret_path/instagram_token_path to their documented defaults; (3) a custom-values round-trip test that writes a config.yaml with `publish: {tiktok_enabled: true, instagram_enabled: true, tiktok_queue_path: "work/_publish/custom_tiktok.json"}` and asserts load_config reflects those exact overridden values while unspecified fields (e.g. instagram_queue_path) keep their defaults. Use the existing `write_config` test helper already defined in this file - do not redefine it.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_config.py -k "tiktok or instagram" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>All new tests pass; existing test_config.py tests (publish and otherwise) still pass unmodified (`pytest tests/test_config.py --basetemp=D:/shorts-maker/.pytest-tmp`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml (local, user-edited) → PublishConfig (in-process) | User-controlled local file feeds directly into which platforms are live and where credentials are read from |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | tiktok_client_key.json / tiktok_token.json / instagram_client_secret.json / instagram_token.json (new credential files, default paths introduced here) | high | mitigate | .gitignore entries added in this same plan, before any later plan's code could create these files - matches the existing client_secret.json/token.json/upload_token.json discipline and this project's prior real leaked-credential incident |
</threat_model>

<verification>
- `PublishConfig()` exposes all 8 new fields with documented defaults.
- `load_config` round-trips custom `publish.tiktok_enabled`/`publish.instagram_enabled`/path overrides correctly, leaving unspecified fields at their defaults.
- `git check-ignore tiktok_client_key.json tiktok_token.json instagram_client_secret.json instagram_token.json` (run from repo root) confirms all four are ignored.
- Full `tests/test_config.py` suite still green.
</verification>

<success_criteria>
`scripts/config.py::PublishConfig` exposes the exact 8 field names Plans 06-03 through 06-06 will read (`config.publish.tiktok_enabled`, etc.), all defaulting to safe/off values, tested, and their credential file paths are gitignored before any code that could write them exists.
</success_criteria>

<output>
Create `.planning/phases/06-tiktok-instagram-auto-publish/06-02-SUMMARY.md` when done.
</output>
