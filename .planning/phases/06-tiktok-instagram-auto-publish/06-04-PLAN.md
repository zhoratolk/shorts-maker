---
phase: 06-tiktok-instagram-auto-publish
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - scripts/instagram_publish.py
  - tests/test_instagram_publish.py
autonomous: true
requirements: [PUB-07]
user_setup:
  - service: instagram
    why: "Graph API Reels publishing for scheduled auto-publish (PUB-07)"
    env_vars: []
    dashboard_config:
      - task: "Create a Meta App (Business type) and add your own Instagram Business account as a tester/admin - save the app id/secret into a local instagram_client_secret.json ({\"client_id\": ..., \"client_secret\": ...}) - gitignored, never committed."
        location: "https://developers.facebook.com/apps"
      - task: "Register the redirect URI http://127.0.0.1:8766/callback for Instagram Login Business (used by this plan's one-time interactive consent helper)."
        location: "Meta App Dashboard -> Instagram -> API setup with Instagram login"
      - task: "Per the user's explicit build-both-scenarios decision (see 06-CONTEXT.md Open Question 1 / RESEARCH.md Assumption A2): do NOT preemptively file Meta App Review. Attempt one real (non-test) publish first with publish.instagram_enabled=true. If Meta accepts it, Standard Access is sufficient for this self-owned-account use case and no review is needed. Only if the app raises an actionable 'InstagramAccessError' (Meta reporting a permission/access-tier rejection) should you then file App Review (Advanced Access)."
        location: "Meta App Dashboard -> App Review (only if the app's error message says so)"

must_haves:
  truths:
    - "With publish.instagram_enabled=False (default), running upload_and_publish makes zero HTTP calls and zero credential loads (PUB-03 parity)"
    - "A queued clip is uploaded via the resumable-upload flow (local file POSTed to rupload.facebook.com, never a public video_url) and published as a REELS media item"
    - "The module does NOT implement a TikTok-style pre-publish gating check - it attempts the real publish directly (Standard Access), and only on a Meta-reported permission/access-tier error does it raise a distinct, actionable InstagramAccessError explaining Advanced Access/App Review may be needed - never a silent skip, never an opaque crash (per user's explicit build-both-scenarios decision)"
    - "A crash between write-ahead persist and a terminal container status is reconcilable on the next check without a duplicate container/publish call"
  artifacts:
    - "scripts/instagram_publish.py: load_queue/save_queue/enqueue/select_next_due/pause_item/resume_item, load_credentials + one-time interactive consent helper, build_media_container_params/create_resumable_container/upload_local_video/poll_container_status/publish_container, InstagramAccessError, upload_and_publish, reconcile_uploading/reconcile_all_uploading"
    - "tests/test_instagram_publish.py covering all of the above via a hand-written FakeSession/FakeResponse HTTP double (no unittest.mock)"
  key_links:
    - "upload_and_publish checks config.instagram_enabled as the literal first statement, before load_credentials or any requests call"
    - "create_resumable_container/upload_local_video/publish_container all route Meta HTTPErrors through a shared _check_meta_permission_error helper that raises InstagramAccessError on a permission/access-tier signal, letting other errors propagate normally"
    - "entry['container_id'] is persisted via save_queue immediately after create_resumable_container returns, before upload_local_video starts"
---

<objective>
Build the core of `scripts/instagram_publish.py`: its own queue manifest lifecycle, Instagram OAuth credential handling (cache/refresh + one-time interactive consent), the Graph API resumable-upload HTTP layer, and the orchestration function that ties them together with PUB-03's dry-run-first discipline. Critically, this module implements the user's explicit decision on 06-RESEARCH.md's Open Question 1 (Standard-vs-Advanced Access): it does NOT bake in a static "always needs App Review" gate the way TikTok's `creator_info/query` is a real pre-post gate — it attempts the real publish via Standard Access first, and only fails closed with a clear, actionable error if Meta's API itself reports a permission/access-tier problem. This plan does not build pause/kill or the CLI wrapper (Plan 06-06).

Purpose: PUB-07 requires the same dry-run-default/idempotency safety mechanism Phase 3 built for YouTube, extended to Instagram's Graph API, with its own manifest file (`work/_publish/instagram_queue.json`) for Success Criterion 3's isolation. Unlike TikTok, 06-RESEARCH.md's Open Question 1 found real uncertainty about whether Instagram's Advanced Access (with its App Review requirement) even applies to an app that only ever publishes to the developer's own linked Business account (Standard Access may suffice, no review needed) — the user resolved this ambiguity by instructing: build code for both scenarios, attempt-then-fail-closed rather than assume-then-block.

Output: `scripts/instagram_publish.py` (importable, queue+OAuth+HTTP+orchestration complete) and `tests/test_instagram_publish.py` covering every behavior in 06-VALIDATION.md's PUB-07 rows plus the permission-error fail-closed design.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-CONTEXT.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-RESEARCH.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-PATTERNS.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-VALIDATION.md

# The whole-file analog this module mirrors (queue lifecycle shape, dry-run-gate-first
# ordering, write-ahead persistence, notification reuse):
@scripts/publish_queue.py
@tests/test_publish_queue.py

# load_credentials contract this plan's OAuth loader mirrors the shape of:
@scripts/youtube_analytics.py

# metadata.py's per-platform schema - instagram entries use the already-rendered "caption" field:
@scripts/metadata.py

# Sibling module built in parallel (same shape, TikTok-specific) - do not import from it,
# but its function names/structure should stay recognizably parallel:
@.planning/phases/06-tiktok-instagram-auto-publish/06-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Queue lifecycle + OAuth credential handling</name>
  <files>scripts/instagram_publish.py, tests/test_instagram_publish.py</files>
  <read_first>scripts/publish_queue.py lines 1-73 (module docstring/imports/status enum/load_queue/save_queue), lines 305-332 (pause_item/resume_item/select_next_due), lines 76-117 (enqueue); scripts/youtube_analytics.py lines 30-55 (load_credentials contract)</read_first>
  <behavior>
    - load_queue/save_queue: missing file yields {"entries": []}; save writes UTF-8 JSON with ensure_ascii=False, indent=2, creating parent dirs.
    - enqueue(queue, clip_id, video_path, metadata_path, caption): idempotent on clip_id (re-enqueuing returns the existing entry unchanged), sequential seq starting at 1, status=QUEUED, container_id=None, media_id=None. No description/tags/publish_at fields (Instagram's media_publish is immediate once called, no native schedule per Pitfall 4).
    - select_next_due: returns lowest-seq QUEUED entry or None; never returns PAUSED/KILLED/UPLOADING/PUBLISHED entries.
    - pause_item/resume_item: flip status QUEUED<->PAUSED, touch updated_at only.
    - load_credentials(client_secret_path, token_path, session=requests): returns a cached access_token if it is not close to expiry; if older than a refresh threshold (e.g. refresh when older than 24h and the 60-day window has more than a few days left), GETs graph.instagram.com/refresh_access_token?grant_type=ig_refresh_token&access_token=..., persists the refreshed token+new expiry, returns the new access_token; raises FileNotFoundError with an actionable message if token_path does not exist yet.
    - run_instagram_oauth_consent(client_secret_path, token_path, port=8766, session=requests): builds the Instagram Login Business authorize URL (client_id, redirect_uri=http://127.0.0.1:{port}/callback, response_type=code, scope=instagram_business_basic,instagram_business_content_publish, a random state), opens it via webbrowser.open, blocks on a local HTTP listener bound to 127.0.0.1 (never 0.0.0.0) for exactly one redirect request, extracts the code, exchanges it for a short-lived token (POST api.instagram.com/oauth/access_token) then immediately exchanges that for a 60-day long-lived token (GET graph.instagram.com/access_token?grant_type=ig_exchange_token), writes the token file in the shape load_credentials expects. Use a DIFFERENT fixed port than tiktok_publish.py's consent helper (8766, not 8765) so both one-time flows can be registered as distinct redirect URIs without collision, even though they are never run simultaneously in practice.
  </behavior>
  <action>Create scripts/instagram_publish.py with the module docstring/import-safety shape from publish_queue.py's own header, adapted per 06-PATTERNS.md's instagram_publish.py section (import requests at top level). Define QUEUED/UPLOADING/PUBLISHED/KILLED/PAUSED (no SCHEDULED - Instagram's media_publish is immediate once invoked, no publishAt-style schedule per Pitfall 4) and VALID_STATUSES. DEFAULT_QUEUE_PATH = "work/_publish/instagram_queue.json". DEFAULT_NOTIFICATIONS_PATH = "work/_publish/notifications.log" (SHARED, D-06 - same value as publish_queue.py's constant, redeclared not imported, per 06-PATTERNS.md). INSTAGRAM_SCOPES = ["instagram_business_basic", "instagram_business_content_publish"] (V4 scope minimization - never request instagram_business_manage_messages/instagram_business_manage_comments, which this phase has no use for). GRAPH_API_VERSION = "v23.0". Implement load_queue/save_queue/enqueue/select_next_due/pause_item/resume_item/_find_entry copying publish_queue.py's exact shape, adapted per the &lt;behavior&gt; block above. Implement load_credentials and run_instagram_oauth_consent per 06-RESEARCH.md Pattern 3's refresh-call description and the &lt;behavior&gt; block above for the consent-flow half - reuse the same _capture_oauth_redirect_code shape described for tiktok_publish.py (own local copy, hand-rolled per platform per this project's established "not a new one invented per platform, and not a shared library" convention - do not import this helper from scripts.tiktok_publish).</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_instagram_publish.py -k "queue or credential or oauth or consent" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>load_queue/save_queue/enqueue/select_next_due/pause_item/resume_item behave exactly per the &lt;behavior&gt; block, tested; load_credentials returns a cached token, refreshes an aging one via a FakeSession, and raises FileNotFoundError with no cached token; a test asserts the local redirect-capture server binds to ("127.0.0.1", &lt;port&gt;), never "0.0.0.0" (V4/Spoofing mitigation, threat T-06-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instagram Graph API resumable-upload layer + fail-closed permission handling</name>
  <files>scripts/instagram_publish.py, tests/test_instagram_publish.py</files>
  <read_first>06-RESEARCH.md Pattern 2 (create_resumable_container/upload_local_video/poll_container_status/publish_container) - copy the request/response shapes verbatim; scripts/publish_queue.py lines 162-208 (build_insert_body) for the pure-body-builder-separate-from-network-call convention this task mirrors</read_first>
  <behavior>
    - validate_caption(caption): raises ValueError if len(caption) exceeds MAX_CAPTION_LENGTH (2200), or hashtag count (regex #\w+) exceeds MAX_HASHTAGS (30), or mention count (regex @\w+) exceeds MAX_MENTIONS (20).
    - build_media_container_params(caption): pure function, calls validate_caption first, returns {"media_type": "REELS", "upload_type": "resumable", "caption": caption}. No network call.
    - class InstagramAccessError(ValueError): pass - raised when Meta's response indicates a permission/access-tier rejection (see below), carrying an actionable message.
    - _check_meta_permission_error(response): inspects a non-2xx response's JSON body for Meta's standard {"error": {"message", "type", "code", ...}} shape; if the status code is 403, or the error object's type/message/code look like a permissions/access-tier rejection (heuristic - documented as best-effort in a code comment since this project has no verified real 403 response to test against yet, per 06-RESEARCH.md's own "no live API call made this session" caveat), raises InstagramAccessError with a message that (a) quotes Meta's original error message, (b) states in plain language "this Instagram Business account may need Advanced Access / Meta App Review before this app can publish to it", and (c) points at docs/publish-queue.md's Instagram section for next steps. For any other non-2xx response, calls response.raise_for_status() normally (propagates as a plain requests.HTTPError) - do NOT swallow non-permission errors into InstagramAccessError.
    - create_resumable_container(ig_user_id, access_token, params, session=requests): POSTs params to https://graph.facebook.com/{GRAPH_API_VERSION}/{ig_user_id}/media with access_token, routes any error response through _check_meta_permission_error before raise_for_status(), returns response.json()["id"].
    - upload_local_video(container_id, access_token, video_path, session=requests): POSTs the raw video bytes to https://rupload.facebook.com/ig-api-upload/{GRAPH_API_VERSION}/{container_id} (NOT graph.facebook.com - the one host divergence, do not "normalize" it) with offset/file_size headers per Pattern 2, routes errors through _check_meta_permission_error.
    - poll_container_status(container_id, access_token, session=requests): GETs /{container_id}?fields=status_code, returns response.json()["status_code"].
    - publish_container(ig_user_id, access_token, creation_id, session=requests): POSTs to /{ig_user_id}/media_publish with creation_id + access_token, routes errors through _check_meta_permission_error, returns response.json()["id"].
  </behavior>
  <action>Implement the functions in the &lt;behavior&gt; block, each accepting an injectable session=requests parameter. Copy the request/response field shapes and the rupload.facebook.com host divergence from 06-RESEARCH.md Pattern 2 verbatim. The _check_meta_permission_error heuristic is the single most important piece of new logic in this phase - implement it defensively (check status_code in {400, 403} AND look for permission-related substrings in the error body's message/type/code fields, case-insensitively) rather than pattern-matching one exact undocumented error shape, since 06-RESEARCH.md explicitly flags that no live Meta error response was captured this session (Assumption A2). Add a code comment stating this heuristic should be verified against a real Meta error response once credentials exist, and that STATE.md Blockers/Concerns should track this the same way Phase 3 tracked its kill-path verification (Plan 06-07 adds that STATE.md/docs note).</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_instagram_publish.py -k "create_resumable_container or upload_local_video or permission_error or caption" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>test_create_resumable_container asserts the exact media_type=REELS/upload_type=resumable params via FakeSession.calls; test_upload_local_video asserts the POST targets rupload.facebook.com (not graph.facebook.com) with correct offset/file_size headers; a test asserts a 403 Meta response with a permission-flavored error body raises InstagramAccessError with an actionable message; a separate test asserts a non-permission 500/other error propagates as a plain requests.HTTPError, not InstagramAccessError; oversized-caption/too-many-hashtags/too-many-mentions tests assert ValueError before any session call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orchestration (dry-run gate, attempt-then-fail-closed, write-ahead) and reconciliation</name>
  <files>scripts/instagram_publish.py, tests/test_instagram_publish.py</files>
  <read_first>scripts/publish_queue.py lines 211-287 (upload_and_schedule's dry-run-gate-first ordering and write-ahead persistence) and lines 559-592 (_success_notification_text/_error_notification_text/_upload_one's error-vs-success branching)</read_first>
  <behavior>
    - upload_and_publish(queue, entry, credentials_factory, config, ig_user_id, session=requests, queue_path=DEFAULT_QUEUE_PATH): if not config.instagram_enabled, returns {"dry_run": True} as the LITERAL FIRST statement - credentials_factory is never called, no session call happens. Otherwise: access_token = credentials_factory(); entry["status"]=UPLOADING, save_queue (write-ahead #1); params = build_media_container_params(entry["caption"]); container_id = create_resumable_container(ig_user_id, access_token, params, session) - per the user's explicit decision, this call is attempted directly with NO prior gating/permission-check call, unlike TikTok's creator_info/query; entry["container_id"] = container_id, save_queue (write-ahead #2, before the byte upload starts); upload_local_video(container_id, access_token, entry["video_path"], session); poll poll_container_status(container_id, access_token, session) in a loop until status_code is a terminal value (FINISHED or ERROR/EXPIRED) - never call publish_container before FINISHED is observed; on FINISHED: media_id = publish_container(ig_user_id, access_token, container_id, session), entry["status"]=PUBLISHED, entry["media_id"]=media_id, save_queue, return {"media_id": media_id}; on ERROR/EXPIRED: raise RuntimeError describing the container's failed state, entry status stays UPLOADING for the next reconcile pass. Any InstagramAccessError raised by create_resumable_container/upload_local_video/publish_container propagates up through this function unchanged (it is a ValueError subclass, callers catch it same as any other Exception) - do not catch and reinterpret it here.
    - _upload_one(entry, queue, credentials_factory, config, ig_user_id, session): mirrors publish_queue.py's exact error-vs-success branching - wraps upload_and_publish in try/except, appends an error notification (using str(error), which for an InstagramAccessError already contains the actionable Advanced-Access-may-be-needed guidance from Task 2) and re-raises on any Exception - this is the "fail closed with a clear, actionable error message, not a silent skip, not a crash" behavior the user explicitly required: the error is loud (notified + raised), never silently swallowed, and never an opaque unlabeled traceback (InstagramAccessError's message is self-explanatory). On a dry-run result, prints/returns without notifying. On success, appends a "залил {seq} в Instagram" style success notification (no SELF_ONLY-equivalent branch - Instagram has no analog to TikTok's D-05 trap per CONTEXT.md D-05's TikTok-only scope and 06-PATTERNS.md's explicit "No D-05-equivalent gating check" instruction).
    - reconcile_uploading(queue, entry, credentials_factory, ig_user_id, session): if entry has a media_id already, nothing to do (already terminal). If entry has a container_id but no media_id, call poll_container_status; if FINISHED, call publish_container to complete the interrupted flow (entry["status"]=PUBLISHED, save) - document in a code comment that Meta's behavior on a media_publish retry against an already-published creation_id is unverified against a live account (06-RESEARCH.md never captured this from a real call), so this is a best-effort completion, not a guaranteed-safe idempotent replay; if ERROR/EXPIRED, reset: status=QUEUED, container_id=None, save. If entry has NO container_id recorded (crash before create_resumable_container returned), reset to QUEUED directly with no API call.
    - reconcile_all_uploading(queue, credentials_factory, ig_user_id, session): iterates every UPLOADING entry, calling reconcile_uploading on each; wired as the mandatory first step before select_next_due is ever called.
  </behavior>
  <action>Implement upload_and_publish, _upload_one, reconcile_uploading, and reconcile_all_uploading per the &lt;behavior&gt; block, importing append_notification from scripts.publish_queue (the one sanctioned cross-script import, reused verbatim - do not reimplement the notification log). ig_user_id is a parameter (not derived from a queue-manifest field) - it identifies the authenticated Instagram Business account and should be passed through from the CLI layer (Plan 06-06) the same way credentials_factory is, not hardcoded or looked up from an API call in this plan. Deliberately do NOT read or reference config.daily_slots_utc anywhere in this module (06-RESEARCH.md Open Question 3): Instagram's media_publish is immediate once invoked, with no publishAt-equivalent to schedule against, so the grid has no role here - "select_next_due returns at most one entry, --check uploads at most one item per invocation" is the only pacing this platform needs.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_instagram_publish.py -k "dry_run or sequencing or idempotent or reconcile or access_error" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>test_dry_run_default_no_upload asserts zero session calls and zero credentials_factory calls when instagram_enabled=False; test_poll_then_publish_sequencing asserts publish_container is never called before poll_container_status returns FINISHED; test_idempotent_retry_no_duplicate-equivalent asserts an entry with a container_id already recorded never triggers a second create_resumable_container call on reconciliation; a test asserts an InstagramAccessError raised by create_resumable_container propagates out of upload_and_publish/_upload_one with its actionable message intact AND is appended to the notification log (not silently skipped, not swallowed); full test_instagram_publish.py suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local queue manifest → Instagram Graph API | Locally-authored caption/video_path data crosses into an external platform's public-facing API |
| Browser redirect → local HTTP listener (one-time consent) | The one-time OAuth consent flow opens a local port to receive an authorization code |
| Meta permission-error response → InstagramAccessError classification | A heuristic string/status-code match decides whether an error is access-tier-related; a false negative here would surface a confusing generic error instead of the actionable one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Spoofing / Elevation of Privilege | Local OAuth redirect-capture HTTP listener (one-time consent flow) | high | mitigate | Bound explicitly to 127.0.0.1 only (never 0.0.0.0), single-request handling, tested directly (Task 1) |
| T-06-06 | Tampering | build_media_container_params (caption from queue manifest) | medium | mitigate | validate_caption raises ValueError before any network call on oversized caption/excess hashtags/mentions, mirroring build_insert_body's exact per-field-limit pattern (V5) |
| T-06-07 | Repudiation / reliability (not classic STRIDE) | media_publish retried during reconciliation against an already-published container | medium | accept | Documented as a known, unverified edge case (code comment + STATE.md/docs note in Plan 06-07) rather than invented behavior presented as verified - matches this project's precedent of flagging unverified live-API assumptions (Phase 3's Assumption A1 kill-path verification) instead of silently guessing |
</threat_model>

<verification>
- `pytest tests/test_instagram_publish.py --basetemp=D:/shorts-maker/.pytest-tmp` - full file green.
- Dry-run makes zero HTTP calls (asserted via FakeSession.calls == []).
- No pre-publish gating call exists anywhere in this module (grep confirms no creator_info/query-equivalent function) - the design attempts Standard Access directly per the user's decision.
- A Meta permission-flavored error is converted to InstagramAccessError with an actionable message; any other error propagates unchanged.
- Write-ahead persistence is visible in the saved queue file at the moment each network call fires.
</verification>

<success_criteria>
`scripts/instagram_publish.py` is a fully importable, fully-tested module capable of publishing one queued clip to Instagram Reels end-to-end (given real credentials), safely dry-run by default, implementing the user's explicit "attempt Standard Access first, fail closed with an actionable error if Advanced Access is actually required" design rather than a static always-needs-review gate - pause/kill and the CLI wrapper are Plan 06-06's job.
</success_criteria>

<output>
Create `.planning/phases/06-tiktok-instagram-auto-publish/06-04-SUMMARY.md` when done.
</output>
