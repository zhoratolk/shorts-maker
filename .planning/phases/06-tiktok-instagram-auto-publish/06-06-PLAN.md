---
phase: 06-tiktok-instagram-auto-publish
plan: 06
type: execute
wave: 3
depends_on: ["06-04"]
files_modified:
  - scripts/instagram_publish.py
  - tests/test_instagram_publish.py
autonomous: true
requirements: [PUB-07]
tags: [publish, instagram, cli]

must_haves:
  truths:
    - "Killing a not-yet-uploaded Instagram queue entry (QUEUED/PAUSED/UPLOADING-without-media_id) is local-only - no API call"
    - "Killing an already-PUBLISHED Instagram entry raises RuntimeError rather than silently no-oping or pretending to revert it - Instagram has no un-publish API (Pitfall 4)"
    - "python scripts/instagram_publish.py --check reconciles stuck UPLOADING entries first, then uploads at most one due item per invocation, identically to publish_queue.py's --check contract"
  artifacts:
    - "scripts/instagram_publish.py: kill_item, build_argument_parser, run_command, main"
    - "tests/test_instagram_publish.py: kill_item tests + CLI run_command tests"
  key_links:
    - "run_command's --check path calls reconcile_all_uploading before select_next_due, exactly mirroring publish_queue.py::run_command's ordering"
    - "main()'s credentials_factory wraps load_credentials(args.client_secret, args.token, session=requests); ig_user_id comes from a new --ig-user-id CLI flag (no discovery API call is made - the account is the one the user already registered as a tester in the Meta App Dashboard per D-02)"
---

<objective>
Finish `scripts/instagram_publish.py`: add `kill_item` (with Instagram's Pitfall-4 divergence from YouTube's revert-and-verify kill) and the CLI wrapper (`--check`/`--now`/`--pause`/`--kill`/`--resume`/`--list`), matching `publish_queue.py`'s exact CLI contract and dispatch shape.

Purpose: PUB-04's pause/kill safety mechanism must exist for Instagram too, but Instagram's `media_publish` is immediate once invoked - there is no server-side "cancel a scheduled release" primitive (06-RESEARCH.md Pitfall 4). The CLI is what actually gets invoked by the extended Windows Task Scheduler task (Plan 06-07's docs update).

Output: `scripts/instagram_publish.py` complete and independently runnable as a CLI (`python scripts/instagram_publish.py --check`, etc.).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-CONTEXT.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-RESEARCH.md
@.planning/phases/06-tiktok-instagram-auto-publish/06-PATTERNS.md

# The whole-file analog this plan finishes mirroring (kill_item/CLI shape):
@scripts/publish_queue.py
@tests/test_publish_queue.py

# The module this plan extends (built in Plan 06-04 - queue/OAuth/HTTP/orchestration
# already exist, this plan only adds kill_item + CLI):
@.planning/phases/06-tiktok-instagram-auto-publish/06-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kill_item with Pitfall-4 semantics</name>
  <files>scripts/instagram_publish.py, tests/test_instagram_publish.py</files>
  <read_first>scripts/publish_queue.py lines 290-398 (_NOT_YET_UPLOADED_STATUSES, kill_item, and the cancel_scheduled_release/verify_killed pair kill_item calls for an already-SCHEDULED entry) - Instagram's kill_item diverges specifically at the "already live" branch</read_first>
  <behavior>
    - _NOT_YET_UPLOADED_STATUSES = frozenset({QUEUED, PAUSED, UPLOADING}) - same shape as publish_queue.py.
    - kill_item(queue, clip_id): if entry.status is in _NOT_YET_UPLOADED_STATUSES AND entry has no media_id recorded yet (regardless of whether a container_id exists - a created-but-not-yet-published container has nothing public on Instagram), this is local-only - flip status to KILLED, no API call. If entry.status is PUBLISHED (media_id recorded), raise RuntimeError with a message stating plainly that Instagram has no un-publish/cancel API for an already-published Reel (Pitfall 4) and the entry's status is left untouched (not silently marked KILLED) - deliberate divergence from publish_queue.py's revert-then-verify PUBLISHED-branch, since no equivalent API call exists for Instagram either.
  </behavior>
  <action>Implement kill_item exactly per the &lt;behavior&gt; block. This function takes no service_factory/credentials_factory parameter at all since it never makes a network call in any branch - a PUBLISHED entry always raises rather than attempting an API call that doesn't exist.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_instagram_publish.py -k "kill" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>kill_item on QUEUED/PAUSED/UPLOADING(no media_id, with or without container_id) entries flips to KILLED with no network call in any case; kill_item on a PUBLISHED entry raises RuntimeError and leaves status unchanged, tested explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: CLI wrapper</name>
  <files>scripts/instagram_publish.py, tests/test_instagram_publish.py</files>
  <read_first>scripts/publish_queue.py lines 595-720 (build_argument_parser, run_command, main) - copy the dispatch shape exactly, swapping service_factory for credentials_factory and the flag defaults</read_first>
  <action>Implement build_argument_parser() with --check, --now CLIP_ID, --pause CLIP_ID, --kill CLIP_ID, --resume CLIP_ID, --list, --client-secret (default "instagram_client_secret.json"), --token (default "instagram_token.json"), --ig-user-id (required for any live-call path: --now/--check when instagram_enabled is true - not required for --list/--pause/--resume/--kill, which never touch the API), --config (default "config.yaml"). Implement run_command(args, credentials_factory, config, ig_user_id, session=requests) with identical dispatch order to publish_queue.py::run_command (--list, --pause, --resume, --kill, --now, --check, fallback usage message) - --check calls reconcile_all_uploading(queue, credentials_factory, ig_user_id, session) then save_queue BEFORE checking config.instagram_enabled or calling select_next_due. --kill calls kill_item(queue, args.kill) (no credentials_factory/ig_user_id arg per Task 1's design); a RuntimeError from kill_item (PUBLISHED entry) is NOT caught here - let it propagate loudly, matching tiktok_publish.py's identical design choice. Implement main(): parses args, builds config = load_config(args.config).publish, defines credentials_factory() calling this module's own load_credentials(args.client_secret, args.token, session=requests), calls run_command(args, credentials_factory, config, args.ig_user_id).</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -m pytest tests/test_instagram_publish.py -k "cli or run_command or list_prints or check" -x --basetemp=D:/shorts-maker/.pytest-tmp</automated>
  </verify>
  <done>--list prints seq/status/caption for every entry sorted by seq; --pause/--resume/--kill on an unknown clip_id exit(2) with a stderr message, mirroring publish_queue.py exactly; --check reconciles before selecting, and uploads at most one item per invocation; full test_instagram_publish.py suite (all 3 plans' tests combined) green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → credential file paths / ig_user_id | --client-secret/--token/--ig-user-id accept arbitrary values from the invoker (same trust level as publish_queue.py's --client-secret/--token, no new boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Repudiation | kill_item silently succeeding on an already-PUBLISHED entry | medium | mitigate | kill_item raises RuntimeError instead of silently marking KILLED when no revert is actually possible (Task 1) - an operator can never be misled into thinking a live Instagram Reel was pulled down when it wasn't |
</threat_model>

<verification>
- `pytest tests/test_instagram_publish.py --basetemp=D:/shorts-maker/.pytest-tmp` - full file green (all 3 plans' tests combined).
- `python scripts/instagram_publish.py --help` runs without error and lists all 7 flags.
- kill_item on a PUBLISHED entry raises, never silently succeeds.
</verification>

<success_criteria>
`scripts/instagram_publish.py` is a complete, independently-runnable CLI module with the exact same operator surface (`--check`/`--now`/`--pause`/`--kill`/`--resume`/`--list`) as `publish_queue.py`, ready to be wired into the Windows Task Scheduler task (Plan 06-07).
</success_criteria>

<output>
Create `.planning/phases/06-tiktok-instagram-auto-publish/06-06-SUMMARY.md` when done.
</output>
