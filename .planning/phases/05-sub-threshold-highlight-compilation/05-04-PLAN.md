---
phase: 05-sub-threshold-highlight-compilation
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - .claude/skills/make-shorts/SKILL.md
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
tags: [compilation, orchestration, skill-md]

must_haves:
  truths:
    - "A candidate whose tightest reasonable step-5 trim is still below config.clip.min_seconds is never force-padded or silently skipped — it is marked sub_threshold, tagged (D-01), and routed into a grouping pass instead of the normal single-clip finish (COMP-01)"
    - "Sub-threshold candidates are grouped by same-session, semantic tag-similarity judgment (D-02), ordered strongest-first (D-04), and only ever grouped with candidates from the same video_stem (COMP-03) — this is a fully automatic step with no new review gate, matching D-03/the existing transitions precedent"
    - "A grouping/transition-selection failure at compilation time degrades to skipping the compilation for that run (or falling back to plain cuts), never aborting the pipeline or blocking normal clips from rendering (Fail-open constraint)"
  artifacts:
    - "SKILL.md: a new step-5 bullet describing sub-threshold detection + tagging, gated on the existing trim-decision logic"
    - "SKILL.md: a new step 5b describing the one-shot grouping pass, flattened-segment transitions reuse, compilation.py invocation, and CANDIDATES.md append wiring"
    - "SKILL.md: the PLAN.json schema block documents the compilation entry shape alongside the existing single-clip shape; the render.py invocation note confirms no new CLI flags are needed"
  key_links:
    - "Step 5's per-candidate sub_threshold/tag fields feed step 5b's grouping pass, which feeds scripts/compilation.py::build_compilation_entry (Plan 05-02), whose output becomes a PLAN.json entry render.py::render_clip (Plan 05-03) dispatches on"
    - "The flattened cross-member segment list step 5b builds is fed, unmodified, into scripts/transitions.py::select_boundary_transitions (unchanged, reused verbatim per its own documented Phase-5-reuse intent) and into scripts/jumpcuts.py::remap_words for compilation-wide subtitle timing"
    - "scripts/candidates.py::append_compilation_sections_markdown (Plan 05-01) is called at the end of step 5b with the grouping pass's own groups/unmatched data, appending to the same CANDIDATES.md step 3/4 already wrote"
---

<objective>
Wire the mechanics built in Plans 05-01/02/03 into the actual pipeline: extend `SKILL.md`'s step 5 (Refine, pass 2) with sub-threshold detection/tagging per candidate, add a new step 5b that runs one Claude grouping pass over the session's sub-threshold pool and turns each resulting group into a compilation `PLAN.json` entry (reusing `scripts/transitions.py`'s existing boundary-selection CLI unchanged against a flattened cross-member segment list), surfaces unmatched candidates and formed groups in `CANDIDATES.md`, and documents the compilation entry shape in the `PLAN.json` schema block plus a one-line note that `render.py`'s step 6 invocation needs no new flags.

Purpose: closes COMP-01/02/03 end-to-end — without this plan, every mechanical piece from Plans 05-01/02/03 exists but is unreachable from a real `/make-shorts` run.
Output: reworked `.claude/skills/make-shorts/SKILL.md` (step 5 extended, new step 5b, PLAN.json schema updated, step 6 note added).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-CONTEXT.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-RESEARCH.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-PATTERNS.md
@.claude/skills/make-shorts/SKILL.md
@docs/viral-clips-ru.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 5 sub-threshold detection + tagging bullet (COMP-01, D-01)</name>
  <files>.claude/skills/make-shorts/SKILL.md</files>
  <read_first>.claude/skills/make-shorts/SKILL.md:125-134 (step 5's opening "Exact trim points" bullet, where the tightest-trim decision already happens — this is the one true place COMP-01's threshold check belongs, per 05-RESEARCH.md Pitfall 2: never decide sub-threshold at step 3's raw window), lines 135-147 (Jump cuts / Context-driven transitions bullets — mirror their fail-open wording style and "only when ..." gating pattern)</read_first>
  <behavior>
    - Immediately after the existing "Exact trim points" bullet, add a new bullet: once this candidate's tightest reasonable trim is decided, if the resulting duration is still below `config.clip.min_seconds`, do not force-pad it to the floor — instead mark it sub-threshold and assign a short, free-form tag describing the gameplay situation or theme (D-01: same register as the existing `reason` field, e.g. "died to same boss", "chat spam reaction" — never a fixed enum/category).
    - State explicitly that a sub-threshold candidate skips the rest of step 5's per-candidate finishing work for this run (crop_style, jump cuts, transitions, punch-zoom, subtitles, title/filename, metadata) — those get decided once for the whole compilation it may join in step 5b below (D-06), not per individual sub-threshold candidate. A candidate whose tightest trim already reaches `min_seconds` is completely unaffected and proceeds through the rest of step 5 exactly as today.
    - Note that jump-cut computation (step 5's existing "Jump cuts" bullet, gated on `config.jumpcuts.enabled`) still runs normally for a sub-threshold candidate if it applies — removing dead air is orthogonal to whether the candidate ends up standalone or in a compilation, and its resulting `keep_segments` (if any) travels with the candidate into step 5b.
  </behavior>
  <action>Edit SKILL.md with a scoped Edit (do not rewrite the whole file): insert the sub-threshold detection/tagging bullet directly after the existing "Exact trim points" bullet in step 5, worded per the behavior block above, citing COMP-01/D-01. Do not alter any other existing step-5 bullet's wording.</action>
  <verify>
    <automated>cd D:/shorts-maker && grep -v '^#' .claude/skills/make-shorts/SKILL.md | grep -c "sub_threshold\|sub-threshold"</automated>
  </verify>
  <done>Step 5 documents sub-threshold detection/tagging as a per-candidate outcome of the existing trim decision, gated correctly, with the rest of step 5's per-candidate work explicitly skipped for a sub-threshold candidate.</done>
</task>

<task type="auto">
  <name>Task 2: Step 5b grouping pass + PLAN.json schema + CANDIDATES.md append + step 6 note (COMP-02, COMP-03, D-02, D-03, D-04, D-06)</name>
  <files>.claude/skills/make-shorts/SKILL.md</files>
  <read_first>.claude/skills/make-shorts/SKILL.md:125-210 (the full step 5 bullet list through the PLAN.json object schema code block, ~196-209) and lines 212-220 (step 6 render invocation), scripts/transitions.py:230-260 (select_boundary_transitions's own docstring: "Generic over any keep_segments list ... so Phase 5's cross-clip compilation can reuse it"), 05-RESEARCH.md Architecture Patterns > "Recommended Pipeline Placement" and Pattern 2 (flattened-segment reuse for transitions/subtitle remap)</read_first>
  <behavior>
    - Insert a new "5b. Group sub-threshold candidates into compilations" subsection after all of step 5's per-candidate bullets (i.e. after the last bullet, before the "Write the merged results to work/&lt;video_stem&gt;/PLAN.json" paragraph), gated on: only run this subsection if at least one approved candidate this run was marked sub-threshold in step 5 above.
    - Describe the one-shot Claude grouping pass (D-02): read every sub-threshold candidate's tag/reason from this run's own approved-candidate pool only (COMP-03 — a run is already scoped to one video_stem's `work/<video_stem>/` directory under the normal pipeline; when this is a library-wide search spanning multiple videos, group only within one video_stem's own pool at a time, never across two). Group candidates whose tags read as the same gameplay situation/theme (semantic judgment, not string matching); a group needs at least 2 members — a sub-threshold candidate with no match is left ungrouped ("unmatched"). Within each group, order members strongest-first (D-04, using the same `reason`/`coherence` signal step 3/5 already use to judge a moment's strength — mirrors `docs/viral-clips-ru.md`'s hook-near-the-front guidance already applied to single clips).
    - For each group, decide ONE uniform `crop_style` (and, if warranted, one `punch_zoom_at`) for the whole compilation exactly as step 5 already decides these per single clip (D-06) — never per member.
    - Describe building the flattened, render-order segment list for the group (Pattern 2): for each member in strongest-first order, append its own `keep_segments` (if step 5's jump-cut pass produced one for it) or its own single `[start, end]` pair.
    - Describe the compilation-scope reuse of `scripts/transitions.py`'s existing `select-transitions` CLI, unchanged, run against this flattened list exactly like a normal multi-segment clip would (only when `config.transitions.enabled` and the flattened list has more than one segment) — same fail-open handling as the existing single-clip Context-driven-transitions bullet: on any error, omit `boundary_transitions` and let the compilation fall back to plain cuts at every stitch point, never abort the compilation or the run.
    - Describe building this group's compilation `PLAN.json` entry via `scripts/compilation.py`'s CLI (members drawn from the group, each carrying its own `video_stem`/`start`/`end`/optional `keep_segments`; `config.clip.compilation_max_seconds` as the cap; the chosen `crop_style`; the `boundary_transitions` list from above if produced; `punch_zoom_at` if decided) — its own mechanical validation (group size, same-`video_stem`, length cap) is the safety net if anything upstream went wrong.
    - Describe reusing step 5's existing "Title and filename" bullet exactly once per compilation, not per member: decide a short title describing the compilation's combined theme, then call `scripts/naming.py` (the same CLI single-clip titles already use) once to get the compilation's `output_filename` — no new filename logic, same slugify/index/title shape as any other clip.
    - If `config.subtitles.enabled`: describe collecting each member's own word-level transcript words (absolute source seconds) in the same strongest-first render order as the flattened segment list above, writing them to one combined absolute-words file, then reusing `scripts/jumpcuts.py::remap-words` unchanged against that same flattened segment list (Pattern 2 — the function already handles an arbitrary elapsed-time-accumulating segment list, no source-adjacency assumption) to produce one clip-relative words file for the whole compilation; group into cues and render the `.srt` via the existing `scripts/subtitles.py` call exactly as step 5 already does for a single clip. Metadata/title text (if `config.metadata.enabled`) is likewise generated once over the compilation's combined theme via the existing `scripts/metadata.py` call, reusing the unchanged per-platform rendering step 5 already documents — not per member (D-06 / RESEARCH's once-per-compilation default).
    - Describe calling `scripts/candidates.py::append_compilation_sections_markdown` at the end of this subsection with every group formed and every sub-threshold candidate that stayed unmatched this run, appending to the same `work/<video_stem>/CANDIDATES.md` step 3/4 already wrote (D-03: visible, no re-approval gate — matches the existing automatic-no-review-gate precedent for jump cuts/transitions).
    - Update the PLAN.json object schema code block to add a second example object showing the compilation entry shape (`"type": "compilation"`, `"segments": [...]`, plus the other fields already documented for single clips), with a one-line note that `"type": "compilation"` is what distinguishes it and that `segments` replaces the single-clip `start`/`end`/`keep_segments` fields for that entry.
    - Add one sentence to step 6 (Render) noting that `render.py` now also renders `"type": "compilation"` entries automatically via the same invocation already documented — no new CLI flags are required.
  </behavior>
  <action>Edit SKILL.md with scoped Edits (do not rewrite the whole file): insert the "5b. Group sub-threshold candidates into compilations" subsection as described, citing COMP-02/COMP-03/D-02/D-03/D-04/D-06 inline where each rule is stated. Update the PLAN.json schema code block to add the compilation entry example. Add the one-sentence step 6 note. Do not alter step 3, step 4, or any other pipeline step.</action>
  <verify>
    <automated>cd D:/shorts-maker && grep -v '^#' .claude/skills/make-shorts/SKILL.md | grep -c "compilation.py" && grep -v '^#' .claude/skills/make-shorts/SKILL.md | grep -c "append_compilation_sections_markdown" && python -m pytest -m "not integration" -q</automated>
    <human-check>Read SKILL.md's new "5b. Group sub-threshold candidates into compilations" subsection and confirm: (1) it only runs when at least one sub-threshold candidate exists; (2) grouping is described as Claude's own semantic judgment, never a Python string-match, and is scoped to one video_stem's own candidate pool (COMP-03); (3) group members are ordered strongest-first (D-04); (4) crop_style/punch_zoom/subtitles/metadata are decided once per compilation, not per member (D-06); (5) transition selection and the compilation.py call are both described as fail-open (log-and-continue, never abort the run); (6) append_compilation_sections_markdown is called with the run's groups and unmatched candidates, targeting the same CANDIDATES.md step 3/4 already wrote; (7) the PLAN.json schema block and the step 6 note were both updated and no other step's wording changed.</human-check>
  </verify>
  <done>SKILL.md drives an automatic, fail-open sub-threshold grouping pass (D-02/D-03/D-04/D-06) that builds a compilation PLAN.json entry via scripts/compilation.py, reuses scripts/transitions.py's select-transitions CLI unchanged for stitch-point analysis, and surfaces every group/unmatched candidate in CANDIDATES.md; the PLAN.json schema and step 6 render note both document the new entry shape; the full non-integration test suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's grouping/tag/title judgment (D-01/D-02/D-04) -> compilation.py's CLI input | Free-form Claude output flowing into a mechanical validator via a file hand-off; already re-validated independently by compilation.py (Plan 05-02) and render.py (Plan 05-03) |
| SKILL.md orchestration step failure -> rest of the pipeline run | The grouping/transition-selection step must never propagate a failure into aborting normal (non-sub-threshold) clip rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Denial of Service | Step 5b grouping/transition-selection orchestration | low | mitigate | Both the transitions reuse and the compilation.py call are documented as fail-open (log-and-continue, omit boundary_transitions / skip the compilation entry for this run) exactly like the existing diarization/audio-energy/transitions optional steps — a compilation-specific failure never aborts the whole pipeline run or blocks normal clips from rendering |
</threat_model>

<verification>
- `grep -c "sub_threshold\|sub-threshold" .claude/skills/make-shorts/SKILL.md` and the `compilation.py`/`append_compilation_sections_markdown` grep checks all return non-zero.
- `python -m pytest -m "not integration" -q` green (confirms Plans 05-01/02/03's code is untouched and still passes after this doc-only plan).
- Human review of the new step 5b subsection against the seven checklist items in Task 2's `<human-check>`.
</verification>

<success_criteria>
- A real `/make-shorts` run with sub-threshold candidates present automatically tags, groups, and stitches them into a compilation short, with zero new manual approval steps beyond the existing candidate-approval gate.
- A sub-threshold candidate that never matches anyone this run is still visibly named in CANDIDATES.md.
- Nothing about this plan can abort a run or block a normal clip from rendering.
</success_criteria>

<output>
Create `.planning/phases/05-sub-threshold-highlight-compilation/05-04-SUMMARY.md` when done.
</output>
