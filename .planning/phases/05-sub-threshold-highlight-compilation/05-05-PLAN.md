---
phase: 05-sub-threshold-highlight-compilation
plan: 05
type: execute
wave: 1
depends_on: ["05-01", "05-02", "05-04"]
files_modified:
  - scripts/compilation.py
  - tests/test_compilation.py
  - .claude/skills/make-shorts/SKILL.md
  - scripts/candidates.py
  - tests/test_candidates.py
autonomous: true
gap_closure: true
requirements: [COMP-01, COMP-02]

must_haves:
  truths:
    - "A sub-threshold compilation group whose full pre-cap duration exceeds config.clip.compilation_max_seconds, combined with config.transitions.enabled=true (both real, non-edge-case run states), still builds successfully via build_compilation_entry — capping and a pre-cap-sized boundary_transitions list no longer combine to always raise CompilationError (CR-01 closed, COMP-02 unblocked)"
    - "A boundary_transitions list that is genuinely too short even after truncation to the fitted length still raises CompilationError — the CR-01 fix truncates, it does not silently accept any length"
    - "SKILL.md step 5b's groups.json/unmatched.json hand-off to append_compilation_sections_markdown has an explicit earlier 'write it here, as you go' instruction (WR-01) instead of being read as an unexplained pre-existing input"
    - "SKILL.md step 5b's compilation words file has the same explicit literal output path a single clip's words file already gets, so the karaoke highlight effect cannot silently disappear for a compilation due to an inferred/mismatched filename (WR-02)"
    - "append_compilation_sections_markdown degrades to a placeholder value instead of raising a bare KeyError when a hand-assembled groups.json/unmatched.json entry is missing id/title/reason/tag (WR-03)"
  artifacts:
    - "scripts/compilation.py::build_compilation_entry — boundary_transitions truncated to the fitted (post-cap) prefix before length validation"
    - "tests/test_compilation.py — new regression test combining capping-drops-members with a pre-cap-sized boundary_transitions list (the exact untested combination CR-01 identified), plus a test proving a genuinely-too-short list still raises"
    - ".claude/skills/make-shorts/SKILL.md step 5b bullet 9 — explicit groups.json/unmatched.json write instruction"
    - ".claude/skills/make-shorts/SKILL.md step 5b bullet 7 — explicit literal compilation words-file output path"
    - "scripts/candidates.py::append_compilation_sections_markdown — defensive .get() access for id/title/reason/tag"
    - "tests/test_candidates.py — new regression test for missing-field defaulting"
  key_links:
    - "SKILL.md step 5b bullet 5 (boundary_transitions computed pre-cap, against the full uncapped flattened segment list) -> scripts/compilation.py CLI (step 8, where capping actually happens) -> build_compilation_entry's own truncation now reconciles the two without SKILL.md needing to predict the cap result"
    - "SKILL.md step 5b bullets 1-8 (where groups/unmatched data is actually decided) -> the two JSON files bullet 9 reads -> append_compilation_sections_markdown -> CANDIDATES.md (D-03 surfacing)"
---

<objective>
Close the single Critical gap (CR-01) blocking Success Criterion 2 / COMP-02: `build_compilation_entry` in `scripts/compilation.py` validates `boundary_transitions` length against the post-cap `fitted` member list, but `SKILL.md` step 5b computes `boundary_transitions` pre-cap (before `compilation.py`'s own length-ceiling capping ever runs). Whenever capping actually drops trailing members AND `config.transitions.enabled` is true, `build_compilation_entry` always raises `CompilationError`, silently discarding that compilation exactly like this phase exists to prevent. Fix per the code reviewer's recommendation: truncate the caller-supplied `boundary_transitions` to the fitted prefix inside `build_compilation_entry` (mechanically safe — capping only ever drops a contiguous trailing run under D-04's literal strongest-first ordering), and add the regression test coverage for the capping+transitions-enabled combination that shipped untested in Wave 1.

Also close three of 05-REVIEW.md's four Warning-level findings that the verifier flagged as worth addressing in this pass: WR-01 (SKILL.md step 9 reads `groups.json`/`unmatched.json` with no earlier "write it here" instruction — directly weakens confidence in Success Criterion 1/COMP-01's end-to-end reliability, per 05-VERIFICATION.md's own Anti-Patterns note), WR-02 (compilation karaoke-words file path is inferred by convention rather than spelled out, risking silent feature loss), and WR-03 (unchecked dict indexing in `append_compilation_sections_markdown` raises a bare `KeyError` on a malformed hand-assembled groups/unmatched dict instead of degrading gracefully).

WR-04 (duplication between `_build_compilation_fold` and `_build_transition_fold` in `scripts/render.py`) is explicitly deferred, not silently dropped: it is a maintainability-only observation with zero functional impact on any Success Criterion or requirement — both fold implementations are independently tested and correct today, and extracting a shared helper touches already-verified, working render-path code purely for tech-debt reasons. It belongs in a future cleanup pass, not a gap-closure plan whose job is to restore blocked/incorrect behavior.

Purpose: restores COMP-02 (grouped sub-threshold candidates render as one full-length short via the transition engine) to a working, regression-tested state, and hardens the orchestration/surfacing plumbing the verifier flagged as weakening confidence in COMP-01's reliability.
Output: `scripts/compilation.py` (truncation fix), `tests/test_compilation.py` (new regression coverage), `.claude/skills/make-shorts/SKILL.md` (two documentation-gap fixes), `scripts/candidates.py` (defensive access), `tests/test_candidates.py` (new regression coverage).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-CONTEXT.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-PATTERNS.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-VERIFICATION.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-REVIEW.md
@scripts/compilation.py
@scripts/candidates.py
@.claude/skills/make-shorts/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01 — truncate boundary_transitions to the fitted prefix in build_compilation_entry</name>
  <files>scripts/compilation.py, tests/test_compilation.py</files>
  <read_first>scripts/compilation.py:62-94 (capping loop at 62-81, boundary_transitions length validation at 83-94 — the exact two blocks CR-01 identifies as unreconciled); tests/test_compilation.py:40-54 (existing capping-only test to model the new regression test's member/duration setup after) and tests/test_compilation.py:68-78 (existing length-mismatch test, no capping involved — the untested combination is these two scenarios together)</read_first>
  <behavior>
    - Test 1 (the CR-01 regression, the exact untested combination): 3 members of durations 60/60/60 (same setup as the existing `test_build_compilation_entry_caps_at_compilation_max_seconds_dropping_weakest`), cap 150 — capping drops the third member, leaving 2 fitted members (1 boundary). Call `build_compilation_entry` with a `boundary_transitions` list of length 2 (`["crossfade", "whip_pan"]`), sized for the PRE-cap 3-member list exactly as SKILL.md step 5b bullet 5 computes it today (before capping ever runs). Expect: no exception, `entry["segments"]` has 2 members, `entry["boundary_transitions"] == ["crossfade"]` (the pre-cap list's own first-boundary prefix, not re-derived or dropped).
    - Test 2 (truncation must not mask a genuinely too-short list): same 3-member/cap-150 setup, but `boundary_transitions=[]` (empty — fewer than even the fitted list's 1 required boundary, unrelated to capping). Expect: `CompilationError` still raised naming the expected length, proving the fix truncates down only, never pads, and preserves the original guard's defense-in-depth role alongside `render.py`'s own independent boundary_transitions length re-validation (Plan 05-03).
  </behavior>
  <action>
    In `build_compilation_entry`, replace the boundary_transitions length check (scripts/compilation.py:87-94) with: compute `expected_length = flattened_segment_count - 1` as today; if `len(boundary_transitions) > expected_length`, reassign `boundary_transitions = boundary_transitions[:expected_length]` — capping only ever drops a contiguous trailing run of members under D-04's literal strongest-first ordering, so the pre-cap boundary list's own prefix is exactly the boundaries that survive; then keep the existing `if len(boundary_transitions) != expected_length: raise CompilationError(...)` check unchanged immediately after, so a list still too short after truncation (a real caller bug unrelated to capping) continues to raise exactly as today. Do not touch the capping loop (lines 62-81) or any other part of the function — this is a validation-block-only fix, per the code reviewer's own recommended approach (05-REVIEW.md CR-01 Fix section), placed inside build_compilation_entry rather than requiring SKILL.md to predict the cap result in advance. Add the two tests described in the behavior block to tests/test_compilation.py, named so the first contains the literal substring "caps_and_truncates_boundary_transitions" and the second contains "still_rejects" (or equivalent, clearly distinguishing the two cases) — do not modify any of the 7 existing tests in this file.
  </action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_compilation.py -x -q</automated>
  </verify>
  <done>build_compilation_entry no longer raises when a real capping-drops-members + pre-cap-sized boundary_transitions combination occurs (the exact CR-01 reproduction from 05-VERIFICATION.md now returns a valid entry instead of raising), while a genuinely too-short boundary_transitions list still raises; all 9 tests in tests/test_compilation.py pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-01 + WR-02 — SKILL.md step 5b documentation gaps</name>
  <files>.claude/skills/make-shorts/SKILL.md</files>
  <read_first>.claude/skills/make-shorts/SKILL.md:202 (bullet 1, One-shot grouping pass — where groups/unmatched are actually decided), .claude/skills/make-shorts/SKILL.md:224 (bullet 7, Subtitles and metadata — the words-file path gap), .claude/skills/make-shorts/SKILL.md:226-236 (bullet 8, the compilation.py CLI call whose members_json this pass already writes explicitly, and bullet 9, the append call this fix feeds), .claude/skills/make-shorts/SKILL.md:153-167 (step 5's single-clip words-file bullet — the explicit-literal-path style bullet 7 must now match)</read_first>
  <behavior>
    - WR-01: Rework bullet 9's lead-in sentence so it explicitly instructs accumulating two running JSON files as bullets 1-8 are worked through for each group this run, before the existing `append_compilation_sections_markdown` call: `work/<video_stem>/compilations/groups.json` (a list of `{"members": [{"id": ...}, ...], "title": ...}` dicts, one appended per group as bullet 8 finishes that group's `_entry.json` build) and `work/<video_stem>/compilations/unmatched.json` (a list of `{"start": ..., "end": ..., "reason": ..., "tag": ...}` dicts, one per sub-threshold candidate that never joined a group in bullet 1's grouping pass). Keep the existing `python -c` call and its trailing explanation sentence unchanged — only the lead-in sentence changes, from treating the two files as pre-existing to explicitly instructing their accumulation.
    - WR-02: Extend bullet 7 with the same explicit-literal-path treatment step 5's single-clip words bullet already gets (SKILL.md:153-167): after "produce one clip-relative words file for the whole compilation", add the literal output path `work/<video_stem>/compilations/<compilation_stem>_words.json` (must match the compilation's own `.srt` filename stem — `render_clip` derives the karaoke words path purely from `subtitles_path`'s stem, so any other name silently falls back to plain `.srt` parsing with no error) and show the literal `scripts/jumpcuts.py remap-words` CLI invocation with all three explicit paths (the combined `_words_absolute.json` from earlier in the same bullet as the first arg, `<compilation_stem>_segments.json` from bullet 4 as the second arg — the same flattened segment list already written there — and `<compilation_stem>_words.json` as the third/output arg), mirroring step 5's own three-arg `remap-words` call shape (SKILL.md:155). Do not alter the metadata half of bullet 7, or any other bullet.
  </behavior>
  <action>Edit SKILL.md with scoped Edits (do not rewrite the whole file): rework bullet 9's lead-in per WR-01 above, and extend bullet 7 with the explicit words-file path + CLI invocation per WR-02 above. Do not alter step 5, steps 1-4/6, or any other step-5b bullet's wording.</action>
  <verify>
    <automated>cd D:/shorts-maker && grep -v '^#' .claude/skills/make-shorts/SKILL.md | grep -c "compilations/<compilation_stem>_words\.json" && grep -v '^#' .claude/skills/make-shorts/SKILL.md | grep -c "groups\.json" && python -m pytest -m "not integration" -q</automated>
    <human-check>Read SKILL.md's step 5b bullet 9 and bullet 7 and confirm: (1) bullet 9 now explicitly instructs accumulating groups.json/unmatched.json as bullets 1-8 are worked through, before the existing append_compilation_sections_markdown call — it no longer reads as an unexplained pre-existing input; (2) bullet 7 now spells out the literal work/&lt;video_stem&gt;/compilations/&lt;compilation_stem&gt;_words.json output path and the full three-argument remap-words CLI call, matching step 5's single-clip bullet's level of explicitness; (3) no other step-5b bullet's wording changed.</human-check>
  </verify>
  <done>SKILL.md step 5b bullet 9 explicitly instructs writing groups.json/unmatched.json as the grouping pass progresses (WR-01 closed), and bullet 7 spells out the compilation words file's literal output path and full remap-words CLI invocation (WR-02 closed); the full non-integration test suite stays green (confirms this doc-only task didn't touch any tested code path).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix WR-03 — defensive dict access in append_compilation_sections_markdown</name>
  <files>scripts/candidates.py, tests/test_candidates.py</files>
  <read_first>scripts/candidates.py:83-110 (append_compilation_sections_markdown — the unchecked member['id']/group['title']/candidate['reason']/candidate['tag'] indexing at lines 97-98 and 107), scripts/candidates.py:46-59 (merge_candidates — the file's own established .get()-with-default convention this fix must match), tests/test_candidates.py:162-190 (existing append_compilation_sections_markdown test to model the new regression test's fixture shape after)</read_first>
  <behavior>
    - New test: build a `groups` list where one member dict omits `id` and the group dict omits `title`, and an `unmatched` list where one candidate dict omits `reason` and `tag` (but keeps `start`/`end`, which stay required — a missing start/end is a more structural bug already caught upstream by build_compilation_entry, out of scope for this display-only fix). Call `append_compilation_sections_markdown` and assert it does not raise, and that the resulting markdown contains readable placeholder text for each missing field instead of crashing.
  </behavior>
  <action>
    In `append_compilation_sections_markdown` (scripts/candidates.py), replace the four unchecked bracket accesses with `.get()` calls carrying sensible display defaults, matching `merge_candidates`'s existing defensive style in the same file: `member['id']` -> `member.get('id', '?')`; `group['title']` -> `group.get('title', '(untitled compilation)')`; `candidate['reason']` -> `candidate.get('reason', '(no reason given)')`; `candidate['tag']` -> `candidate.get('tag', '(untagged)')`. Leave `candidate["start"]`/`candidate["end"]` (fed to format_timecode) as required bracket access — unchanged, since a missing start/end is a structural malformation this display function should not paper over. Add the regression test described in the behavior block to tests/test_candidates.py, named to contain the literal substring "missing_fields" or "defaults_missing" — do not modify either of the two existing append_compilation_sections_markdown tests.
  </action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_candidates.py -x -q</automated>
  </verify>
  <done>append_compilation_sections_markdown degrades to readable placeholder text instead of raising KeyError when a hand-assembled groups.json/unmatched.json entry is missing id/title/reason/tag; all tests in tests/test_candidates.py pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md orchestrator's pre-cap `boundary_transitions` JSON hand-off -> `build_compilation_entry` (`scripts/compilation.py` CLI, step 8) | An LLM-authored file artifact whose length was computed against the full uncapped member list; must be safely reconciled against the function's own post-cap `fitted` list without the caller needing to predict capping in advance |
| Hand-assembled `groups.json`/`unmatched.json` (SKILL.md step 9, orchestrator-written per-run JSON, not schema-validated) -> `append_compilation_sections_markdown` | Same class of untrusted-shape-from-LLM-orchestrator input; a missing `id`/`title`/`reason`/`tag` must not raise an unhandled `KeyError` that aborts step 9's CANDIDATES.md (D-03) bookkeeping for the whole run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering / Denial of Service | `build_compilation_entry`'s `boundary_transitions` validation | high | mitigate | CR-01 fix (Task 1): truncate the caller-supplied pre-cap `boundary_transitions` to the fitted post-cap prefix before length-validating, so the real, non-adversarial "transitions enabled + capping drops trailing members" run state never raises `CompilationError` and silently drops the compilation; a genuinely too-short list still raises after truncation, preserving the guard's defense-in-depth role alongside `render.py`'s own independent re-validation (Plan 05-03) |
| T-05-09 | Denial of Service | SKILL.md step 5b's `groups.json`/`unmatched.json` hand-off (bullet 9) | low | mitigate | WR-01 fix (Task 2): bullet 9 previously read two JSON files with no earlier instruction telling the orchestrator to write them, risking a `FileNotFoundError` that aborts step 9's CANDIDATES.md bookkeeping on any real run with sub-threshold groups; bullet 9 now explicitly instructs accumulating both files as bullets 1/8 produce their data |
| T-05-10 | Information Disclosure / Denial of Service | `append_compilation_sections_markdown`'s dict field access | low | mitigate | WR-03 fix (Task 3): `.get(...)` with sensible display defaults for `id`/`title`/`reason`/`tag` instead of bare bracket indexing, so a malformed `groups.json`/`unmatched.json` entry degrades to a placeholder in CANDIDATES.md instead of raising an unhandled `KeyError` that aborts step 9's bookkeeping |
</threat_model>

<verification>
- `pytest tests/test_compilation.py -x -q` green, including the two new CR-01 regression tests (Task 1).
- `pytest tests/test_candidates.py -x -q` green, including the new WR-03 regression test (Task 3).
- `python -m pytest -m "not integration" -q` green (full non-integration regression check after the SKILL.md-only Task 2).
- Direct reproduction of 05-VERIFICATION.md's own CR-01 repro snippet (3 members, cap 150, `boundary_transitions=["crossfade", "crossfade"]` sized for the pre-cap 2-boundary list) now returns a valid entry instead of raising `CompilationError`.
- Human review of SKILL.md's reworked bullet 7/bullet 9 against Task 2's `<human-check>` checklist.
</verification>

<success_criteria>
- A sub-threshold compilation group that both overflows `compilation_max_seconds` and has `config.transitions.enabled=true` builds successfully instead of always raising `CompilationError` — Success Criterion 2 / COMP-02 is unblocked.
- SKILL.md's step 5b orchestration instructions leave no undocumented file hand-offs between the grouping-pass bullets and the CANDIDATES.md-surfacing bullet, and the compilation words-file path is as explicit as the single-clip path.
- A malformed hand-assembled groups/unmatched entry degrades to a placeholder in CANDIDATES.md rather than crashing step 9.
- WR-04 remains explicitly deferred (documented in this plan's objective) — not silently dropped, not addressed in this pass.
- Full non-integration test suite stays green throughout.
</success_criteria>

<output>
Create `.planning/phases/05-sub-threshold-highlight-compilation/05-05-SUMMARY.md` when done.
</output>
