---
phase: 05-sub-threshold-highlight-compilation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/candidates.py
  - tests/test_candidates.py
autonomous: true
requirements: [COMP-01]
tags: [compilation, candidates, sub-threshold, tagging]

must_haves:
  truths:
    - "A sub-threshold candidate (tightest trim still below config.clip.min_seconds) can carry a free-form tag, sub_threshold flag, group_id, and unmatched flag without any existing single-clip candidate's JSON/markdown output changing shape"
    - "An unmatched sub-threshold candidate is surfaced by name (timecode + reason + tag) in a distinct, clearly-labeled CANDIDATES.md section rather than silently disappearing (D-03)"
  artifacts:
    - "scripts/candidates.py: Candidate dataclass gains tag/sub_threshold/group_id/unmatched optional fields; merge_candidates reads them the same way it already reads coherence"
    - "scripts/candidates.py: append_compilation_sections_markdown(path, groups, unmatched) appends '## Sub-Threshold Compilations' and '## Unmatched Sub-Threshold' sections to an existing CANDIDATES.md"
  key_links:
    - "merge_candidates' new .get() reads are the only way tag/sub_threshold/group_id/unmatched enter a Candidate — Plan 05-04's SKILL.md wiring is what actually sets these values on real candidate dicts before merge/append run"
    - "append_compilation_sections_markdown is a pure read-append-rewrite over the same CANDIDATES.md path render_candidates_markdown already wrote in step 3/4 — it must not touch the original numbered candidate list"
---

<objective>
Extend `scripts/candidates.py`'s `Candidate` dataclass and merge logic with four new optional fields (`tag`, `sub_threshold`, `group_id`, `unmatched`) that a later SKILL.md orchestration step (Plan 05-04) will populate once a candidate's step-5 trim is known to be sub-threshold, and add a new `append_compilation_sections_markdown` function that surfaces grouped/unmatched sub-threshold candidates in `CANDIDATES.md` per D-03.

Purpose: gives COMP-01 ("candidates shorter than min_seconds are tagged instead of discarded") a concrete, testable data shape and a review-visible surface, without touching how normal (non-sub-threshold) candidates are merged/rendered today.
Output: extended `scripts/candidates.py`, extended `tests/test_candidates.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-CONTEXT.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-RESEARCH.md
@.planning/phases/05-sub-threshold-highlight-compilation/05-PATTERNS.md
@scripts/candidates.py
@tests/test_candidates.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Candidate dataclass with tag/sub_threshold/group_id/unmatched (D-01, D-03)</name>
  <files>scripts/candidates.py, tests/test_candidates.py</files>
  <read_first>scripts/candidates.py:1-51 (Candidate dataclass, merge_candidates) and tests/test_candidates.py:76-100 (test_write_candidates_json_round_trips — this test's expected dict must be updated in this task, see below)</read_first>
  <behavior>
    - Candidate gains four new fields, all optional/defaulted so no existing single-clip candidate changes shape: `tag: str | None = None` (D-01, free-form Claude-assigned gameplay/theme description, set only once a candidate is known sub-threshold), `sub_threshold: bool = False`, `group_id: int | None = None` (set by the grouping pass; None = ungrouped), `unmatched: bool = False` (True only when sub_threshold and no group formed this run, D-03).
    - `merge_candidates` reads `item.get("tag")`, `item.get("sub_threshold", False)`, `item.get("group_id")`, `item.get("unmatched", False)` for each flattened item, in the exact same style as the existing `coherence=item.get("coherence")` line — do not touch the existing sort-by-start / sequential-id-assignment logic.
    - `test_write_candidates_json_round_trips` currently asserts the JSON round-trip equals a dict with only `id`/`start`/`end`/`reason`/`coherence` keys. Since `dataclasses.asdict` now serializes the four new fields too, update that test's expected dict to include `"tag": None, "sub_threshold": False, "group_id": None, "unmatched": False` — this is a required update to an existing test, not a new regression.
    - Add two new tests whose names both contain the literal substring `tag_or_sub_threshold` (binding test-selection filter, 05-VALIDATION.md): one asserting all four new fields survive `merge_candidates` when present in the source dict (e.g. `{"start":10.0,"end":15.0,"reason":"died to boss","tag":"died to same boss","sub_threshold":True,"group_id":2,"unmatched":False}`), one asserting all four default to `None`/`False`/`None`/`False` when the source dict omits them entirely (the normal case for every existing candidate).
  </behavior>
  <action>Edit scripts/candidates.py: add the four fields to the Candidate dataclass immediately after `coherence`, and extend the Candidate(...) construction inside merge_candidates with the four new `.get()` reads, mirroring the existing coherence line exactly. Edit tests/test_candidates.py: update test_write_candidates_json_round_trips's expected dict to include the four new keys with their default values, and add the two new tests named to contain "tag_or_sub_threshold" as described above.</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_candidates.py -k tag_or_sub_threshold -x -q</automated>
  </verify>
  <done>Candidate carries tag/sub_threshold/group_id/unmatched, merge_candidates threads them through from source dicts exactly like coherence, and both the updated round-trip test and the two new tag_or_sub_threshold tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: append_compilation_sections_markdown for CANDIDATES.md surfacing (D-03, COMP-01)</name>
  <files>scripts/candidates.py, tests/test_candidates.py</files>
  <read_first>scripts/candidates.py:20-26 (format_timecode, reuse verbatim), scripts/candidates.py:54-66 (render_candidates_markdown — mirror its guard/lines.append/join shape), 05-RESEARCH.md Open Question #1/#2 (append-in-place recommendation, no second document, no re-approval gate)</read_first>
  <behavior>
    - New function `append_compilation_sections_markdown(path: str, groups: list[dict], unmatched: list[dict]) -> None`. Deliberately dict-based (not `Candidate`-typed) for both parameters so it round-trips trivially through a JSON file when called from SKILL.md via a `python -c` one-liner (mirrors the existing `style_profile.format_naming_examples_block` invocation style already used in SKILL.md step 5) — no dataclass reconstruction needed at the call site.
    - `groups` shape: `[{"members": [{"id": 4}, {"id": 7}, ...], "title": "Boss Rage Compilation"}, ...]` — only `id` and `title` are read (this function does not need full candidate data, just enough to name what got grouped, per D-03's transparency intent and RESEARCH Open Question #2's "no second approval gate, just visibility" recommendation).
    - `unmatched` shape: `[{"start": 12.3, "end": 15.1, "reason": "...", "tag": "..."}, ...]` — one entry per sub-threshold candidate that found no group this run.
    - If both `groups` and `unmatched` are empty, the function is a no-op (does not touch the file at all) — a run with zero sub-threshold candidates should never add empty/clutter sections to CANDIDATES.md.
    - Otherwise: read the existing file at `path`, append two new sections — `## Sub-Threshold Compilations` (one bullet per group: `"- Candidates #{id}, #{id}, ... grouped into compilation: {title}"`, comma-joined member ids in `#N` form) and `## Unmatched Sub-Threshold` (one bullet per unmatched candidate using `format_timecode` for start/end exactly like render_candidates_markdown's existing timecode format, plus its reason and tag: `` "- `HH:MM:SS` - `HH:MM:SS` — {reason} (tag: {tag})" ``) — then write the combined text back to `path`. Omit either section header entirely if that section's own list is empty (a run can have compilations but zero unmatched, or vice versa).
    - Add two new tests whose names both contain the literal substring `compilation_sections` (binding test-selection filter, 05-VALIDATION.md): one building a temp CANDIDATES.md with base content, calling the function with one 2-member group and one unmatched entry, and asserting the appended text contains the member ids, the title, and the unmatched candidate's timecode/reason/tag; one asserting the file is left byte-identical when both `groups` and `unmatched` are empty (no-op).
  </behavior>
  <action>Add append_compilation_sections_markdown to scripts/candidates.py as described, reusing format_timecode. Add the two compilation_sections-named tests to tests/test_candidates.py as described. Do not add a CLI subcommand for this function in this task — SKILL.md (Plan 05-04) will call it via a python -c one-liner, matching the existing style_profile.py precedent, so main()'s current single-command shape stays untouched.</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_candidates.py -k compilation_sections -x -q</automated>
  </verify>
  <done>append_compilation_sections_markdown appends distinct, clearly-labeled group/unmatched sections to an existing CANDIDATES.md (or is a no-op when there's nothing to report), and both compilation_sections tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude-authored tag/title text → CANDIDATES.md | Free-form text (D-01 tags, compilation titles) is written as plain markdown via Path.write_text, never interpolated into a shell command or ffmpeg filter string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | append_compilation_sections_markdown | low | accept | Free-form tag/title text is written as plain markdown text only, never interpolated into a shell command or ffmpeg filter string — same trust level as the existing `reason` field already rendered by render_candidates_markdown; no new injection surface introduced |
</threat_model>

<verification>
- `pytest tests/test_candidates.py -x` green (whole file, includes the updated round-trip test and both new test pairs).
- `pytest tests/test_candidates.py -k tag_or_sub_threshold -x` and `pytest tests/test_candidates.py -k compilation_sections -x` both green (05-VALIDATION.md binding commands).
</verification>

<success_criteria>
- Candidate can carry sub-threshold tagging/grouping metadata without changing the JSON/markdown shape of any existing normal candidate.
- A sub-threshold candidate that ends up unmatched is visibly named in CANDIDATES.md, never silently dropped.
</success_criteria>

<output>
Create `.planning/phases/05-sub-threshold-highlight-compilation/05-01-SUMMARY.md` when done.
</output>
