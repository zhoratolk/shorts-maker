---
phase: 07-profanity-auto-bleep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/config.py
  - tests/test_config.py
  - config.example.yaml
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-03]
must_haves:
  truths:
    - "ProfanityConfig defaults to enabled=False (opt-in, D-04) and exposes wordlist_path, pad_seconds, max_masked_spans_per_clip, and the duck/garble tunables that Plans 07-01 and 07-02 consume"
    - "Out-of-range profanity config values raise ConfigError with a 'profanity.<field> must ..., got <value>' message, matching every other config section"
    - "config.example.yaml has a documented profanity: section that loads cleanly with the feature off"
  artifacts:
    - "scripts/config.py::ProfanityConfig"
    - "config.example.yaml profanity: section"
  key_links:
    - "ProfanityConfig wired into the Config aggregate + load_config _build(...) + _validate, mirroring AudioEnergyConfig/DiarizationConfig exactly"
---

<objective>
Add the `ProfanityConfig` dataclass — the single fail-open, default-off toggle + tunables surface for the whole profanity feature (D-04) — to `scripts/config.py`, wire it into the `Config` aggregate and `load_config`, add its `_validate` invariants, and document a `profanity:` section in `config.example.yaml`.

Purpose: D-04 requires a new `config.yaml` section following the same fail-open, default-off pattern as `diarization`/`audio_energy`. The dataclass holds the values Plan 07-01 (wordlist_path, pad_seconds, max_masked_spans_per_clip) and Plan 07-02 (duck_volume + garble/warble tunables) read, and Plan 07-04 wires into the render CLI — so it touches all three requirements.
Output: extended `scripts/config.py`, `tests/test_config.py`, `config.example.yaml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-profanity-auto-bleep/07-CONTEXT.md
@.planning/phases/07-profanity-auto-bleep/07-RESEARCH.md
@.planning/phases/07-profanity-auto-bleep/07-PATTERNS.md
@scripts/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfanityConfig dataclass + Config wiring + _validate + tests</name>
  <read_first>
    - scripts/config.py (lines 118-129 — `AudioEnergyConfig` dataclass shape to mirror; lines 10-11 — `ConfigError`; lines 221-282 — `Config` aggregate + `_build` + `load_config`; lines 376-390 — `AudioEnergyConfig` validation block as the pattern to copy)
    - .planning/phases/07-profanity-auto-bleep/07-PATTERNS.md ("scripts/config.py" section — copy-this-shape guidance)
    - .planning/phases/07-profanity-auto-bleep/07-RESEARCH.md (Code Examples — the default values: duck_volume 0.12, garble_freq 1800.0, garble_width_octaves 4.0, warble_freq 18.0, warble_depth 0.7, pad_seconds 0.08; Pitfall 5 — max_masked_spans_per_clip default 40)
    - tests/test_config.py (existing DiarizationConfig/AudioEnergyConfig test coverage to mirror)
  </read_first>
  <files>scripts/config.py, tests/test_config.py</files>
  <behavior>
    - ProfanityConfig() defaults: enabled=False (opt-in, D-04), wordlist_path="data/profanity_wordlist.yaml", pad_seconds=0.08, max_masked_spans_per_clip=40, duck_volume=0.12, garble_freq=1800.0, garble_width_octaves=4.0, warble_freq=18.0, warble_depth=0.7
    - load_config on a config with no `profanity:` section yields ProfanityConfig() defaults (feature off)
    - _validate raises ConfigError for: pad_seconds < 0; max_masked_spans_per_clip <= 0; duck_volume not in (0,1) exclusive; garble_freq <= 0; garble_width_octaves <= 0; warble_freq <= 0; warble_depth not in (0,1] — each message in the "profanity.<field> must ..., got <value>" format
    - a fully-valid profanity section loads without error
  </behavior>
  <action>
    Add a `ProfanityConfig` dataclass to scripts/config.py mirroring AudioEnergyConfig exactly: `enabled: bool = False` first field (default-off per D-04, this is a new optional feature not a mandatory gate), then the flat float/int/str tunables above, each with an inline why-comment (matching the project's "comments explain why, not what" convention — e.g. pad_seconds compensates for Whisper word-timestamp drift per 07-RESEARCH Pitfall 2; max_masked_spans_per_clip is the fail-open DoS cap per Pitfall 5). No nested dataclasses. Wire it into the `Config` aggregate (add the field + default_factory), into `load_config` via `_build(ProfanityConfig, data.get("profanity", {}), "profanity")`, and add the validation block inside `_validate` reusing the existing `ConfigError` type and the exact `raise ConfigError(f"profanity.<field> must ..., got {value}")` message format used throughout. Note config validation is hard-fail (raises ConfigError on bad values, like every other section) — the fail-open behavior lives at the wordlist/detection layer (Plan 07-01), not here. Add tests to tests/test_config.py mirroring the DiarizationConfig/AudioEnergyConfig cases: default-off, valid-load, and one raising case per invariant.
  </action>
  <verify>
    <automated>pytest -m "not integration" tests/test_config.py -k "profanity" -x</automated>
  </verify>
  <acceptance_criteria>
    - ProfanityConfig defaults to enabled=False and the documented tunable defaults
    - Every listed invariant raises ConfigError with a profanity.<field> message
    - Missing `profanity:` section resolves to defaults (feature off)
    - Full `pytest -m "not integration" tests/test_config.py -x` green (no regression)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: document the profanity: section in config.example.yaml</name>
  <read_first>
    - config.example.yaml (lines around 57-129 — the `diarization:`/`audio_energy:` sections as the documented-section format to mirror)
    - .planning/phases/07-profanity-auto-bleep/07-CONTEXT.md (D-04 — fail-open, default-off framing)
  </read_first>
  <files>config.example.yaml</files>
  <action>
    Add a `profanity:` section to config.example.yaml documenting all nine ProfanityConfig fields with the same commented, default-off style as the neighboring `diarization:`/`audio_energy:` sections. Lead with `enabled: false` and a one-line comment that this is opt-in and fail-open (missing/malformed config or wordlist degrades to no masking, never fails the pipeline — D-04). Keep each numeric tunable's default matching the dataclass and add a short why-comment (duck depth, garble center frequency/width, warble rate/depth, pad seconds, span cap, wordlist path).
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'.'); from scripts.config import load_config; c=load_config('config.example.yaml'); assert c.profanity.enabled is False; print('profanity section loads, enabled=False')"</automated>
  </verify>
  <acceptance_criteria>
    - config.example.yaml has a documented `profanity:` section, default off
    - The example config loads via load_config with profanity.enabled False
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
This plan creates/extends:
- `scripts/config.py::ProfanityConfig` — new dataclass (enabled, wordlist_path, pad_seconds, max_masked_spans_per_clip, duck_volume, garble_freq, garble_width_octaves, warble_freq, warble_depth)
- `scripts/config.py::Config` — new `profanity` field; `load_config` `_build(...)` wiring; `_validate` invariants
- `config.example.yaml` — new `profanity:` documented section
- new tests in `tests/test_config.py`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml `profanity:` values → runtime tunables | operator-supplied config crosses into detection/render parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering / Input Validation | `_validate` (ProfanityConfig block) | low | mitigate | Each tunable range-checked with a `ConfigError` (pad >= 0, cap > 0, duck in (0,1), garble/warble > 0, depth in (0,1]) — hard-fail on bad config, matching every other section; prevents a nonsensical value reaching the ffmpeg filter builder |
| T-07-SC | Tampering | package installs | n/a | accept | No new packages this phase (07-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest -m "not integration" tests/test_config.py -x` all green
- `load_config('config.example.yaml')` succeeds with profanity.enabled False
</verification>

<success_criteria>
- The whole feature is gated behind a single opt-in, fail-open, range-validated config section documented in config.example.yaml — D-04 satisfied; tunables ready for Plans 07-01/07-02/07-04 to consume.
</success_criteria>

<output>
Create `.planning/phases/07-profanity-auto-bleep/07-03-SUMMARY.md` when done.
</output>
