---
phase: 07-profanity-auto-bleep
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - tests/test_integration_ffmpeg.py
  - .claude/skills/make-shorts/SKILL.md
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-03]
must_haves:
  truths:
    - "A real ffmpeg render of a clip with profanity_spans has measurably lower loudness INSIDE the span than outside (AUDIO-02, volumedetect)"
    - "A masked spoken word, re-transcribed by this project's own faster-whisper, no longer cleanly yields the original word (AUDIO-03 automated proxy)"
    - "SKILL.md step 5/5b produces profanity_spans in PLAN.json when config.profanity.enabled, reusing the existing clip-relative _words.json, and fails open (omits the field) on any detection error (AUDIO-01, D-04)"
  artifacts:
    - "tests/test_integration_ffmpeg.py profanity loudness-delta + self-transcription tests"
    - ".claude/skills/make-shorts/SKILL.md profanity orchestration bullets + profanity_spans schema + step-6 CLI flags"
  key_links:
    - "SKILL profanity detection runs on the SAME clip-relative _words.json subtitles already build (jumpcuts.remap_words reuse) — jump-cut-dropped words are absent for free"
    - "detection must NOT depend on subtitles being enabled; when subtitles are off, SKILL builds its own remapped words file first (D-04 independent toggles)"
---

<objective>
Prove the masking end-to-end against the real ffmpeg binary and wire the orchestrator. Two `integration`-marked tests in `tests/test_integration_ffmpeg.py` (a `volumedetect` loudness-delta for AUDIO-02 and a faster-whisper self-transcription check for AUDIO-03), plus the `SKILL.md` step-5/step-5b bullets that actually produce `profanity_spans` in `PLAN.json` and the step-6 render flags.

Purpose: closes the AUDIO-01 orchestration gap (per-clip detection wired into the pipeline) and provides the strongest available automated proxies for AUDIO-02 ("audio keeps playing, measurably ducked") and AUDIO-03 ("defeats STT"). The `.claude/` directory is gitignored project-wide, so the SKILL.md edit lives on disk only, not in git history — the established convention (Plans 02-01/04-06/05-04), not a regression.
Output: `tests/test_integration_ffmpeg.py` additions, `.claude/skills/make-shorts/SKILL.md` edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-profanity-auto-bleep/07-CONTEXT.md
@.planning/phases/07-profanity-auto-bleep/07-RESEARCH.md
@.planning/phases/07-profanity-auto-bleep/07-PATTERNS.md
@tests/test_integration_ffmpeg.py
@.claude/skills/make-shorts/SKILL.md
@.planning/phases/07-profanity-auto-bleep/07-01-SUMMARY.md
@.planning/phases/07-profanity-auto-bleep/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: real-ffmpeg loudness-delta integration test (AUDIO-02)</name>
  <read_first>
    - tests/test_integration_ffmpeg.py (module docstring + existing `integration`-marked self-skip pattern for missing ffmpeg/ffprobe)
    - .planning/phases/07-profanity-auto-bleep/07-RESEARCH.md (Architecture Patterns > Pattern 1 — the live-verified filter clause + the volumedetect technique: baseline -21.1dB, ducked window -33.3dB, outside window unchanged; Validation Architecture > Phase Requirements → Test Map, AUDIO-02 integration row)
    - .planning/phases/07-profanity-auto-bleep/07-02-SUMMARY.md (final build_profanity_mask_filter signature / render_clip wiring as shipped)
  </read_first>
  <files>tests/test_integration_ffmpeg.py</files>
  <action>
    Add an `integration`-marked test that renders a short clip carrying `profanity_spans` through the real ffmpeg binary (via render_clip / build_ffmpeg_command as shipped in Plan 07-02, or a synthetic `-f lavfi` sine input using build_profanity_mask_filter's output), then measures loudness inside vs. outside a masked span with ffmpeg `volumedetect` (isolate each window with `atrim=start=X:end=Y,volumedetect`, the exact technique live-verified in 07-RESEARCH Pattern 1). Assert the mean_volume inside the span is measurably lower than outside (AUDIO-02: audio ducked, not silenced — the span is quieter but non-empty). Self-skip when ffmpeg/ffprobe are absent, matching the file's existing convention. If any direct ffmpeg invocation is needed for the volumedetect measurement, use the project's injectable `runner=subprocess.run` shape rather than an inline subprocess call.
  </action>
  <verify>
    <automated>pytest tests/test_integration_ffmpeg.py -k "profanity and not defeats_transcription" -m integration -x</automated>
  </verify>
  <acceptance_criteria>
    - A real-ffmpeg render/measure shows lower loudness inside the masked span than outside
    - Test self-skips cleanly when ffmpeg is unavailable
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: faster-whisper self-transcription STT-defeat test (AUDIO-03 proxy)</name>
  <read_first>
    - scripts/transcribe.py (transcribe_video / word_timestamps interface — how this project loads and runs faster-whisper)
    - .planning/phases/07-profanity-auto-bleep/07-RESEARCH.md (Validation Architecture > Phase Requirements → Test Map, AUDIO-03 row + Sampling Rate "phase gate, not every wave merge"; Assumptions Log A3 — this is a real automatable proxy, not a guarantee of any specific platform's moderation model)
  </read_first>
  <files>tests/test_integration_ffmpeg.py</files>
  <action>
    Add an `integration`-marked (slow) test that renders a clip in which a known spoken word is masked, re-transcribes the masked output through this project's own faster-whisper model, and asserts the masked word no longer appears cleanly in the re-transcription (AUDIO-03's strongest available automated proxy for "a platform STT pass wouldn't transcribe it cleanly" — per A3, document in a comment that this validates against this project's own model, not any external platform). Gate it behind the `integration` marker so it self-skips without ffmpeg and is run at the phase gate rather than every wave merge (it loads a real Whisper model). Keep the fixture audio tiny to bound model-load cost.
  </action>
  <verify>
    <automated>pytest tests/test_integration_ffmpeg.py -k "profanity_defeats_transcription" -m integration -x</automated>
  </verify>
  <acceptance_criteria>
    - A masked word fails to re-transcribe cleanly to the original via faster-whisper
    - Test is integration-gated and self-skips when the model/ffmpeg are unavailable
    - A comment documents this as an own-model proxy, not a platform guarantee (A3)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SKILL.md orchestration — produce profanity_spans in PLAN.json (fail-open)</name>
  <read_first>
    - .claude/skills/make-shorts/SKILL.md (lines 150-167 — step-5 subtitle word-collection + `remap-words` bullet; lines 224-234 — step-5b compilation combined `_words.json` bullet; lines 242-274 — PLAN.json single-clip + compilation schema blocks; lines 276-284 — step-6 render invocation)
    - .planning/phases/07-profanity-auto-bleep/07-RESEARCH.md (Architecture Patterns > Pattern 2 — reuse the same clip-relative `_words.json`; Common Pitfalls 3 + Assumptions Log A4 — the subtitles-recall-gap limitation to document, not hide; Open Questions 1 — ship independent per D-04)
    - .planning/phases/07-profanity-auto-bleep/07-01-SUMMARY.md (final scripts/profanity.py CLI shape) and 07-02-SUMMARY.md (final render.py --profanity-* flag names)
  </read_first>
  <files>.claude/skills/make-shorts/SKILL.md</files>
  <action>
    Add a step-5 "Profanity auto-bleep" bullet that runs ONLY when `config.profanity.enabled` is true: detect profane spans by calling `scripts/profanity.py` against the clip's clip-relative `_words.json` (reuse the very file the subtitles bullet already produces via `jumpcuts.py remap-words` when `config.subtitles.enabled`; when subtitles are OFF, build the same clip-relative words file first via the identical collect+remap-words step — profanity masking must NOT depend on subtitles being enabled, D-04 independent toggles / Pattern 2), passing `--wordlist config.profanity.wordlist_path --pad-seconds config.profanity.pad_seconds --max-spans config.profanity.max_masked_spans_per_clip --clip-duration <clip length>`; record the printed spans verbatim as `profanity_spans` in the plan entry (omit the field entirely when empty/disabled, matching the existing optional-field convention). Fail open exactly like the diarization/transitions bullets: on any error, tell the user why in one line and omit `profanity_spans` — never block rendering. Add the parallel step-5b bullet for compilations: compute `profanity_spans` ONCE against the compilation's combined flattened `_words.json` (the same file bullet 7 already builds) as a TOP-LEVEL field on the compilation plan entry (sibling to `boundary_transitions`), never per-member (Pattern 2). Document `profanity_spans` in BOTH PLAN.json schema blocks (single-clip and compilation) as an optional `[[start, end], ...]` list omitted when empty/disabled. In step 6, add the five `--profanity-*` render flags (duck-volume/garble-freq/garble-width-octaves/warble-freq/warble-depth from `config.profanity`) to the documented `render.py` invocation. Add a short explicit note (not buried) documenting the known recall limitation: when `config.subtitles.enabled`, detection benefits from the word-correction proofreading pass; when subtitles are off, detection runs on raw Whisper words and may under-detect Whisper-garbled profanity (Pitfall 3 / A4) — disclosed, shipped independent per D-04.
  </action>
  <verify>
    <automated>python -c "p=open('.claude/skills/make-shorts/SKILL.md',encoding='utf-8').read(); assert 'profanity_spans' in p and 'scripts/profanity.py' in p and '--profanity-duck-volume' in p, 'SKILL profanity wiring missing'; print('SKILL profanity orchestration present')"</automated>
  </verify>
  <acceptance_criteria>
    - Step 5 + step 5b bullets produce profanity_spans (single-clip + compilation), gated on config.profanity.enabled, fail-open
    - Detection reuses the existing clip-relative _words.json and does not require subtitles enabled
    - profanity_spans documented in both PLAN.json schema blocks; step-6 gains the five --profanity-* flags
    - The subtitles-recall-gap limitation is documented explicitly
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
This plan creates/extends:
- `tests/test_integration_ffmpeg.py` — profanity loudness-delta test (AUDIO-02) + faster-whisper self-transcription test (AUDIO-03), both `integration`-marked
- `.claude/skills/make-shorts/SKILL.md` (disk-only, gitignored) — step-5 + step-5b profanity-detection bullets producing `profanity_spans`, both PLAN.json schema blocks documenting the field, step-6 `--profanity-*` flags, and the recall-gap limitation note
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profanity.py CLI / wordlist → PLAN.json | detection output crosses into the render plan; a detection error must not block the pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Denial of Service (pipeline abort) | SKILL.md profanity orchestration | low | mitigate | Fail-open bullet: any profanity-detection error → warn one line, omit `profanity_spans`, continue rendering — mirrors the diarization/transitions fail-open steps (D-04) |
| T-07-08 | Information Disclosure (under-detection) | SKILL.md recall-gap | low | accept | Whisper mis-transcribes exactly the targeted words (Pitfall 3); documented explicitly as a disclosed limitation, shipped independent per D-04 (Open Question 1) rather than silently coupling subtitles |
| T-07-SC | Tampering | package installs | n/a | accept | No new packages — reuses existing faster-whisper + ffmpeg (07-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest tests/test_integration_ffmpeg.py -k profanity -m integration -x` green (or self-skips cleanly if ffmpeg/whisper unavailable on the runner)
- SKILL.md contains the profanity bullets, both schema-block field mentions, and the step-6 flags (grep check)
- `pytest tests/ -x` full suite green
</verification>

<success_criteria>
- Real-ffmpeg proof that masked spans are ducked (AUDIO-02) and defeat this project's own STT (AUDIO-03), and the orchestrator produces `profanity_spans` per clip and per compilation, fail-open, reusing the existing clip-relative word plumbing (AUDIO-01) — the feature works end-to-end.
</success_criteria>

<output>
Create `.planning/phases/07-profanity-auto-bleep/07-04-SUMMARY.md` when done.
</output>
