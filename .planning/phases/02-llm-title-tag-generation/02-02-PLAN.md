---
phase: 02-llm-title-tag-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - TAGS-01
  - TAGS-02
user_setup: []

must_haves:
  truths:
    - "A future reader of REQUIREMENTS.md/ROADMAP.md can see that TAGS-01 is satisfied via the existing orchestrator session (no separate Claude API call/script) — not left ambiguous against the original literal wording."
    - "A future reader can see that TAGS-02 (Ollama fallback) is intentionally deferred to Phase 6 (headless runner), not silently dropped or silently marked done — the pending status is explained as deliberate, not an oversight."
    - "The architectural reinterpretation of TAGS-01/TAGS-02 is confirmed by an explicit human sign-off (blocking checkpoint) before the docs are edited, since it changes how a shipped-milestone requirement is interpreted."
  artifacts:
    - ".planning/REQUIREMENTS.md — TAGS-01 annotated as reframed (orchestrator-session, no separate API), TAGS-02 annotated as deferred-to-Phase-6 in both the requirement line and the Traceability table"
    - ".planning/ROADMAP.md — Phase 2 detail block carries a reconciliation note matching REQUIREMENTS.md so ROADMAP's literal success-criteria wording is not read as un-met"
  key_links:
    - "REQUIREMENTS.md Traceability table row for TAGS-02 links to the CONTEXT.md D-03 rationale and to Phase 6 as the destination — so gsd-audit-uat / gsd-verify-work read a deliberate deferral, not a gap"
    - "02-CONTEXT.md D-01/D-02/D-03 is the authoritative rationale both edits cite — no new decision is invented here, only recorded into the durable traceability docs"
---

<objective>
Close the requirement-traceability reconciliation the research flagged as the phase's real risk (not code): the literal TAGS-01/TAGS-02 wording in REQUIREMENTS.md/ROADMAP.md predates the discuss-phase session that rejected the separate-API-call + Ollama-fallback architecture. This plan records, in the durable traceability docs, that TAGS-01 is satisfied via the existing orchestrator (reframed, D-01/D-02) and TAGS-02 is deferred to Phase 6 (D-03) — with an explicit human sign-off, since it reinterprets how a shipped requirement is read.

Purpose: Prevent a future audit (`gsd-audit-uat`, `gsd-verify-work`) from either assuming a nonexistent second API call exists (Pitfall 1) or treating TAGS-02 as a silently-dropped gap (Pitfall 2).
Output: Annotated REQUIREMENTS.md + ROADMAP.md carrying the reframe/defer notes with a paper trail to CONTEXT.md D-01/D-02/D-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-llm-title-tag-generation/02-CONTEXT.md
@.planning/phases/02-llm-title-tag-generation/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm the TAGS-01 reframe and TAGS-02 deferral before editing durable docs</name>
  <what-built>Nothing yet — this checkpoint gates the traceability edit BEFORE it happens, because reinterpreting a shipped-milestone requirement (TAGS-01 "via the Claude API" → "via the existing orchestrator session"; TAGS-02 "Ollama fallback" → "deferred to Phase 6") is a project-level decision that warrants explicit confirmation, per RESEARCH.md's recommendation ("Update REQUIREMENTS.md traceability notes... with explicit human sign-off since this changes how a shipped requirement is interpreted").</what-built>
  <how-to-verify>
    Confirm the reframing is what you intend:
    1. TAGS-01 will be marked satisfied-this-phase via the orchestrator session itself acting as the generator — NO separate Claude API script, NO `anthropic` SDK, NO new billing path (locked by 02-CONTEXT.md D-01/D-02).
    2. TAGS-02 (Ollama fallback) will be marked DEFERRED to Phase 6 (scheduled auto-publish / headless runner) — NOT implemented in Phase 2, because there is no "Claude API unavailable" state to fall back from when the orchestrator itself is the generator (02-CONTEXT.md D-03).
    3. These notes will be written into REQUIREMENTS.md (requirement lines + Traceability table) and ROADMAP.md (Phase 2 detail block).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the traceability edits, or describe a different reframing.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Annotate TAGS-01 (reframed) and TAGS-02 (deferred) in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md lines 22-25 (the TAGS-01/02/03 requirement lines) and lines 80-83 + 89-96 (the Traceability table rows for TAGS-01/02/03)
    - .planning/phases/02-llm-title-tag-generation/02-CONTEXT.md lines 13-20 (D-01/D-02/D-03 — the authoritative rationale) and lines 80-89 (Deferred Ideas)
    - .planning/phases/02-llm-title-tag-generation/02-RESEARCH.md lines 213-225 (Pitfall 1 + Pitfall 2 — exactly what the annotation must prevent)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` (scoped Edit, do not rewrite the whole file):
    (1) On the TAGS-01 requirement line, append a parenthetical note that it is satisfied in Phase 2 via the existing Claude Code orchestrator session acting as the generator — no separate Claude API script/SDK/billing path — per 02-CONTEXT.md D-01/D-02. Do NOT change the requirement's checkbox to done here (the phase-transition/verify step owns final status); this is a clarifying annotation of *how* it's met.
    (2) On the TAGS-02 requirement line, append a note that the Ollama fallback is DEFERRED to Phase 6 (headless/non-interactive runner) per 02-CONTEXT.md D-03 — it is not applicable to Phase 2's architecture because there is no separate API call to fail over from when the orchestrator is the generator.
    (3) In the Traceability table, change the TAGS-02 row so its Status reads a deliberate-deferral phrase (e.g. "Deferred to Phase 6 (D-03)") instead of a bare "Pending" that a reader could mistake for an oversight, and add/adjust a note for TAGS-01 that it is reframed (orchestrator-session). If the table has no notes column, put the deferral marker inline in the Status cell and add a one-line footnote beneath the table pointing to 02-CONTEXT.md D-03.
    Keep all other rows and requirements untouched. Use no rejected-architecture language as if it were being built (no "will call the Anthropic SDK", no "Ollama fallback implemented").
  </action>
  <verify>
    <automated>cd /d/shorts-maker && grep -qiE 'D-0[123]|orchestrator|deferred to phase 6|Phase 6' .planning/REQUIREMENTS.md && grep -qi 'TAGS-02' .planning/REQUIREMENTS.md && echo REQS_OK</automated>
  </verify>
  <acceptance_criteria>
    - REQS_OK prints: REQUIREMENTS.md references the D-0x rationale / orchestrator reframe / Phase 6 deferral.
    - The TAGS-01 line explains it is met via the orchestrator session (no separate API script), citing D-01/D-02.
    - The TAGS-02 Traceability status reads as a deliberate deferral to Phase 6, not a bare "Pending", with a pointer to D-03.
    - No requirement other than TAGS-01/TAGS-02 changed; TAGS-03 and all non-TAGS rows are byte-identical to before.
  </acceptance_criteria>
  <done>REQUIREMENTS.md carries the TAGS-01 reframe and TAGS-02 deferral notes with a paper trail to 02-CONTEXT.md D-01/D-02/D-03, in both the requirement lines and the Traceability table.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the reconciliation note into ROADMAP.md Phase 2 detail</name>
  <read_first>
    - .planning/ROADMAP.md lines 48-60 (the Phase 2 detail block: Goal, Depends on, Requirements, Success Criteria)
    - .planning/REQUIREMENTS.md (the annotations Task 1 just wrote — keep the ROADMAP note consistent with them)
    - .planning/phases/02-llm-title-tag-generation/02-CONTEXT.md lines 13-20 (D-01/D-02/D-03)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` (scoped Edit, do not rewrite the whole file). In the Phase 2 detail block (starting "### Phase 2: LLM Title/Tag Generation", around line 48), add a short reconciliation note — one or two lines directly under the Requirements line or beneath the Success Criteria — stating that: Success Criterion 1's "via the Claude API" is satisfied by the existing Claude Code orchestrator session (no separate API script, per 02-CONTEXT.md D-01/D-02); and Success Criterion 2's Ollama fallback (TAGS-02) is deferred to Phase 6 (headless runner), not delivered in Phase 2, per D-03. This keeps the ROADMAP's literal success-criteria wording from being read as un-met after Phase 2 closes. Do not delete or renumber the existing Success Criteria; only append the clarifying note. Leave every other phase's detail block untouched.
  </action>
  <verify>
    <automated>cd /d/shorts-maker && grep -qiE 'orchestrator|D-0[123]|deferred' .planning/ROADMAP.md && grep -qi 'Phase 6' .planning/ROADMAP.md && echo ROADMAP_OK</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP_OK prints: the Phase 2 detail block references the orchestrator reframe / D-0x / a Phase 6 deferral.
    - The note is consistent with the REQUIREMENTS.md annotations from Task 1 (same reframe for TAGS-01, same deferral target Phase 6 for TAGS-02).
    - The existing Phase 2 Success Criteria are not deleted or renumbered — only a clarifying note is appended.
    - No other phase's detail block changed.
  </acceptance_criteria>
  <done>ROADMAP.md Phase 2 detail carries a reconciliation note aligning its literal success-criteria wording with the locked orchestrator-session architecture (TAGS-01) and the Phase 6 deferral (TAGS-02), consistent with REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → future audits | REQUIREMENTS.md/ROADMAP.md are read by later audit steps (gsd-audit-uat, gsd-verify-work) and human reviewers; an ambiguous status here becomes a false gap or a false "done" downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-DOC1 | Repudiation | Requirement status ambiguity — a future reader cannot tell whether TAGS-02 was forgotten, descoped, or done (Pitfall 2), or assumes a nonexistent API call for TAGS-01 (Pitfall 1) | medium | mitigate | Explicit annotations in both the requirement line and the Traceability table, each citing 02-CONTEXT.md D-01/D-02/D-03; a blocking human sign-off checkpoint before the edit records deliberate intent. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A — zero new dependencies). Nothing to audit. |

*ASVS level 1; block-on-high. V2/V3/V4/V5/V6 are N/A — this is a documentation-only plan with no code, input, auth, or crypto surface (RESEARCH.md Security Domain).*
</threat_model>

<verification>
- REQS_OK (Task 1) and ROADMAP_OK (Task 2) gates both pass.
- Manual: read the TAGS-01/TAGS-02 rows in REQUIREMENTS.md's Traceability table and the ROADMAP Phase 2 note — a first-time reader can tell TAGS-01 is orchestrator-satisfied and TAGS-02 is deliberately deferred to Phase 6, without cross-referencing CONTEXT.md.
- `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows only the intended requirement rows / Phase 2 block changed.
</verification>

<success_criteria>
- TAGS-01: recorded as satisfied via the existing orchestrator session (no separate API call), with a D-01/D-02 paper trail — Pitfall 1 avoided.
- TAGS-02: recorded as a deliberate deferral to Phase 6 (not a bare pending, not silently dropped), with a D-03 paper trail — Pitfall 2 avoided.
- The reinterpretation was human-confirmed (blocking checkpoint) before the durable docs were edited.
</success_criteria>

<output>
Create `.planning/phases/02-llm-title-tag-generation/02-02-SUMMARY.md` when done.
</output>
