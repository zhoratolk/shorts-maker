---
phase: 08-hook-title-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/render.py
  - tests/test_render.py
autonomous: true
requirements: [HOOK-01, HOOK-02, HOOK-03]
must_haves:
  truths:
    - "build_hook_banner_filter returns a chained-drawtext filter string (one clause per wrapped line + optional CTA line), fontfile= explicit paths only, expansion=none on every clause — and returns None for empty/whitespace text so the caller omits the filter entirely (HOOK-01, HOOK-03)"
    - "hook mode gates every clause with enable='between(t,0,dur)' + an alpha fade-out (fade_seconds=0 = hard cut, enable only); persistent mode (the ROADMAP 2026-07-12 locked default) has no enable/alpha and shows for the whole clip with the optional CTA line under the title (HOOK-01)"
    - "The banner clause is inserted AFTER punch-zoom/effects and BEFORE subtitles in all three command builders, and a plan entry without banner_text builds a command byte-identical to today across plain/jumpcut/compilation branches (HOOK-02 ordering, HOOK-03)"
    - "render_clip raises RenderError when banner_text is set, subtitles are being burned in, and banner position equals the subtitle position — fail-loud collision guard, mirroring the punch_zoom_at+crop_style check (HOOK-02, Pitfall 3)"
  artifacts:
    - "scripts/render.py::build_hook_banner_filter"
    - "scripts/render.py::_escape_drawtext_text + _wrap_banner_lines + HOOK_BANNER_FONT_PATHS"
    - "render.py --banner-* CLI flags"
  key_links:
    - "banner_text from PLAN.json is escaped (backslash, quote, colon, comma — backslash FIRST) before entering text=, and expansion=none closes the % strftime-expansion class — no raw external text corrupts the filter graph (ASVS V5)"
    - "Multi-line = chained drawtext clauses, one per line — a literal \\n inside text= renders as a stray lowercase n when built as an argv list (live-verified, 08-RESEARCH Pitfall 2), never rely on it"
    - "fontfile= explicit path always — font=<name> fails outright on this Windows ffmpeg build (fontconfig has no config file, 08-RESEARCH Pitfall 1)"
---

<objective>
Add the codebase's first static on-frame text feature: a new pure builder `build_hook_banner_filter` in `scripts/render.py` that turns a pre-sanitized `banner_text` into chained `drawtext` clauses (title line(s) + optional CTA/nick line), threaded through all three ffmpeg command builders after punch-zoom and before subtitles, read from `plan_entry["banner_text"]` in `render_clip`, and exposed as `--banner-*` CLI flags.

Purpose: HOOK-01's render mechanics — "a legible text banner with the hook, hook mode first ~2-3s then gone, persistent mode (default) whole clip with optional CTA line" — plus HOOK-02's placement/ordering guarantees and HOOK-03's render-side fail-open (no banner_text → byte-identical command). All drawtext mechanics (fontfile requirement, escaping, chained multi-line, enable/alpha timeline) were live-verified against this machine's ffmpeg 8.1.2 in 08-RESEARCH.md — implementation-ready, not speculative.
Output: extended `scripts/render.py` + `tests/test_render.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hook-title-overlay/08-RESEARCH.md
@scripts/render.py
@tests/test_render.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: font resolution, drawtext escaping, and greedy line-wrap helpers</name>
  <read_first>
    - scripts/render.py (lines 12-24 — NAMED_ASS_COLORS/SUBTITLE_MARGIN_V constant shapes to mirror; lines 48-58 — ass_color's RenderError-on-unknown-name shape; lines 80-90 — _escape_ass_text's backslash-first ordering precedent; line 468 — the existing subtitles-path colon-escape)
    - .planning/phases/08-hook-title-overlay/08-RESEARCH.md (Pattern 1 — live-verified escaping order + expansion=none; Pitfall 1 — fontfile mandatory; Placement Math — measured ~22-23 chars/line at fontsize=58; Pitfall 4 — overflow handling)
  </read_first>
  <files>scripts/render.py, tests/test_render.py</files>
  <behavior>
    - HOOK_BANNER_FONT_PATHS maps "Arial Black" -> "C:/Windows/Fonts/ariblk.ttf" and "Arial Bold" -> "C:/Windows/Fonts/arialbd.ttf" (module constant next to NAMED_ASS_COLORS, with a non-Windows fallback comment mirroring config.example.yaml's subtitles.font comment)
    - resolve_banner_font("Arial Black") -> the mapped path; resolve_banner_font("C:/Fonts/custom.ttf") or any value containing a path separator or ending in .ttf/.otf -> returned verbatim (forward-slash normalized) — the non-Windows escape hatch; an unknown bare name raises RenderError listing the known names (mirrors ass_color)
    - _escape_drawtext_text applies, in this exact order: backslash -> \\, single quote -> \', colon -> \:, comma -> \, (backslash FIRST, mirroring _escape_ass_text — a later rule must never re-escape an earlier rule's output)
    - _wrap_banner_lines("МУЖИКИ, ВЫ ЧЁ ТВОРИТЕ, ПАРНИ?", max_chars=22, max_lines=2) greedy-word-wraps into <=2 lines of <=22 chars; a single word longer than max_chars stays alone on its own line; text needing more than max_lines lines is truncated to max_lines with a trailing ellipsis on the last line plus a "[warn]" line to stderr (fail-open, never raises — Pitfall 4)
  </behavior>
  <action>
    Add to scripts/render.py: a module-level `HOOK_BANNER_FONT_PATHS` dict constant (with a comment noting ariblk.ttf/arialbd.ttf are Windows-shipped with live-verified full Cyrillic coverage, and that non-Windows users pass an explicit font-file path instead — same spirit as config.example.yaml's subtitles.font comment), `resolve_banner_font(font: str) -> str` (verbatim-path passthrough when the value contains "/" or "\\" or endswith .ttf/.otf, else dict lookup, else RenderError naming the valid keys — never fall back to font=<name>, which fails on this build per Pitfall 1), `_escape_drawtext_text(text: str) -> str` (the four replacements in backslash-first order per 08-RESEARCH Pattern 1 — % is deliberately NOT escaped; expansion=none in Task 2 neutralizes it), and `_wrap_banner_lines(text: str, max_chars: int, max_lines: int) -> list[str]` (greedy word wrap; overflow beyond max_lines truncates with "…" + a `[warn] banner text truncated...` stderr line, matching the project's [warn]-prefix convention). Place these immediately above where build_hook_banner_filter will land in Task 2 (before build_audio_filter_chain, mirroring the Phase 07-02 placement decision: helpers next to their single call site). Add covering tests to tests/test_render.py mirroring the existing pure-helper test style: escaping order (a text containing a backslash AND a colon), path-passthrough vs map lookup vs RenderError, wrap cases (fits one line / wraps to two / truncates with ellipsis + no raise / long single word).
  </action>
  <verify>
    <automated>pytest -m "not integration" tests/test_render.py -k "banner_font or escape_drawtext or wrap_banner" -x</automated>
  </verify>
  <acceptance_criteria>
    - Escaping is backslash-first and covers backslash/quote/colon/comma
    - Font resolution never emits a bare font name; unknown names raise RenderError, explicit paths pass through
    - Wrap truncates fail-open (warn + ellipsis), never raises on long text
  </acceptance_criteria>
  <commit>feat(08-01): add banner font-resolution, drawtext-escaping, and line-wrap helpers to render.py</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_hook_banner_filter — chained drawtext, both modes, CTA line (pure builder)</name>
  <read_first>
    - scripts/render.py (lines 247-274 — build_punch_zoom_filter's validate-then-build shape; lines 316-352 — build_profanity_mask_filter's enable-expression + None-for-empty precedent)
    - .planning/phases/08-hook-title-overlay/08-RESEARCH.md (Code Examples > "render.py addition" — the drafted builder body to extend with wrapping/position; Pattern 1 — the live-verified two-line clause strings; Placement Math — y=140 top anchor, bottom-zone figures, chars-per-line scaling)
    - tests/test_render.py (lines 375-412 — build_profanity_mask_filter exact-string + RenderError-raises test style to mirror)
  </read_first>
  <files>scripts/render.py, tests/test_render.py</files>
  <behavior>
    - build_hook_banner_filter("", mode="persistent") and build_hook_banner_filter("   ", ...) return None (fail-open, HOOK-03 — caller omits the filter entirely)
    - build_hook_banner_filter("МУЖИКИ, ВЫ ЧЁ ТВОРИТЕ?", mode="persistent", cta_text="@channel_nick") returns exactly two comma-joined drawtext clauses: title clause (fontfile='C\:/Windows/Fonts/ariblk.ttf', escaped text, fontsize=58, fontcolor=white, expansion=none, x=(w-text_w)/2, y=140, box=1, boxcolor=black@0.55, boxborderw=24) then CTA clause (arialbd.ttf, fontsize=36, fontcolor=0xffe98a, y=218, boxborderw=16) — Python-computed integer y values, no ffmpeg arithmetic expressions
    - mode="hook" appends :enable='between(t,0,3.0)' and :alpha='if(lt(t,2.6),1,max(0,1-(t-2.6)/0.4))' to EVERY clause (duration_seconds=3.0, fade_seconds=0.4); fade_seconds=0 appends enable only (hard cut, no division-by-zero alpha)
    - a title longer than the chars-per-line budget produces one drawtext clause PER wrapped line, each at y = y_top + i*(size + line_gap), CTA below the LAST title line
    - RenderError on: mode not in {hook, persistent}; position not in {top, bottom}; size <= 0; box_opacity outside [0, 1]; hook mode with duration_seconds <= 0, fade_seconds < 0, or fade_seconds >= duration_seconds
  </behavior>
  <action>
    Add `build_hook_banner_filter(text, mode, cta_text="", font="Arial Black", cta_font="Arial Bold", size=58, cta_size=36, color="white", cta_color="#ffe98a", box_color="black", box_opacity=0.55, position="top", duration_seconds=3.0, fade_seconds=0.4, max_lines=2, line_gap=20) -> str | None` to scripts/render.py, following build_punch_zoom_filter's validate-then-build shape (reuse RenderError, no new exception type). Body: return None for empty/whitespace text; validate per the behavior list; resolve fonts via resolve_banner_font and escape the resolved path's colons (backslash->/ then : -> \:, same as the line-468 subtitles-path escape); normalize a leading "#" in color values to "0x" (drawtext-safe, matches the live-verified 0xffe98a form); wrap the title via _wrap_banner_lines with max_chars scaled from the measured 22-chars-at-58 ratio (max_chars = max(8, round(22 * 58 / size)) — mechanical scaling of the live measurement, A1); build one drawtext clause per title line with expansion=none and Python-computed y (top: y0=140, line i at y0 + i*(size+line_gap); bottom: y0 = 1920 - 480 - block_height where block_height counts all title lines plus the CTA line when present — keeps the block above the ~380-480px bottom platform-UI zone, Placement Math); append the CTA clause (cta_font/cta_size/cta_color, boxborderw=16) below the last title line when cta_text is non-empty — CTA text goes through the SAME _escape_drawtext_text; in hook mode append the shared enable expression and (when fade_seconds > 0) the alpha fade-out expression to every clause per the drafted 08-RESEARCH body; persistent mode appends nothing (the ROADMAP 2026-07-12 locked default). Join clauses with ",". Add exact-string tests to tests/test_render.py mirroring test_build_profanity_mask_filter_two_spans_shape (persistent+CTA two-clause string, hook-mode enable/alpha string, hard-cut fade_seconds=0, multi-line wrap producing 2 title clauses, escaping of a title containing a comma and an apostrophe) plus the RenderError-raises battery mirroring test_build_punch_zoom_filter_rejects_*.
  </action>
  <verify>
    <automated>pytest -m "not integration" tests/test_render.py -k "hook_banner_filter" -x</automated>
  </verify>
  <acceptance_criteria>
    - Exact-string tests pass for persistent+CTA, hook enable/alpha, hard-cut, and multi-line wrap shapes
    - None returned for empty/whitespace text; RenderError battery covers mode/position/size/opacity/duration/fade
    - Every clause carries fontfile= (escaped path) and expansion=none; no clause ever contains font=
  </acceptance_criteria>
  <commit>feat(08-01): add build_hook_banner_filter chained-drawtext hook/persistent banner builder</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: thread banner_text through the three builders, render_clip guard, and CLI</name>
  <read_first>
    - scripts/render.py (lines 461-476 — build_ffmpeg_command's video_filter accumulation and the effects/subtitles insertion point; lines 726-748 — build_jumpcut_command's video_ops; lines 984-1006 — build_compilation_command's video_ops; lines 1092-1104 — render_clip's punch_zoom_at fail-loud guard to mirror; lines 1134-1158 — the ASS-bake block that sets subtitle_style to None (the collision guard MUST run before it); lines 1255-1348 — main() argparse flag conventions)
    - tests/test_render.py (lines 1397-1500 — render_clip profanity presence/absence mock-runner test style to mirror)
    - .planning/phases/08-hook-title-overlay/08-RESEARCH.md (Pattern 2 — insertion after punch-zoom before subtitles, and why; Code Examples > "Insertion point" + "render.py CLI flags")
  </read_first>
  <files>scripts/render.py, tests/test_render.py</files>
  <behavior>
    - build_ffmpeg_command/build_jumpcut_command/build_compilation_command each accept `banner_filter: str | None = None` (trailing keyword param, passed by keyword — Phase 07-02 precedent for not disturbing positional call sites) and insert it into video_filter/video_ops AFTER the effects chain and BEFORE the subtitles clause; banner_filter=None leaves each command byte-identical to today
    - render_clip with plan_entry["banner_text"] = "ТЕСТ ХУК" builds a command whose video filter contains a drawtext clause positioned after the punch-zoom/crop clauses and before subtitles=; without banner_text (or with it empty) the command equals today's exactly, across plain, keep_segments (jumpcut), and compilation branches
    - render_clip raises RenderError when banner_text is truthy AND subtitles_path is set AND banner_position == subtitle_style["position"] — checked BEFORE the ASS-bake block nulls subtitle_style, right after the punch_zoom_at guard
    - the CLI exposes --banner-mode (choices hook|persistent, default persistent), --banner-font, --banner-size, --banner-color, --banner-cta-text, --banner-cta-font, --banner-cta-size, --banner-cta-color, --banner-box-color, --banner-box-opacity, --banner-position (choices top|bottom, default top), --banner-duration-seconds, --banner-fade-seconds — all threaded to render_clip with defaults matching build_hook_banner_filter's
  </behavior>
  <action>
    Add the `banner_filter` keyword param to all three command builders and insert `if banner_filter: ...append/concat...` between the effects-chain block and the subtitles block at each of the three sites (lines 464-467, 729-732, 987-990) — the ordering is load-bearing: after punch-zoom so the zoom crop can never clip/shift the banner (Pattern 2), before subtitles to keep build_video_effects_chain's established "subtitles are the last visual layer" convention. In render_clip: read `banner_text = plan_entry.get("banner_text")` (same optional-field convention as punch_zoom_at/subtitles_path/profanity_spans); add banner params (banner_mode="persistent", banner_font="Arial Black", banner_size=58, banner_color="white", banner_cta_text="", banner_cta_font="Arial Bold", banner_cta_size=36, banner_cta_color="#ffe98a", banner_box_color="black", banner_box_opacity=0.55, banner_position="top", banner_duration_seconds=3.0, banner_fade_seconds=0.4); add the fail-loud collision guard (RenderError with a message naming both positions and pointing at config.hook_banner.position / config.subtitles.position — mirrors the punch_zoom_at guard's explanatory style) placed BEFORE the subtitles ASS-bake block, since that block sets subtitle_style to None; build `banner_filter = build_hook_banner_filter(banner_text, banner_mode, ...)` only when banner_text is truthy and pass it by keyword into all three command-builder branches. In main(), add the 13 --banner-* argparse flags per the behavior list (help-text style matching the existing --profanity-* flags, noting they are harmless no-ops for plan entries without banner_text) and thread them into the render_clip call. Tests (mock runner, mirroring the render_clip profanity tests): banner clause present + ordering asserted via substring indexes (drawtext index greater than the punch-zoom clause's index and less than the subtitles= index) in the plain branch; byte-identical command equality without banner_text across plain/jumpcut/compilation; collision RenderError (banner_text set, subtitles_path set, both positions "top"); no-collision pass when positions differ.
  </action>
  <verify>
    <automated>pytest -m "not integration" tests/test_render.py -k "banner" -x</automated>
  </verify>
  <acceptance_criteria>
    - Banner clause lands after punch-zoom/effects and before subtitles in all three branches (ordering asserted)
    - Commands are byte-identical to today when banner_text is absent/empty (HOOK-03)
    - Collision guard raises RenderError before any ffmpeg call when banner and subtitles share a position (HOOK-02)
    - CLI exposes the 13 --banner-* flags, threaded to render_clip
    - Full `pytest -m "not integration" tests/test_render.py -x` green (no regression)
  </acceptance_criteria>
  <commit>feat(08-01): thread banner_text through command builders, render_clip collision guard, and --banner-* CLI flags</commit>
</task>

</tasks>

<artifacts_produced>
This plan creates/extends:
- `scripts/render.py::HOOK_BANNER_FONT_PATHS` / `resolve_banner_font` / `_escape_drawtext_text` / `_wrap_banner_lines` — new helpers
- `scripts/render.py::build_hook_banner_filter(text, mode, cta_text, ...)` — new pure builder (chained drawtext, hook/persistent, CTA line)
- `scripts/render.py::build_ffmpeg_command` / `build_jumpcut_command` / `build_compilation_command` — each gains a `banner_filter` keyword pass-through param
- `scripts/render.py::render_clip` — reads `plan_entry["banner_text"]`, fail-loud subtitle-position collision guard, threads the banner through all branches
- `render.py` CLI flags: `--banner-mode`, `--banner-font`, `--banner-size`, `--banner-color`, `--banner-cta-text`, `--banner-cta-font`, `--banner-cta-size`, `--banner-cta-color`, `--banner-box-color`, `--banner-box-opacity`, `--banner-position`, `--banner-duration-seconds`, `--banner-fade-seconds`
- new tests in `tests/test_render.py`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLAN.json `banner_text` / config CTA text → ffmpeg filter graph | generator-controlled text crosses into the -vf/-filter_complex string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering / Input Validation | `build_hook_banner_filter` text interpolation | medium | mitigate | `_escape_drawtext_text` (backslash-first: \\, ', :, ,) on title AND CTA text before every `text=` interpolation, plus `expansion=none` on every clause closing the `%` strftime-expansion class — no raw text corrupts the filtergraph parse (ASVS V5; drawtext has no eval/exec surface, this is a correctness bug class, not code execution) |
| T-08-02 | DoS (off-frame overflow / oversized graph) | `_wrap_banner_lines` | low | mitigate | max_lines cap with fail-open warn+ellipsis truncation (Pitfall 4) — banner text can never grow the filter graph or draw off-frame unbounded |
| T-08-SC | Tampering | package installs | n/a | accept | No new packages — drawtext is compiled into the existing ffmpeg dependency; fonts are OS-shipped files (08-RESEARCH.md Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `pytest -m "not integration" tests/test_render.py -x` all green (existing + new)
- `python scripts/render.py --help` lists the --banner-* flags
</verification>

<success_criteria>
- A plan entry carrying `banner_text` renders a styled hook banner (hook or persistent mode, optional CTA line) drawn after punch-zoom and before subtitles; entries without it are byte-identical to today; a banner/subtitle position collision fails loud before ffmpeg runs — HOOK-01/HOOK-02/HOOK-03 render mechanics complete and unit-tested.
</success_criteria>

<output>
Create `.planning/phases/08-hook-title-overlay/08-01-SUMMARY.md` when done.
</output>
