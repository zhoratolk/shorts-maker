---
phase: 04-context-driven-transitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [TRANS-01]
user_setup: []
tags: [transitions, dependencies, opencv, librosa]

must_haves:
  truths:
    - "A human verified opencv-python-headless and librosa legitimacy on pypi.org before either is installed (D-04 enabling dep; TRANS-01)"
    - "cv2 and librosa import successfully from the project's .venv after install"
    - "The base pipeline still imports and runs without cv2/librosa present (Fail-open) — they are documented as optional, not core"
  artifacts:
    - "requirements.txt entries for opencv-python-headless and librosa under an optional-dependency comment block (transitions-only)"
  key_links:
    - "pip install is gated behind a blocking-human legitimacy checkpoint (SUS verdict per 04-RESEARCH.md Package Legitimacy Audit); threat T-04-SC mitigated here"
---

<objective>
Install the two optional dependencies this phase's boundary analysis needs — `opencv-python-headless` (dense optical flow + histogram similarity for match-cut) and `librosa` (audio onset, named explicitly in TRANS-01's own wording) — after a blocking-human legitimacy check, and register them in `requirements.txt` following the existing optional-dependency comment convention already used for `pyannote.audio` and the google-api clients.

Purpose: TRANS-01's motion/audio boundary analysis cannot run without these two libraries. Both were flagged `SUS` by the legitimacy tool (a download-count/age-signal limitation, not a substantive concern — see 04-RESEARCH.md Package Legitimacy Audit, which even flags `numpy` for the same reason), so the package-legitimacy protocol requires a blocking-human checkpoint before install. This plan writes NO analysis code; it only provisions and documents the dependencies the later plans lazy-import.

Output: `opencv-python-headless` + `librosa` installed in `.venv`; `requirements.txt` updated with an optional-dependency block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-context-driven-transitions/04-CONTEXT.md
@.planning/phases/04-context-driven-transitions/04-RESEARCH.md

# Existing optional-dependency comment convention this plan mirrors 1:1:
@requirements.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy checkpoint for opencv-python-headless and librosa</name>
  <read_first>.planning/phases/04-context-driven-transitions/04-RESEARCH.md (Package Legitimacy Audit + Security Domain sections)</read_first>
  <action>Pause for a blocking-human legitimacy review of the two SUS-flagged packages BEFORE any install runs. Present the pypi.org verification steps below; do not proceed to Task 2 until a human explicitly approves. This gate is never auto-approvable (workflow.auto_advance is ignored) and mitigates threat T-04-SC.</action>
  <what-built>Nothing installed yet. 04-RESEARCH.md's Package Legitimacy Audit flagged both packages `SUS` and explicitly attributes that verdict to a tool-signal limitation (no download-count API; the `too-new` signal keys off the latest-release date and fires even on numpy), not a real risk. Per the package-legitimacy protocol, a human must still confirm both packages before the install runs. Threat T-04-SC (supply-chain tampering via a new PyPI install) is mitigated by this gate.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/opencv-python-headless/ — confirm the source repo is github.com/opencv/opencv-python, the project is actively maintained, and the latest version matches 04-RESEARCH.md (~5.0.0.93).
    2. Open https://pypi.org/project/librosa/ — confirm the source repo is github.com/librosa/librosa, actively maintained, latest ~0.11.0.
    3. Confirm neither is a typosquat: exact spelling is opencv-python-headless (not opencv-headless / opencv-contrib-*) and librosa (not libirosa / librosaa).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or describe concerns. This checkpoint is NOT auto-approvable regardless of workflow.auto_advance.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages and register them in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt (the existing "# optional, only needed when ..." comment blocks above pyannote.audio and the google-api clients)</read_first>
  <action>Only after Task 1 is approved. Install both packages into the project venv using the venv's own interpreter — on this Windows machine `.venv\Scripts\python.exe -m pip install opencv-python-headless librosa` (04-RESEARCH.md verified both resolve to prebuilt win_amd64 wheels with zero source builds, so no compiler/CUDA/VC++ step is involved). Then append a new optional-dependency block to requirements.txt mirroring the pyannote.audio block's style exactly: a "# optional, only needed when transitions.enabled is true (Phase 4 boundary analysis)" comment immediately above the two requirements, then opencv-python-headless>=5.0 and librosa>=0.11 on their own lines. Use >= version floors (no pinned versions, no lockfile — matches the repo convention). Keep them OUT of the mandatory core: per the project Fail-open constraint they stay optional like pyannote, so the base install and every non-transitions code path must still work when they are absent (scripts/transitions.py lazy-imports them in plan 04-03). Do NOT add scenedetect — 04-RESEARCH.md documents that it pulls plain opencv-python alongside opencv-python-headless, a known cv2-module conflict; the match-cut proxy uses cv2.calcHist/compareHist directly instead.</action>
  <verify>
    <automated>cd D:/shorts-maker && .venv/Scripts/python.exe -c "import cv2, librosa; print(cv2.__version__, librosa.__version__)"</automated>
  </verify>
  <done>Both imports succeed from the project's venv Python and print versions; requirements.txt has both packages under an optional-dependency comment matching the pyannote block's shape; the two libraries are NOT promoted into the mandatory core (base install still works without them, since they are lazy-imported later).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local .venv | Third-party package code is installed and later executed in-process during boundary analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering / Elevation of Privilege | pip install of opencv-python-headless + librosa | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies both on pypi.org against their known GitHub repos before install, with an exact-spelling typosquat check; install runs only on explicit approval and is never auto-approvable |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -c "import cv2, librosa"` succeeds with no ImportError.
- `requirements.txt` diff shows both packages under an optional-dependency comment (not silently added to the mandatory core).
- The legitimacy checkpoint was approved by a human before the install command ran.
</verification>

<success_criteria>
opencv-python-headless and librosa are installed in the project venv and documented as optional dependencies, after human legitimacy sign-off. Downstream plans (04-03 analysis functions) can lazy-import them; the base install still works without them (Fail-open per PROJECT.md).
</success_criteria>

<output>
Create `.planning/phases/04-context-driven-transitions/04-01-SUMMARY.md` when done.
</output>
