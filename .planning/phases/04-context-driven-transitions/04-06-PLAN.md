---
phase: 04-context-driven-transitions
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - .claude/skills/make-shorts/SKILL.md
  - tests/test_integration_ffmpeg.py
autonomous: true
requirements: [TRANS-01, TRANS-02, TRANS-03]
user_setup: []
tags: [transitions, orchestration, integration, end-to-end]

must_haves:
  truths:
    - "When config.transitions.enabled is true and a clip has multi-segment keep_segments, the orchestrator computes per-boundary transition types via scripts/transitions.py select-transitions and records them as boundary_transitions in that clip's PLAN.json entry — fully automatic, no review gate (D-03)"
    - "The transition-selection step is fail-open: if the select-transitions command errors (e.g. cv2/librosa absent) the orchestrator omits boundary_transitions so render.py runs today's all-cut behavior (TRANS-03)"
    - "A real ffmpeg render of a forced non-cut boundary produces a playable 1080x1920 output (TRANS-01/02 end-to-end)"
  artifacts:
    - "SKILL.md: a transition-selection orchestration step between keep-segments and PLAN.json assembly, plus the boundary_transitions plan-entry field and the render.py --transition-duration/--min-overlap-seconds invocation flags, all gated on config.transitions.enabled"
    - "tests/test_integration_ffmpeg.py: a new integration test rendering a forced-crossfade boundary and asserting playable, correctly-dimensioned output"
  key_links:
    - "SKILL.md calls python scripts/transitions.py select-transitions <video> <keep_json> <out_json> with config.transitions threshold flags, then injects the JSON list verbatim as boundary_transitions (omitted when the command fails — Fail-open)"
    - "The integration test forces boundary_transitions on a keep_segments pair straddling the fixture's real 2s silent gap, exercising 04-05's build_jumpcut_command xfade fold on a real binary (a string assertion cannot catch a bad xfade offset/filter syntax)"
---

<objective>
Make context-driven transitions run automatically in the real pipeline and prove they render. Two pieces: (1) add the orchestration step to `SKILL.md` that, when `config.transitions.enabled`, runs the `select-transitions` CLI on each multi-segment clip's keep_segments and injects the resulting `boundary_transitions` list into that clip's `PLAN.json` entry (fully automatic per D-03, fail-open per TRANS-03); and (2) add a real-ffmpeg integration test that renders a forced non-cut boundary and asserts the output is playable and correctly dimensioned — the check a string assertion cannot make (a malformed xfade offset only fails at encode time).

Purpose: closes TRANS-01 end-to-end ("pipeline analyzes ... and selects a transition type at each boundary" — wired into the actual run, not just unit-tested), and provides the 04-VALIDATION.md 04-02-02 integration coverage for TRANS-01/02. This is the capstone that connects 04-04's selection to 04-05's rendering through the file-as-message-bus PLAN.json, keeping render.py a pure executor.
Output: reworked `SKILL.md` transition step + a new `integration`-marked test in `tests/test_integration_ffmpeg.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-context-driven-transitions/04-CONTEXT.md
@.planning/phases/04-context-driven-transitions/04-RESEARCH.md
@.claude/skills/make-shorts/SKILL.md   # keep-segments step ~135-139, PLAN.json schema ~189-202, render invocation ~207
@scripts/transitions.py                # select-transitions CLI (04-04)
@scripts/render.py                     # render_clip reads plan_entry["boundary_transitions"]; new --transition-duration/--min-overlap-seconds flags (04-05)
@tests/test_integration_ffmpeg.py      # test_jumpcut_splices_out_silence_gap pattern + the test_video fixture (2s tone / 2s silent gap / 2s tone)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md transition-selection orchestration step (automatic, fail-open)</name>
  <files>.claude/skills/make-shorts/SKILL.md</files>
  <read_first>.claude/skills/make-shorts/SKILL.md (the Jump cuts step at ~135-139 where keep_segments is produced; the PLAN.json object schema at ~189-202; the render.py invocation at ~207; the step 1b/1c/1d fail-open wording for how optional steps are described)</read_first>
  <behavior>
    - A new step (placed after the keep-segments step and before PLAN.json assembly) states: only when config.transitions.enabled is true AND this clip's keep_segments has more than one [start,end] pair, run `python scripts/transitions.py select-transitions "<video>" work/<video_stem>/jumpcuts/<clip_stem>_keep.json work/<video_stem>/transitions/<clip_stem>_boundary.json` with the config.transitions threshold flags (--transition-duration, --min-overlap-seconds, --strong-signal-percentile, --match-cut-similarity), then record the JSON list from that file verbatim as boundary_transitions in this clip's PLAN.json entry.
    - Fail-open (TRANS-03 / Fail-open constraint): if the command fails or the transitions deps are absent, omit boundary_transitions entirely — render.py then applies today's all-cut behavior. The step must say to log-and-continue, never abort the clip, exactly like the diarization/audio-energy optional steps.
    - The PLAN.json schema documentation gains boundary_transitions as an optional field (omitted when transitions are disabled/unused, matching how keep_segments/punch_zoom_at/subtitles_path are documented as omitted-when-unused).
    - The render.py invocation line is updated to also pass --transition-duration <config.transitions.transition_duration> and --min-overlap-seconds <config.transitions.min_overlap_seconds> (only meaningful when boundary_transitions were injected; harmless otherwise).
  </behavior>
  <action>Edit SKILL.md (use scoped Edits, do not rewrite the whole file). Add a "Context-driven transitions" step immediately after the Jump cuts step (~139), gated on config.transitions.enabled AND len(keep_segments) > 1, describing the select-transitions CLI call and the boundary_transitions injection exactly as in the behavior block, with explicit fail-open (log + continue, omit the field on any error — mirrors the 1c/1d optional-step wording). Note it is fully automatic with no CANDIDATES.md review gate (D-03). Add boundary_transitions to the PLAN.json object schema (~189-202) as an optional list-of-strings field with a one-line comment that it is omitted when transitions are disabled or the clip has a single segment, and that it must have exactly one entry per boundary (len(keep_segments)-1). Update the render.py command (~207) to append --transition-duration <config.transitions.transition_duration> --min-overlap-seconds <config.transitions.min_overlap_seconds>. Do not change any other pipeline step.</action>
  <verify>
    <human-check>SKILL.md's new transition step: (1) is gated on config.transitions.enabled and multi-segment keep_segments; (2) calls scripts/transitions.py select-transitions with the four threshold flags; (3) injects boundary_transitions into PLAN.json and documents it as an optional field; (4) states fail-open (omit on error, never abort); (5) updates the render.py invocation with the two new flags. Confirm no other step was altered.</human-check>
  </verify>
  <done>SKILL.md drives automatic, review-gate-free per-boundary transition selection (D-03) that fails open to all-cut (TRANS-03), threads config.transitions through, and documents boundary_transitions in the PLAN.json schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end real-ffmpeg integration test for a rendered transition</name>
  <files>tests/test_integration_ffmpeg.py</files>
  <read_first>tests/test_integration_ffmpeg.py:44-76 (test_video fixture: 2s tone / 2s silent gap / 2s tone, SRC 640x360) and 109-126 (test_jumpcut_splices_out_silence_gap pattern: build a plan_entry, call render_clip with a real runner, probe the output)</read_first>
  <behavior>
    - A new integration-marked test renders a clip whose keep_segments straddle the fixture's real silent gap — e.g. keep_segments=[[0.0, 2.0], [4.0, 6.0]] (gap 2.0s, far above min_overlap) — with plan_entry["boundary_transitions"]=["crossfade"] and crop_style "zoom".
    - render_clip runs the real ffmpeg command (no stub runner) and returns 0; probe of the output reports width 1080 and height 1920 (correctly dimensioned) and a video+audio stream (playable).
    - The test self-skips when ffmpeg/ffprobe are absent (inherits the file's pytestmark) and needs NO cv2/librosa — the transition type is forced directly, exercising 04-05's xfade fold on a real binary without depending on the analysis libraries.
    - The test name contains "transition" so `pytest tests/test_integration_ffmpeg.py -x -m integration -k transition` selects it (04-VALIDATION.md 04-02-02 binding command).
  </behavior>
  <action>Add a test following the test_jumpcut_splices_out_silence_gap pattern (real render_clip call against the test_video fixture, probe the output). Construct plan_entry with start/end spanning the fixture (0.0..6.0), crop_style "zoom", keep_segments=[[0.0, 2.0], [4.0, 6.0]], and boundary_transitions=["crossfade"]. Call render_clip with the real subprocess runner (default), video_duration=SRC_DURATION, src_width=SRC_WIDTH, src_height=SRC_HEIGHT, transition_duration and min_overlap_seconds at their defaults. Assert the returned command contains -filter_complex with "xfade=transition=fade" (the fold path was taken) and that probe(output) reports the video stream width==1080/height==1920 and that an audio stream exists (playable via acrossfade). Keep the source small (reuse the module fixture) to keep runtime reasonable, per the fixture's own docstring rationale. Do NOT import cv2/librosa — force the transition to isolate the render path.</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_integration_ffmpeg.py -x -m integration -k transition -q</automated>
  </verify>
  <done>The integration test renders a forced-crossfade boundary end-to-end on real ffmpeg, producing a playable 1080x1920 output with an xfade in the filter graph; it self-skips without ffmpeg and needs no cv2/librosa; selected by `-m integration -k transition`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| select-transitions CLI output → PLAN.json | The orchestrator injects the CLI's JSON list into the plan; a failed/absent command must degrade to omitting the field (all-cut), never inject a partial/garbage list |
| PLAN.json boundary_transitions → render.py | The rendered field is re-validated by render.py (04-05) against the enum and boundary count before any filter dispatch (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-ORCH | Denial of Service | select-transitions step in the pipeline | low | mitigate | The step is gated on config.transitions.enabled and fails open (log + continue, omit boundary_transitions) on any error or missing dep, so a transitions failure never aborts a clip render (TRANS-03 / Fail-open constraint) |
| T-04-INJ2 | Tampering | boundary_transitions flowing PLAN.json → render | low | mitigate | render.py (04-05) re-validates every boundary_transitions entry against VALID_TRANSITIONS and the boundary count, raising RenderError before building a filter graph — the orchestrator is not the only line of defense (V5) |
</threat_model>

<verification>
- `pytest tests/test_integration_ffmpeg.py -x -m integration -k transition` green on this machine (ffmpeg present); self-skips where ffmpeg is absent.
- Full suite: `pytest -x` green (integration included, since ffmpeg is on PATH) before phase gate.
- SKILL.md review confirms automatic (no review gate, D-03), config-gated, fail-open transition selection wired between keep-segments and PLAN.json.
</verification>

<success_criteria>
- TRANS-01 end-to-end: with transitions enabled, the pipeline automatically analyzes each multi-segment clip's boundaries and records the chosen transition types into PLAN.json without a manual step (D-03).
- TRANS-02/03: a forced non-cut boundary renders to a playable, correctly-dimensioned clip on real ffmpeg; a transitions failure or missing dep degrades cleanly to all-cut.
</success_criteria>

<output>
Create `.planning/phases/04-context-driven-transitions/04-06-SUMMARY.md` when done.
</output>
