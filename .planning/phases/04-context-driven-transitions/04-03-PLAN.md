---
phase: 04-context-driven-transitions
plan: 03
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/transitions.py
  - tests/test_transitions.py
autonomous: true
requirements: [TRANS-01]
user_setup: []
tags: [transitions, opencv, librosa, analysis, fail-open]

must_haves:
  truths:
    - "scripts/transitions.py imports cleanly with neither cv2 nor librosa installed (module top-level is stdlib-only; heavy deps are lazy-imported inside the functions that use them)"
    - "analyze_motion_at_boundary / analyze_similarity_at_boundary / analyze_audio_onset_at_boundary each return None (not raise) when their optional dependency is unimportable — the inconclusive sentinel driving TRANS-03 fallback"
    - "With cv2/librosa present, each analysis function returns a finite float score on real fixture inputs (TRANS-01)"
    - "TRANSITION_TYPES is a frozenset of exactly the 6 required type strings, and TransitionError subclasses a builtin"
  artifacts:
    - "scripts/transitions.py: module skeleton, TRANSITION_TYPES enum, TransitionError, three analysis functions, extract_audio_window helper"
    - "tests/test_transitions.py: new file (Wave-0 gap closure) covering enum, fail-open None paths, and real-score paths"
  key_links:
    - "Lazy-import fail-open mirrors scripts/diarize.py:72-78 — import inside the function body, return None on ImportError, never at module top level, so the whole optional feature stays disable-able (Fail-open constraint)"
    - "extract_audio_window and any ffmpeg-invoking helper take runner=subprocess.run so unit tests substitute a stub without a real binary (project injectable-runner convention)"
---

<objective>
Create the new `scripts/transitions.py` module and its `tests/test_transitions.py` (the Wave-0 scaffold flagged by 04-VALIDATION.md), delivering the three independent, unit-testable signal-analysis functions that TRANS-01 requires: dense optical-flow motion, spectral-flux audio onset, and a histogram-similarity match-cut proxy. Each is lazy-imported and fail-open (returns None on missing dependency or inconclusive input), keeping the module — and thus the whole transitions feature — importable and disable-able without cv2/librosa installed.

Purpose: TRANS-01 ("analyzes clip-boundary motion via optical flow and audio via energy/onset via librosa"). These pure-ish analysis functions are the signal layer the classifier (04-04) consumes. Splitting them out from the decision logic keeps this plan light and each function independently testable, matching the module-per-concern, pure-function-plus-injectable-runner house style (04-PATTERNS.md maps this file onto scripts/audio_energy.py + scripts/diarize.py + scripts/frames.py).

Output: `scripts/transitions.py` (skeleton + enum + error type + 3 analysis functions + audio-window extractor) and `tests/test_transitions.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-context-driven-transitions/04-RESEARCH.md
@.planning/phases/04-context-driven-transitions/04-PATTERNS.md
@scripts/diarize.py          # lazy-import fail-open pattern (load_diarization_pipeline, lines 72-85)
@scripts/frames.py           # ffmpeg still-extraction + injectable runner + module-specific *Error(ValueError)
@scripts/audio_energy.py     # module shape, argparse main, pure-function decomposition
@tests/test_jumpcuts.py      # plain top-level test_* function style, no test classes, no mock library
---
# INTERFACE CONTRACT (canonical, consumed by 04-04 and 04-05):
# TRANSITION_TYPES = frozenset({"cut", "crossfade", "whip_pan", "mask_wipe", "glitch", "match_cut"})
# analyze_motion_at_boundary(frame_a_path: str, frame_b_path: str) -> float | None
# analyze_similarity_at_boundary(frame_a_path: str, frame_b_path: str) -> float | None
# analyze_audio_onset_at_boundary(audio_window_path: str) -> float | None
# extract_audio_window(video_path, center_time, duration, output_path, runner=subprocess.run) -> str
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module skeleton, TRANSITION_TYPES enum, TransitionError, and Wave-0 test scaffold</name>
  <files>scripts/transitions.py, tests/test_transitions.py</files>
  <read_first>scripts/audio_energy.py (top-of-file imports + module docstring shape); scripts/frames.py:1-10 (FrameExtractionError definition); 04-PATTERNS.md (transitions.py module header + error-type + enum assignments)</read_first>
  <behavior>
    - importing scripts.transitions succeeds in an environment with NEITHER cv2 NOR librosa installed (top-level imports are stdlib only: from __future__ import annotations, argparse, json, subprocess, pathlib).
    - TRANSITION_TYPES is a module-level frozenset equal to {"cut", "crossfade", "whip_pan", "mask_wipe", "glitch", "match_cut"} — len 6.
    - TransitionError is a subclass of ValueError (like RenderError/ConfigError/FrameExtractionError).
    - tests/test_transitions.py imports the module and asserts TRANSITION_TYPES has exactly those 6 members, so `pytest tests/test_transitions.py -x` is a real green command from this plan onward (closes the 04-VALIDATION.md Wave-0 gap).
  </behavior>
  <action>Create scripts/transitions.py following the house module shape (04-PATTERNS.md header pattern): from __future__ import annotations then stdlib-only top-level imports (argparse, json, subprocess, pathlib.Path). Add a module docstring stating the file is the boundary motion/audio/similarity analysis + transition-type decision layer, and that cv2/librosa are lazy-imported so the module stays importable without them (Fail-open). Define TRANSITION_TYPES = frozenset({"cut", "crossfade", "whip_pan", "mask_wipe", "glitch", "match_cut"}) as the single canonical enum (04-04 and 04-05 both key off this exact set — it is the fixed 6-item enum the render layer validates against, per 04-PATTERNS.md and 04-RESEARCH.md Security Domain V5). Define class TransitionError(ValueError): pass. Create tests/test_transitions.py in the tests/test_jumpcuts.py style (plain module-level test_* functions, pytest, no mock library) asserting the enum membership and count. Do NOT put cv2/librosa at module top level.</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_transitions.py -x -q</automated>
  </verify>
  <done>scripts/transitions.py imports with no cv2/librosa present; TRANSITION_TYPES has exactly the 6 members; TransitionError subclasses ValueError; `pytest tests/test_transitions.py -x` runs and passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motion (optical-flow) and match-cut (histogram) analysis over boundary frames</name>
  <files>scripts/transitions.py, tests/test_transitions.py</files>
  <read_first>scripts/diarize.py:72-85 (lazy-import inside function body); 04-RESEARCH.md Code Examples (Farneback optical flow + cv2.calcHist/compareHist); scripts/frames.py:31-53 (extract_frames call shape for producing the fixture frames)</read_first>
  <behavior>
    - analyze_motion_at_boundary(frame_a_path, frame_b_path) lazy-imports cv2 inside the function; on ImportError returns None (fail-open — the caller treats None as inconclusive → TRANS-03). With cv2 present it reads both frames grayscale, runs cv2.calcOpticalFlowFarneback, and returns float(magnitude.mean()) — a finite, non-negative float. Two identical frames give a near-zero score; two visibly different frames give a strictly larger score.
    - analyze_similarity_at_boundary(frame_a_path, frame_b_path) lazy-imports cv2; on ImportError returns None. With cv2 present it computes per-frame histograms (cv2.calcHist) and returns cv2.compareHist(..., cv2.HISTCMP_CORREL) as a float in roughly [-1, 1] — two identical frames give ~1.0 (high similarity → match_cut candidate).
    - Both functions return None (not raise) if a frame path is missing/unreadable, keeping the whole boundary inconclusive rather than aborting the render.
  </behavior>
  <action>Implement analyze_motion_at_boundary and analyze_similarity_at_boundary in scripts/transitions.py using the diarize.py lazy-import fail-open shape: try: import cv2 / except ImportError: return None, at the top of each function body (never module level). For motion use cv2.calcOpticalFlowFarneback(prev_gray, next_gray, None, 0.5, 3, 15, 3, 5, 1.2, 0) then cv2.cartToPolar on the flow channels and return float(magnitude.mean()) — 04-RESEARCH.md Code Examples has the exact parameter defaults. For similarity use cv2.calcHist + cv2.compareHist(method=cv2.HISTCMP_CORREL) — a ~10-line proxy that reuses the same cv2 import, NO scenedetect (04-RESEARCH.md Anti-Patterns: scenedetect pulls plain opencv-python and collides with opencv-python-headless). If cv2.imread returns None for either path (unreadable), return None rather than raising. Add tests to tests/test_transitions.py: (a) a fail-open test that forces the None path deterministically — inject the missing-dependency behavior without requiring cv2 to be absent, e.g. by monkeypatching builtins.__import__ so importing cv2 raises ImportError, asserting the function returns None; (b) real-score tests gated by pytest.importorskip("cv2") that generate two solid-color and two different fixture frames (write small images via cv2 or reuse scripts/frames.extract_frames against a tiny generated clip) and assert identical frames → similarity ~1.0 and near-zero motion, differing frames → larger motion. Name the analysis tests so `pytest tests/test_transitions.py -x -k analyze` selects them (04-VALIDATION.md 04-01-01 binding command).</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_transitions.py -x -k analyze -q</automated>
  </verify>
  <done>Both functions return None on missing cv2 (deterministic monkeypatched test) and finite floats on real fixture frames (importorskip-gated); identical frames score ~1.0 similarity and ~0 motion; `pytest tests/test_transitions.py -x -k analyze` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Audio-onset analysis and boundary audio-window extraction</name>
  <files>scripts/transitions.py, tests/test_transitions.py</files>
  <read_first>04-RESEARCH.md Code Examples (librosa.onset.onset_strength); scripts/diarize.py:60-69 (extract_audio_wav ffmpeg call shape + runner); scripts/audio_energy.py (why librosa onset, not the ebur128 pattern, is used here)</read_first>
  <behavior>
    - analyze_audio_onset_at_boundary(audio_window_path) lazy-imports librosa inside the function; on ImportError returns None (fail-open). With librosa present it loads the window (librosa.load(..., sr=None)), computes librosa.onset.onset_strength, and returns float(onset_env.max()) — a finite float; a window containing a sharp transient scores higher than a steady/near-silent window.
    - extract_audio_window(video_path, center_time, duration, output_path, runner=subprocess.run) builds an ffmpeg argument LIST (never shell=True) that slices a short mono wav centered on center_time (-ss before -i for fast seek, -t duration, -vn -ac 1), runs runner, raises TransitionError on nonzero returncode, and returns output_path. Injectable runner lets a unit test assert the command shape without a real binary.
  </behavior>
  <action>Implement analyze_audio_onset_at_boundary using the diarize.py lazy-import fail-open shape (try: import librosa / except ImportError: return None). Return float(librosa.onset.onset_strength(y=y, sr=sr).max()) per 04-RESEARCH.md — librosa is named explicitly in TRANS-01's wording, so use it (not audio_energy.py's ebur128 spike detector, which cannot distinguish a transient attack from a sustained loud passage — 04-RESEARCH.md Alternatives Considered). Implement extract_audio_window as an ffmpeg-argument-LIST subprocess wrapper with the injectable runner=subprocess.run parameter (project convention: scripts/render.py:417 probe_video, scripts/frames.py:32, scripts/diarize.py:60) — clamp the window start at max(0.0, center_time - duration/2). Raise TransitionError(f"...") on nonzero returncode, mirroring FrameExtractionError. Add tests: (a) fail-open None on missing librosa via the same monkeypatched-__import__ technique from Task 2; (b) a real-score test gated by pytest.importorskip("librosa") on a tiny generated tone-vs-transient wav; (c) an extract_audio_window test passing a stub runner (captures the command list) asserting -ss precedes -i, -t equals the duration, -ac 1 is present, and no shell string interpolation is used. Name these so `pytest tests/test_transitions.py -x -k analyze` still selects the onset test.</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_transitions.py -x -q</automated>
  </verify>
  <done>analyze_audio_onset_at_boundary returns None on missing librosa and a finite float on a real fixture; extract_audio_window builds a shell-free argument list (stub-runner test) and raises TransitionError on failure; full `pytest tests/test_transitions.py -x` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator video/frame files → analysis functions | Local operator-supplied media is decoded by cv2/librosa/ffmpeg; not attacker-controlled, but unreadable/corrupt input must fail-open (None), not crash the render |
| function args → ffmpeg (extract_audio_window) | center_time/duration are internally-computed floats interpolated into an ffmpeg argument list, never a shell string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-INJ-A | Tampering | extract_audio_window ffmpeg invocation | medium | mitigate | Command is built as an argument list (no shell=True, no string interpolation into a shell); timing args are internally-computed floats — mirrors every existing subprocess wrapper in this repo (V5 Input Validation) |
| T-04-FAILOPEN | Denial of Service | analysis on missing dep / unreadable media | low | mitigate | Lazy-import returns None on ImportError and functions return None on unreadable frames rather than raising — the render degrades to today's cut/punch-zoom (TRANS-03), never aborts |
</threat_model>

<verification>
- `pytest tests/test_transitions.py -x` green (both fail-open and importorskip-gated real-score paths).
- `python -c "import scripts.transitions"` succeeds even with cv2/librosa uninstalled (top-level is stdlib-only) — confirm by inspecting that cv2/librosa appear only inside function bodies.
- extract_audio_window's stub-runner test proves the ffmpeg call is a shell-free argument list.
</verification>

<success_criteria>
- TRANS-01 signal layer exists: motion (optical flow), audio onset (librosa), and match-cut similarity (histogram) each return sane floats on real input and None on missing dependency/inconclusive input.
- The module and feature stay fully optional/import-safe without cv2/librosa (Fail-open).
- tests/test_transitions.py is the green Wave-0 scaffold the rest of the phase's unit tests extend.
</success_criteria>

<output>
Create `.planning/phases/04-context-driven-transitions/04-03-SUMMARY.md` when done.
</output>
