---
phase: 04-context-driven-transitions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/config.py
  - config.example.yaml
  - scripts/jumpcuts.py
  - tests/test_config.py
  - tests/test_jumpcuts.py
autonomous: true
requirements: [TRANS-01, TRANS-03]
user_setup: []
tags: [transitions, config, jumpcuts]

must_haves:
  truths:
    - "A config.yaml transitions: section loads into a TransitionsConfig dataclass with enabled=False default (opt-in, conservative default per D-01, Fail-open)"
    - "_validate rejects out-of-range transition config values with a ConfigError, following the jumpcuts/_validate precedent (V5 input validation)"
    - "compute_boundary_gaps(keep_segments) returns the cut pause-gap seconds at each adjacent boundary, one value per boundary (len == len(keep_segments) - 1)"
  artifacts:
    - "scripts/config.py: TransitionsConfig dataclass + Config.transitions field + _build wiring + _validate block"
    - "config.example.yaml: transitions: section with commented empirical defaults (D-02)"
    - "scripts/jumpcuts.py: compute_boundary_gaps pure function"
    - "tests/test_config.py + tests/test_jumpcuts.py assertions"
  key_links:
    - "Config.transitions resolves via _build(TransitionsConfig, data.get('transitions', {}), 'transitions'), so a missing section defaults to disabled (D-01 conservative default)"
    - "compute_boundary_gaps is pure over keep_segments (gap N = keep_segments[N+1][0] - keep_segments[N][1]); no pause file re-read — feeds the overlap-availability / gap-fallback check consumed by 04-04 (select) and 04-05 (render fold) for TRANS-03"
---

<objective>
Add the two pure foundational helpers the transition engine builds on. Both extend existing files 1:1 against their in-file analogs, and neither depends on the new cv2/librosa libraries — so this plan can run in Wave 1 in parallel with the dependency install (04-01):

1. `TransitionsConfig` — a new config dataclass + `_validate` block + `config.example.yaml` section, copying the `JumpcutsConfig`/`AudioEnergyConfig` template exactly (04-PATTERNS.md config assignment), with `enabled: bool = False` first (opt-in, matching every optional feature).
2. `compute_boundary_gaps` — a sibling pure function in `jumpcuts.py` that surfaces the pause-gap size at each keep-segment boundary (the unused source footage 04-RESEARCH.md Pitfall 1 borrows for xfade overlap).

Purpose: gives 04-04 (classifier/select) its config thresholds and gives 04-05 (render fold) the per-boundary gap data needed to decide whether a boundary has enough borrowable overlap for a non-cut transition or must fall back to a plain cut (TRANS-03).
Output: config schema + validation; a pure gap-exposure function; unit tests for both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-context-driven-transitions/04-CONTEXT.md
@.planning/phases/04-context-driven-transitions/04-RESEARCH.md
@.planning/phases/04-context-driven-transitions/04-PATTERNS.md
@scripts/config.py
@scripts/jumpcuts.py
@config.example.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TransitionsConfig dataclass, validation, and config.example.yaml section</name>
  <files>scripts/config.py, config.example.yaml, tests/test_config.py</files>
  <read_first>scripts/config.py (JumpcutsConfig lines 126-130, AudioEnergyConfig 112-124, Config aggregate 178-197, _build 200-204, load_config 216-237, the jumpcuts _validate block 279-290); config.example.yaml (jumpcuts: block near line 190, audio_energy: near line 67 for the inline-comment tone)</read_first>
  <behavior>
    - Loading a config with no transitions: section yields config.transitions.enabled is False and the documented field defaults.
    - Loading transitions: {enabled: true, transition_duration: 0.35, min_overlap_seconds: 0.12, strong_signal_percentile: 85.0, match_cut_similarity: 0.90} populates the dataclass.
    - transition_duration <= 0 raises ConfigError.
    - min_overlap_seconds <= 0 raises ConfigError.
    - min_overlap_seconds > transition_duration raises ConfigError (a floor above the whole duration is nonsensical).
    - strong_signal_percentile outside the open interval 0..100 raises ConfigError.
    - match_cut_similarity outside the closed interval 0..1 raises ConfigError.
    - An unknown field inside transitions: raises ConfigError via the existing _build TypeError wrapper.
  </behavior>
  <action>Add TransitionsConfig following the JumpcutsConfig template (04-PATTERNS.md config assignment). Field order/defaults: enabled: bool = False first (matches the opt-in/Fail-open convention every optional feature uses — DiarizationConfig/AudioEnergyConfig/JumpcutsConfig all default enabled=False; this satisfies D-01's conservative-default intent at the config layer); transition_duration: float = 0.35 (xfade/borrowed-overlap window; short so transitions read snappy, not drawn-out, per D-01); min_overlap_seconds: float = 0.12 (below this much borrowed overlap a transition is imperceptible, so the boundary falls back to a plain cut — TRANS-03); strong_signal_percentile: float = 85.0 (a boundary's motion/audio score must exceed this percentile of THIS video's own boundary-score distribution before a non-cut transition is chosen — adaptive, not a fixed magic number, per 04-RESEARCH.md Pitfall 4 and D-01/D-02); match_cut_similarity: float = 0.90 (histogram correlation at/above this reads as visually continuous, selecting match_cut). Put an inline why-comment above each threshold matching AudioEnergyConfig's comment tone (config.py lines 115-120) and stating explicitly that these thresholds are empirical starting points to tune after watching real renders (D-02). Wire transitions: TransitionsConfig = dataclasses.field(default_factory=TransitionsConfig) into the Config aggregate and add the _build(TransitionsConfig, data.get("transitions", {}), "transitions") line in load_config next to the jumpcuts line. Add the _validate block per the behavior list, copying the jumpcuts block's raise ConfigError(f"...") message shape verbatim in structure. In config.example.yaml, add a transitions: section after the jumpcuts: block with the same field values and short leading-hash comments noting each is an empirical, tune-after-watching default. This mitigates threat T-04-CFG (invalid config values reaching a filter graph) — validation fails loudly at load time. Add tests to tests/test_config.py in the existing style (default-value assertions plus ConfigError-raising cases), with at least one test name containing "transition" so `pytest tests/test_config.py -x -k transition` selects them.</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_config.py -x -k transition -q</automated>
  </verify>
  <done>TransitionsConfig loads with correct defaults when the section is absent; each out-of-range value raises ConfigError; config.example.yaml has a documented transitions: section; full `pytest tests/test_config.py -x` stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compute_boundary_gaps pure function in jumpcuts.py</name>
  <files>scripts/jumpcuts.py, tests/test_jumpcuts.py</files>
  <read_first>scripts/jumpcuts.py (compute_keep_segments lines 8-37, total_kept_duration 40-41, remap_timestamp docstring 44-49 for the why-comment tone)</read_first>
  <behavior>
    - compute_boundary_gaps([(0.0, 10.0)]) returns [] (a single segment has zero boundaries).
    - compute_boundary_gaps([(0.0, 10.0), (12.5, 20.0)]) returns [2.5] (the cut pause between them).
    - compute_boundary_gaps([(0.0, 5.0), (6.0, 9.0), (9.0, 14.0)]) returns [1.0, 0.0] — one gap per adjacent boundary, including a zero gap when segments abut.
    - Result length always equals len(keep_segments) - 1.
  </behavior>
  <action>Add compute_boundary_gaps(keep_segments: list[tuple[float, float]]) -> list[float] as a sibling pure function alongside total_kept_duration. For each adjacent pair, gap = keep_segments[N+1][0] - keep_segments[N][1] (absolute source seconds) — this is exactly the pause footage compute_keep_segments already cut out, recoverable directly from the kept segments with no need to re-read the pause file (04-PATTERNS.md jumpcuts assignment: surface the same data instead of discarding it). Include a docstring in remap_timestamp's tone explaining WHY the gap matters: it is the unused source footage 04-RESEARCH.md Pitfall 1 borrows as the xfade overlap window so a non-cut transition never eats into real kept content. No I/O, no subprocess, plain lists in and out — mirrors the module's pure-function style. Do NOT add a CLI subcommand (render.py and transitions.py call it internally; 04-PATTERNS.md notes a standalone CLI entry point is unnecessary). Add tests to tests/test_jumpcuts.py in the existing plain-function style with at least one test name containing "boundary_gap" so `pytest tests/test_jumpcuts.py -x -k boundary_gap` selects them.</action>
  <verify>
    <automated>cd D:/shorts-maker && python -m pytest tests/test_jumpcuts.py -x -k boundary_gap -q</automated>
  </verify>
  <done>compute_boundary_gaps returns one gap per boundary with correct values including zero-gap abutting segments and empty for a single segment; full `pytest tests/test_jumpcuts.py -x` stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → pipeline | Operator-supplied config values are read into typed dataclasses and later shape ffmpeg filter parameters (duration/overlap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-CFG | Tampering | transitions: config fields consumed by render.py filter builders | medium | mitigate | _validate rejects non-positive durations/overlaps, an overlap floor above the duration, out-of-(0,100) percentile, and out-of-[0,1] similarity with ConfigError before any value reaches a filter string (V5 Input Validation, mirrors the jumpcuts validation block) |
| T-04-GAP | Denial of Service | compute_boundary_gaps over degenerate segments | low | accept | Pure arithmetic over already-validated keep_segments; degenerate (zero/negative) gaps are surfaced as-is and the 04-04/04-05 consumers clamp or fall back to a plain cut — no crash surface here |
</threat_model>

<verification>
- `pytest tests/test_config.py -x` and `pytest tests/test_jumpcuts.py -x` both green.
- A config with no transitions: section loads with enabled=False.
- compute_boundary_gaps output length == boundaries count for representative inputs.
</verification>

<success_criteria>
TransitionsConfig is a validated, opt-in config section with empirical documented defaults (D-01/D-02), and compute_boundary_gaps exposes per-boundary pause-gap seconds — the two pure inputs the classifier (04-04) and render fold (04-05) consume for conservative selection and TRANS-03 gap fallback.
</success_criteria>

<output>
Create `.planning/phases/04-context-driven-transitions/04-02-SUMMARY.md` when done.
</output>
