# Base install - enough for the core pipeline (transcribe, analyze, render).
# Optional features are pip extras (see pyproject.toml [project.optional-dependencies]):
#   pip install .[diarization]   # speaker diarization (pyannote.audio + torch, heavy)
#   pip install .[transitions]   # context-driven transitions (opencv, librosa)
#   pip install .[publish]       # YouTube analytics/publish queue, TikTok/Instagram publish
#   pip install .[all]           # everything above
faster-whisper>=1.0.0
PyYAML>=6.0
